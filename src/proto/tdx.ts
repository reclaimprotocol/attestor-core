// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.31.1
// source: tdx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tdx";

export interface QuoteV4 {
  /** Header of quote structure */
  header:
    | Header
    | undefined;
  /** TD report by which quote is generated */
  tdQuoteBody:
    | TDQuoteBody
    | undefined;
  /** Size of the Quote Signature Data structure */
  signedDataSize: number;
  /** Quote Signature Data structure. */
  signedData:
    | Ecdsa256BitQuoteV4AuthData
    | undefined;
  /** Extra bytes included to fill the quote buffer */
  extraBytes: Uint8Array;
}

export interface Header {
  /** Version 4 supported */
  version: number;
  /**
   * Type of attestation key used by quoting enclave
   * Values :
   * 2 (ECDSA-256-with-P-256 curve)
   * 3 (ECDSA-384-with-P-384 curve) (Currently not supported)
   */
  attestationKeyType: number;
  /**
   * TEE for this attestation
   * TDX : 0x00000081
   */
  teeType: number;
  /** should be 2 bytes */
  qeSvn: Uint8Array;
  /** should be 2 bytes */
  pceSvn: Uint8Array;
  /** Unique vendor id of QE vendor */
  qeVendorId: Uint8Array;
  /** Custom user defined data */
  userData: Uint8Array;
}

export interface TDQuoteBody {
  /** should be 16 bytes */
  teeTcbSvn: Uint8Array;
  /** should be 48 bytes */
  mrSeam: Uint8Array;
  /** should be 48 bytes */
  mrSignerSeam: Uint8Array;
  /** should be 8 bytes */
  seamAttributes: Uint8Array;
  /** should be 8 bytes */
  tdAttributes: Uint8Array;
  /** should be 8 bytes */
  xfam: Uint8Array;
  /** should be 48 bytes */
  mrTd: Uint8Array;
  /** should be 48 bytes */
  mrConfigId: Uint8Array;
  /** should be 48 bytes */
  mrOwner: Uint8Array;
  /** should be 48 bytes */
  mrOwnerConfig: Uint8Array;
  /** should be 48 * rtmrsCount */
  rtmrs: Uint8Array[];
  /** should be 64 bytes */
  reportData: Uint8Array;
}

export interface Ecdsa256BitQuoteV4AuthData {
  /** The ECDSA 256-bit signature. */
  signature: Uint8Array;
  /** The ECDSA 256-bit public key of the attestation key. */
  ecdsaAttestationKey: Uint8Array;
  /** The certification data. */
  certificationData: CertificationData | undefined;
}

export interface CertificationData {
  /**
   * Supported values:
   * - 1 (PCK identifier: PPID in plain text,  CPUSVN and PCESVN)
   * - 2 (PCK identifier: PPID encrypted using RSA-2048-OAEP, CPUSVN and PCESVN)
   * - 3 (PCK identifier: PPID encrypted using RSA-3072-OAEP, CPUSVN and PCESVN)
   * - 4 (PCK Leaf Certificate in plain text,  currently not supported)
   * - 5 (Concatenated PCK Cert Chain)
   * - 6 (QE Report Certification Data)
   * - 7 (PLATFORM_MANIFEST, currently  not supported)
   */
  certificateDataType: number;
  /** Size of Certification Data field */
  size: number;
  qeReportCertificationData: QEReportCertificationData | undefined;
}

export interface QEReportCertificationData {
  qeReport:
    | EnclaveReport
    | undefined;
  /** should be 64 bytes */
  qeReportSignature: Uint8Array;
  qeAuthData: QeAuthData | undefined;
  pckCertificateChainData: PCKCertificateChainData | undefined;
}

export interface PCKCertificateChainData {
  /**
   * Supported values:
   * - 1 (PCK identifier: PPID in plain text,  CPUSVN and PCESVN)
   * - 2 (PCK identifier: PPID encrypted using RSA-2048-OAEP, CPUSVN and PCESVN)
   * - 3 (PCK identifier: PPID encrypted using RSA-3072-OAEP, CPUSVN and PCESVN)
   * - 4 (PCK Leaf Certificate in plain text,  currently not supported)
   * - 5 (Concatenated PCK Cert Chain)
   * - 6 (QE Report Certification Data)
   * - 7 (PLATFORM_MANIFEST, currently  not supported)
   */
  certificateDataType: number;
  /** Size of Certification Data field */
  size: number;
  pckCertChain: Uint8Array;
}

export interface QeAuthData {
  /** The parsed data size. */
  parsedDataSize: number;
  /** The data. */
  data: Uint8Array;
}

export interface EnclaveReport {
  /** should be 16 bytes */
  cpuSvn: Uint8Array;
  /** should be 4 bytes */
  miscSelect: number;
  /** should be 28 bytes */
  reserved1: Uint8Array;
  /** should be 16 bytes */
  attributes: Uint8Array;
  /** should be 32 bytes */
  mrEnclave: Uint8Array;
  /** should be 32 bytes */
  reserved2: Uint8Array;
  /** should be 32 bytes */
  mrSigner: Uint8Array;
  /** should be 96 bytes */
  reserved3: Uint8Array;
  /** should be 2 bytes */
  isvProdId: number;
  /** should be 2 bytes */
  isvSvn: number;
  /** should be 60 bytes */
  reserved4: Uint8Array;
  /** should be 64 bytes */
  reportData: Uint8Array;
}

function createBaseQuoteV4(): QuoteV4 {
  return {
    header: undefined,
    tdQuoteBody: undefined,
    signedDataSize: 0,
    signedData: undefined,
    extraBytes: new Uint8Array(0),
  };
}

export const QuoteV4: MessageFns<QuoteV4> = {
  encode(message: QuoteV4, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      Header.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.tdQuoteBody !== undefined) {
      TDQuoteBody.encode(message.tdQuoteBody, writer.uint32(18).fork()).join();
    }
    if (message.signedDataSize !== 0) {
      writer.uint32(24).uint32(message.signedDataSize);
    }
    if (message.signedData !== undefined) {
      Ecdsa256BitQuoteV4AuthData.encode(message.signedData, writer.uint32(34).fork()).join();
    }
    if (message.extraBytes.length !== 0) {
      writer.uint32(42).bytes(message.extraBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuoteV4 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuoteV4();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = Header.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tdQuoteBody = TDQuoteBody.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.signedDataSize = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.signedData = Ecdsa256BitQuoteV4AuthData.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.extraBytes = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuoteV4 {
    return {
      header: isSet(object.header) ? Header.fromJSON(object.header) : undefined,
      tdQuoteBody: isSet(object.tdQuoteBody)
        ? TDQuoteBody.fromJSON(object.tdQuoteBody)
        : isSet(object.td_quote_body)
        ? TDQuoteBody.fromJSON(object.td_quote_body)
        : undefined,
      signedDataSize: isSet(object.signedDataSize)
        ? globalThis.Number(object.signedDataSize)
        : isSet(object.signed_data_size)
        ? globalThis.Number(object.signed_data_size)
        : 0,
      signedData: isSet(object.signedData)
        ? Ecdsa256BitQuoteV4AuthData.fromJSON(object.signedData)
        : isSet(object.signed_data)
        ? Ecdsa256BitQuoteV4AuthData.fromJSON(object.signed_data)
        : undefined,
      extraBytes: isSet(object.extraBytes)
        ? bytesFromBase64(object.extraBytes)
        : isSet(object.extra_bytes)
        ? bytesFromBase64(object.extra_bytes)
        : new Uint8Array(0),
    };
  },

  toJSON(message: QuoteV4): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = Header.toJSON(message.header);
    }
    if (message.tdQuoteBody !== undefined) {
      obj.tdQuoteBody = TDQuoteBody.toJSON(message.tdQuoteBody);
    }
    if (message.signedDataSize !== 0) {
      obj.signedDataSize = Math.round(message.signedDataSize);
    }
    if (message.signedData !== undefined) {
      obj.signedData = Ecdsa256BitQuoteV4AuthData.toJSON(message.signedData);
    }
    if (message.extraBytes.length !== 0) {
      obj.extraBytes = base64FromBytes(message.extraBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<QuoteV4>): QuoteV4 {
    return QuoteV4.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuoteV4>): QuoteV4 {
    const message = createBaseQuoteV4();
    message.header = (object.header !== undefined && object.header !== null)
      ? Header.fromPartial(object.header)
      : undefined;
    message.tdQuoteBody = (object.tdQuoteBody !== undefined && object.tdQuoteBody !== null)
      ? TDQuoteBody.fromPartial(object.tdQuoteBody)
      : undefined;
    message.signedDataSize = object.signedDataSize ?? 0;
    message.signedData = (object.signedData !== undefined && object.signedData !== null)
      ? Ecdsa256BitQuoteV4AuthData.fromPartial(object.signedData)
      : undefined;
    message.extraBytes = object.extraBytes ?? new Uint8Array(0);
    return message;
  },
};

function createBaseHeader(): Header {
  return {
    version: 0,
    attestationKeyType: 0,
    teeType: 0,
    qeSvn: new Uint8Array(0),
    pceSvn: new Uint8Array(0),
    qeVendorId: new Uint8Array(0),
    userData: new Uint8Array(0),
  };
}

export const Header: MessageFns<Header> = {
  encode(message: Header, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.attestationKeyType !== 0) {
      writer.uint32(16).uint32(message.attestationKeyType);
    }
    if (message.teeType !== 0) {
      writer.uint32(24).uint32(message.teeType);
    }
    if (message.qeSvn.length !== 0) {
      writer.uint32(34).bytes(message.qeSvn);
    }
    if (message.pceSvn.length !== 0) {
      writer.uint32(42).bytes(message.pceSvn);
    }
    if (message.qeVendorId.length !== 0) {
      writer.uint32(50).bytes(message.qeVendorId);
    }
    if (message.userData.length !== 0) {
      writer.uint32(58).bytes(message.userData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Header {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.attestationKeyType = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.teeType = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.qeSvn = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pceSvn = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.qeVendorId = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.userData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Header {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      attestationKeyType: isSet(object.attestationKeyType)
        ? globalThis.Number(object.attestationKeyType)
        : isSet(object.attestation_key_type)
        ? globalThis.Number(object.attestation_key_type)
        : 0,
      teeType: isSet(object.teeType)
        ? globalThis.Number(object.teeType)
        : isSet(object.tee_type)
        ? globalThis.Number(object.tee_type)
        : 0,
      qeSvn: isSet(object.qeSvn)
        ? bytesFromBase64(object.qeSvn)
        : isSet(object.qe_svn)
        ? bytesFromBase64(object.qe_svn)
        : new Uint8Array(0),
      pceSvn: isSet(object.pceSvn)
        ? bytesFromBase64(object.pceSvn)
        : isSet(object.pce_svn)
        ? bytesFromBase64(object.pce_svn)
        : new Uint8Array(0),
      qeVendorId: isSet(object.qeVendorId)
        ? bytesFromBase64(object.qeVendorId)
        : isSet(object.qe_vendor_id)
        ? bytesFromBase64(object.qe_vendor_id)
        : new Uint8Array(0),
      userData: isSet(object.userData)
        ? bytesFromBase64(object.userData)
        : isSet(object.user_data)
        ? bytesFromBase64(object.user_data)
        : new Uint8Array(0),
    };
  },

  toJSON(message: Header): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.attestationKeyType !== 0) {
      obj.attestationKeyType = Math.round(message.attestationKeyType);
    }
    if (message.teeType !== 0) {
      obj.teeType = Math.round(message.teeType);
    }
    if (message.qeSvn.length !== 0) {
      obj.qeSvn = base64FromBytes(message.qeSvn);
    }
    if (message.pceSvn.length !== 0) {
      obj.pceSvn = base64FromBytes(message.pceSvn);
    }
    if (message.qeVendorId.length !== 0) {
      obj.qeVendorId = base64FromBytes(message.qeVendorId);
    }
    if (message.userData.length !== 0) {
      obj.userData = base64FromBytes(message.userData);
    }
    return obj;
  },

  create(base?: DeepPartial<Header>): Header {
    return Header.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Header>): Header {
    const message = createBaseHeader();
    message.version = object.version ?? 0;
    message.attestationKeyType = object.attestationKeyType ?? 0;
    message.teeType = object.teeType ?? 0;
    message.qeSvn = object.qeSvn ?? new Uint8Array(0);
    message.pceSvn = object.pceSvn ?? new Uint8Array(0);
    message.qeVendorId = object.qeVendorId ?? new Uint8Array(0);
    message.userData = object.userData ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTDQuoteBody(): TDQuoteBody {
  return {
    teeTcbSvn: new Uint8Array(0),
    mrSeam: new Uint8Array(0),
    mrSignerSeam: new Uint8Array(0),
    seamAttributes: new Uint8Array(0),
    tdAttributes: new Uint8Array(0),
    xfam: new Uint8Array(0),
    mrTd: new Uint8Array(0),
    mrConfigId: new Uint8Array(0),
    mrOwner: new Uint8Array(0),
    mrOwnerConfig: new Uint8Array(0),
    rtmrs: [],
    reportData: new Uint8Array(0),
  };
}

export const TDQuoteBody: MessageFns<TDQuoteBody> = {
  encode(message: TDQuoteBody, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.teeTcbSvn.length !== 0) {
      writer.uint32(10).bytes(message.teeTcbSvn);
    }
    if (message.mrSeam.length !== 0) {
      writer.uint32(18).bytes(message.mrSeam);
    }
    if (message.mrSignerSeam.length !== 0) {
      writer.uint32(26).bytes(message.mrSignerSeam);
    }
    if (message.seamAttributes.length !== 0) {
      writer.uint32(34).bytes(message.seamAttributes);
    }
    if (message.tdAttributes.length !== 0) {
      writer.uint32(42).bytes(message.tdAttributes);
    }
    if (message.xfam.length !== 0) {
      writer.uint32(50).bytes(message.xfam);
    }
    if (message.mrTd.length !== 0) {
      writer.uint32(58).bytes(message.mrTd);
    }
    if (message.mrConfigId.length !== 0) {
      writer.uint32(66).bytes(message.mrConfigId);
    }
    if (message.mrOwner.length !== 0) {
      writer.uint32(74).bytes(message.mrOwner);
    }
    if (message.mrOwnerConfig.length !== 0) {
      writer.uint32(82).bytes(message.mrOwnerConfig);
    }
    for (const v of message.rtmrs) {
      writer.uint32(90).bytes(v!);
    }
    if (message.reportData.length !== 0) {
      writer.uint32(98).bytes(message.reportData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TDQuoteBody {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTDQuoteBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.teeTcbSvn = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mrSeam = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mrSignerSeam = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.seamAttributes = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tdAttributes = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.xfam = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.mrTd = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.mrConfigId = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.mrOwner = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.mrOwnerConfig = reader.bytes();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.rtmrs.push(reader.bytes());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.reportData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TDQuoteBody {
    return {
      teeTcbSvn: isSet(object.teeTcbSvn)
        ? bytesFromBase64(object.teeTcbSvn)
        : isSet(object.tee_tcb_svn)
        ? bytesFromBase64(object.tee_tcb_svn)
        : new Uint8Array(0),
      mrSeam: isSet(object.mrSeam)
        ? bytesFromBase64(object.mrSeam)
        : isSet(object.mr_seam)
        ? bytesFromBase64(object.mr_seam)
        : new Uint8Array(0),
      mrSignerSeam: isSet(object.mrSignerSeam)
        ? bytesFromBase64(object.mrSignerSeam)
        : isSet(object.mr_signer_seam)
        ? bytesFromBase64(object.mr_signer_seam)
        : new Uint8Array(0),
      seamAttributes: isSet(object.seamAttributes)
        ? bytesFromBase64(object.seamAttributes)
        : isSet(object.seam_attributes)
        ? bytesFromBase64(object.seam_attributes)
        : new Uint8Array(0),
      tdAttributes: isSet(object.tdAttributes)
        ? bytesFromBase64(object.tdAttributes)
        : isSet(object.td_attributes)
        ? bytesFromBase64(object.td_attributes)
        : new Uint8Array(0),
      xfam: isSet(object.xfam) ? bytesFromBase64(object.xfam) : new Uint8Array(0),
      mrTd: isSet(object.mrTd)
        ? bytesFromBase64(object.mrTd)
        : isSet(object.mr_td)
        ? bytesFromBase64(object.mr_td)
        : new Uint8Array(0),
      mrConfigId: isSet(object.mrConfigId)
        ? bytesFromBase64(object.mrConfigId)
        : isSet(object.mr_config_id)
        ? bytesFromBase64(object.mr_config_id)
        : new Uint8Array(0),
      mrOwner: isSet(object.mrOwner)
        ? bytesFromBase64(object.mrOwner)
        : isSet(object.mr_owner)
        ? bytesFromBase64(object.mr_owner)
        : new Uint8Array(0),
      mrOwnerConfig: isSet(object.mrOwnerConfig)
        ? bytesFromBase64(object.mrOwnerConfig)
        : isSet(object.mr_owner_config)
        ? bytesFromBase64(object.mr_owner_config)
        : new Uint8Array(0),
      rtmrs: globalThis.Array.isArray(object?.rtmrs)
        ? object.rtmrs.map((e: any) => bytesFromBase64(e))
        : [],
      reportData: isSet(object.reportData)
        ? bytesFromBase64(object.reportData)
        : isSet(object.report_data)
        ? bytesFromBase64(object.report_data)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TDQuoteBody): unknown {
    const obj: any = {};
    if (message.teeTcbSvn.length !== 0) {
      obj.teeTcbSvn = base64FromBytes(message.teeTcbSvn);
    }
    if (message.mrSeam.length !== 0) {
      obj.mrSeam = base64FromBytes(message.mrSeam);
    }
    if (message.mrSignerSeam.length !== 0) {
      obj.mrSignerSeam = base64FromBytes(message.mrSignerSeam);
    }
    if (message.seamAttributes.length !== 0) {
      obj.seamAttributes = base64FromBytes(message.seamAttributes);
    }
    if (message.tdAttributes.length !== 0) {
      obj.tdAttributes = base64FromBytes(message.tdAttributes);
    }
    if (message.xfam.length !== 0) {
      obj.xfam = base64FromBytes(message.xfam);
    }
    if (message.mrTd.length !== 0) {
      obj.mrTd = base64FromBytes(message.mrTd);
    }
    if (message.mrConfigId.length !== 0) {
      obj.mrConfigId = base64FromBytes(message.mrConfigId);
    }
    if (message.mrOwner.length !== 0) {
      obj.mrOwner = base64FromBytes(message.mrOwner);
    }
    if (message.mrOwnerConfig.length !== 0) {
      obj.mrOwnerConfig = base64FromBytes(message.mrOwnerConfig);
    }
    if (message.rtmrs?.length) {
      obj.rtmrs = message.rtmrs.map((e) => base64FromBytes(e));
    }
    if (message.reportData.length !== 0) {
      obj.reportData = base64FromBytes(message.reportData);
    }
    return obj;
  },

  create(base?: DeepPartial<TDQuoteBody>): TDQuoteBody {
    return TDQuoteBody.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TDQuoteBody>): TDQuoteBody {
    const message = createBaseTDQuoteBody();
    message.teeTcbSvn = object.teeTcbSvn ?? new Uint8Array(0);
    message.mrSeam = object.mrSeam ?? new Uint8Array(0);
    message.mrSignerSeam = object.mrSignerSeam ?? new Uint8Array(0);
    message.seamAttributes = object.seamAttributes ?? new Uint8Array(0);
    message.tdAttributes = object.tdAttributes ?? new Uint8Array(0);
    message.xfam = object.xfam ?? new Uint8Array(0);
    message.mrTd = object.mrTd ?? new Uint8Array(0);
    message.mrConfigId = object.mrConfigId ?? new Uint8Array(0);
    message.mrOwner = object.mrOwner ?? new Uint8Array(0);
    message.mrOwnerConfig = object.mrOwnerConfig ?? new Uint8Array(0);
    message.rtmrs = object.rtmrs?.map((e) => e) || [];
    message.reportData = object.reportData ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEcdsa256BitQuoteV4AuthData(): Ecdsa256BitQuoteV4AuthData {
  return { signature: new Uint8Array(0), ecdsaAttestationKey: new Uint8Array(0), certificationData: undefined };
}

export const Ecdsa256BitQuoteV4AuthData: MessageFns<Ecdsa256BitQuoteV4AuthData> = {
  encode(message: Ecdsa256BitQuoteV4AuthData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signature.length !== 0) {
      writer.uint32(10).bytes(message.signature);
    }
    if (message.ecdsaAttestationKey.length !== 0) {
      writer.uint32(18).bytes(message.ecdsaAttestationKey);
    }
    if (message.certificationData !== undefined) {
      CertificationData.encode(message.certificationData, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ecdsa256BitQuoteV4AuthData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEcdsa256BitQuoteV4AuthData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ecdsaAttestationKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.certificationData = CertificationData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ecdsa256BitQuoteV4AuthData {
    return {
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      ecdsaAttestationKey: isSet(object.ecdsaAttestationKey)
        ? bytesFromBase64(object.ecdsaAttestationKey)
        : isSet(object.ecdsa_attestation_key)
        ? bytesFromBase64(object.ecdsa_attestation_key)
        : new Uint8Array(0),
      certificationData: isSet(object.certificationData)
        ? CertificationData.fromJSON(object.certificationData)
        : isSet(object.certification_data)
        ? CertificationData.fromJSON(object.certification_data)
        : undefined,
    };
  },

  toJSON(message: Ecdsa256BitQuoteV4AuthData): unknown {
    const obj: any = {};
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.ecdsaAttestationKey.length !== 0) {
      obj.ecdsaAttestationKey = base64FromBytes(message.ecdsaAttestationKey);
    }
    if (message.certificationData !== undefined) {
      obj.certificationData = CertificationData.toJSON(message.certificationData);
    }
    return obj;
  },

  create(base?: DeepPartial<Ecdsa256BitQuoteV4AuthData>): Ecdsa256BitQuoteV4AuthData {
    return Ecdsa256BitQuoteV4AuthData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Ecdsa256BitQuoteV4AuthData>): Ecdsa256BitQuoteV4AuthData {
    const message = createBaseEcdsa256BitQuoteV4AuthData();
    message.signature = object.signature ?? new Uint8Array(0);
    message.ecdsaAttestationKey = object.ecdsaAttestationKey ?? new Uint8Array(0);
    message.certificationData = (object.certificationData !== undefined && object.certificationData !== null)
      ? CertificationData.fromPartial(object.certificationData)
      : undefined;
    return message;
  },
};

function createBaseCertificationData(): CertificationData {
  return { certificateDataType: 0, size: 0, qeReportCertificationData: undefined };
}

export const CertificationData: MessageFns<CertificationData> = {
  encode(message: CertificationData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.certificateDataType !== 0) {
      writer.uint32(8).uint32(message.certificateDataType);
    }
    if (message.size !== 0) {
      writer.uint32(16).uint32(message.size);
    }
    if (message.qeReportCertificationData !== undefined) {
      QEReportCertificationData.encode(message.qeReportCertificationData, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CertificationData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCertificationData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.certificateDataType = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.qeReportCertificationData = QEReportCertificationData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CertificationData {
    return {
      certificateDataType: isSet(object.certificateDataType)
        ? globalThis.Number(object.certificateDataType)
        : isSet(object.certificate_data_type)
        ? globalThis.Number(object.certificate_data_type)
        : 0,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      qeReportCertificationData: isSet(object.qeReportCertificationData)
        ? QEReportCertificationData.fromJSON(object.qeReportCertificationData)
        : isSet(object.qe_report_certification_data)
        ? QEReportCertificationData.fromJSON(object.qe_report_certification_data)
        : undefined,
    };
  },

  toJSON(message: CertificationData): unknown {
    const obj: any = {};
    if (message.certificateDataType !== 0) {
      obj.certificateDataType = Math.round(message.certificateDataType);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.qeReportCertificationData !== undefined) {
      obj.qeReportCertificationData = QEReportCertificationData.toJSON(message.qeReportCertificationData);
    }
    return obj;
  },

  create(base?: DeepPartial<CertificationData>): CertificationData {
    return CertificationData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CertificationData>): CertificationData {
    const message = createBaseCertificationData();
    message.certificateDataType = object.certificateDataType ?? 0;
    message.size = object.size ?? 0;
    message.qeReportCertificationData =
      (object.qeReportCertificationData !== undefined && object.qeReportCertificationData !== null)
        ? QEReportCertificationData.fromPartial(object.qeReportCertificationData)
        : undefined;
    return message;
  },
};

function createBaseQEReportCertificationData(): QEReportCertificationData {
  return {
    qeReport: undefined,
    qeReportSignature: new Uint8Array(0),
    qeAuthData: undefined,
    pckCertificateChainData: undefined,
  };
}

export const QEReportCertificationData: MessageFns<QEReportCertificationData> = {
  encode(message: QEReportCertificationData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.qeReport !== undefined) {
      EnclaveReport.encode(message.qeReport, writer.uint32(10).fork()).join();
    }
    if (message.qeReportSignature.length !== 0) {
      writer.uint32(18).bytes(message.qeReportSignature);
    }
    if (message.qeAuthData !== undefined) {
      QeAuthData.encode(message.qeAuthData, writer.uint32(26).fork()).join();
    }
    if (message.pckCertificateChainData !== undefined) {
      PCKCertificateChainData.encode(message.pckCertificateChainData, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QEReportCertificationData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQEReportCertificationData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.qeReport = EnclaveReport.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.qeReportSignature = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.qeAuthData = QeAuthData.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pckCertificateChainData = PCKCertificateChainData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QEReportCertificationData {
    return {
      qeReport: isSet(object.qeReport)
        ? EnclaveReport.fromJSON(object.qeReport)
        : isSet(object.qe_report)
        ? EnclaveReport.fromJSON(object.qe_report)
        : undefined,
      qeReportSignature: isSet(object.qeReportSignature)
        ? bytesFromBase64(object.qeReportSignature)
        : isSet(object.qe_report_signature)
        ? bytesFromBase64(object.qe_report_signature)
        : new Uint8Array(0),
      qeAuthData: isSet(object.qeAuthData)
        ? QeAuthData.fromJSON(object.qeAuthData)
        : isSet(object.qe_auth_data)
        ? QeAuthData.fromJSON(object.qe_auth_data)
        : undefined,
      pckCertificateChainData: isSet(object.pckCertificateChainData)
        ? PCKCertificateChainData.fromJSON(object.pckCertificateChainData)
        : isSet(object.pck_certificate_chain_data)
        ? PCKCertificateChainData.fromJSON(object.pck_certificate_chain_data)
        : undefined,
    };
  },

  toJSON(message: QEReportCertificationData): unknown {
    const obj: any = {};
    if (message.qeReport !== undefined) {
      obj.qeReport = EnclaveReport.toJSON(message.qeReport);
    }
    if (message.qeReportSignature.length !== 0) {
      obj.qeReportSignature = base64FromBytes(message.qeReportSignature);
    }
    if (message.qeAuthData !== undefined) {
      obj.qeAuthData = QeAuthData.toJSON(message.qeAuthData);
    }
    if (message.pckCertificateChainData !== undefined) {
      obj.pckCertificateChainData = PCKCertificateChainData.toJSON(message.pckCertificateChainData);
    }
    return obj;
  },

  create(base?: DeepPartial<QEReportCertificationData>): QEReportCertificationData {
    return QEReportCertificationData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QEReportCertificationData>): QEReportCertificationData {
    const message = createBaseQEReportCertificationData();
    message.qeReport = (object.qeReport !== undefined && object.qeReport !== null)
      ? EnclaveReport.fromPartial(object.qeReport)
      : undefined;
    message.qeReportSignature = object.qeReportSignature ?? new Uint8Array(0);
    message.qeAuthData = (object.qeAuthData !== undefined && object.qeAuthData !== null)
      ? QeAuthData.fromPartial(object.qeAuthData)
      : undefined;
    message.pckCertificateChainData =
      (object.pckCertificateChainData !== undefined && object.pckCertificateChainData !== null)
        ? PCKCertificateChainData.fromPartial(object.pckCertificateChainData)
        : undefined;
    return message;
  },
};

function createBasePCKCertificateChainData(): PCKCertificateChainData {
  return { certificateDataType: 0, size: 0, pckCertChain: new Uint8Array(0) };
}

export const PCKCertificateChainData: MessageFns<PCKCertificateChainData> = {
  encode(message: PCKCertificateChainData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.certificateDataType !== 0) {
      writer.uint32(8).uint32(message.certificateDataType);
    }
    if (message.size !== 0) {
      writer.uint32(16).uint32(message.size);
    }
    if (message.pckCertChain.length !== 0) {
      writer.uint32(26).bytes(message.pckCertChain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PCKCertificateChainData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePCKCertificateChainData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.certificateDataType = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.size = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pckCertChain = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PCKCertificateChainData {
    return {
      certificateDataType: isSet(object.certificateDataType)
        ? globalThis.Number(object.certificateDataType)
        : isSet(object.certificate_data_type)
        ? globalThis.Number(object.certificate_data_type)
        : 0,
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      pckCertChain: isSet(object.pckCertChain)
        ? bytesFromBase64(object.pckCertChain)
        : isSet(object.pck_cert_chain)
        ? bytesFromBase64(object.pck_cert_chain)
        : new Uint8Array(0),
    };
  },

  toJSON(message: PCKCertificateChainData): unknown {
    const obj: any = {};
    if (message.certificateDataType !== 0) {
      obj.certificateDataType = Math.round(message.certificateDataType);
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.pckCertChain.length !== 0) {
      obj.pckCertChain = base64FromBytes(message.pckCertChain);
    }
    return obj;
  },

  create(base?: DeepPartial<PCKCertificateChainData>): PCKCertificateChainData {
    return PCKCertificateChainData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PCKCertificateChainData>): PCKCertificateChainData {
    const message = createBasePCKCertificateChainData();
    message.certificateDataType = object.certificateDataType ?? 0;
    message.size = object.size ?? 0;
    message.pckCertChain = object.pckCertChain ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQeAuthData(): QeAuthData {
  return { parsedDataSize: 0, data: new Uint8Array(0) };
}

export const QeAuthData: MessageFns<QeAuthData> = {
  encode(message: QeAuthData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.parsedDataSize !== 0) {
      writer.uint32(8).uint32(message.parsedDataSize);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QeAuthData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQeAuthData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.parsedDataSize = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QeAuthData {
    return {
      parsedDataSize: isSet(object.parsedDataSize)
        ? globalThis.Number(object.parsedDataSize)
        : isSet(object.parsed_data_size)
        ? globalThis.Number(object.parsed_data_size)
        : 0,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
    };
  },

  toJSON(message: QeAuthData): unknown {
    const obj: any = {};
    if (message.parsedDataSize !== 0) {
      obj.parsedDataSize = Math.round(message.parsedDataSize);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<QeAuthData>): QeAuthData {
    return QeAuthData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QeAuthData>): QeAuthData {
    const message = createBaseQeAuthData();
    message.parsedDataSize = object.parsedDataSize ?? 0;
    message.data = object.data ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEnclaveReport(): EnclaveReport {
  return {
    cpuSvn: new Uint8Array(0),
    miscSelect: 0,
    reserved1: new Uint8Array(0),
    attributes: new Uint8Array(0),
    mrEnclave: new Uint8Array(0),
    reserved2: new Uint8Array(0),
    mrSigner: new Uint8Array(0),
    reserved3: new Uint8Array(0),
    isvProdId: 0,
    isvSvn: 0,
    reserved4: new Uint8Array(0),
    reportData: new Uint8Array(0),
  };
}

export const EnclaveReport: MessageFns<EnclaveReport> = {
  encode(message: EnclaveReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cpuSvn.length !== 0) {
      writer.uint32(10).bytes(message.cpuSvn);
    }
    if (message.miscSelect !== 0) {
      writer.uint32(16).uint32(message.miscSelect);
    }
    if (message.reserved1.length !== 0) {
      writer.uint32(26).bytes(message.reserved1);
    }
    if (message.attributes.length !== 0) {
      writer.uint32(34).bytes(message.attributes);
    }
    if (message.mrEnclave.length !== 0) {
      writer.uint32(42).bytes(message.mrEnclave);
    }
    if (message.reserved2.length !== 0) {
      writer.uint32(50).bytes(message.reserved2);
    }
    if (message.mrSigner.length !== 0) {
      writer.uint32(58).bytes(message.mrSigner);
    }
    if (message.reserved3.length !== 0) {
      writer.uint32(66).bytes(message.reserved3);
    }
    if (message.isvProdId !== 0) {
      writer.uint32(72).uint32(message.isvProdId);
    }
    if (message.isvSvn !== 0) {
      writer.uint32(80).uint32(message.isvSvn);
    }
    if (message.reserved4.length !== 0) {
      writer.uint32(90).bytes(message.reserved4);
    }
    if (message.reportData.length !== 0) {
      writer.uint32(98).bytes(message.reportData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnclaveReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnclaveReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cpuSvn = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.miscSelect = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reserved1 = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.attributes = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mrEnclave = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.reserved2 = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.mrSigner = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.reserved3 = reader.bytes();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.isvProdId = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.isvSvn = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.reserved4 = reader.bytes();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.reportData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnclaveReport {
    return {
      cpuSvn: isSet(object.cpuSvn)
        ? bytesFromBase64(object.cpuSvn)
        : isSet(object.cpu_svn)
        ? bytesFromBase64(object.cpu_svn)
        : new Uint8Array(0),
      miscSelect: isSet(object.miscSelect)
        ? globalThis.Number(object.miscSelect)
        : isSet(object.misc_select)
        ? globalThis.Number(object.misc_select)
        : 0,
      reserved1: isSet(object.reserved1) ? bytesFromBase64(object.reserved1) : new Uint8Array(0),
      attributes: isSet(object.attributes) ? bytesFromBase64(object.attributes) : new Uint8Array(0),
      mrEnclave: isSet(object.mrEnclave)
        ? bytesFromBase64(object.mrEnclave)
        : isSet(object.mr_enclave)
        ? bytesFromBase64(object.mr_enclave)
        : new Uint8Array(0),
      reserved2: isSet(object.reserved2) ? bytesFromBase64(object.reserved2) : new Uint8Array(0),
      mrSigner: isSet(object.mrSigner)
        ? bytesFromBase64(object.mrSigner)
        : isSet(object.mr_signer)
        ? bytesFromBase64(object.mr_signer)
        : new Uint8Array(0),
      reserved3: isSet(object.reserved3) ? bytesFromBase64(object.reserved3) : new Uint8Array(0),
      isvProdId: isSet(object.isvProdId)
        ? globalThis.Number(object.isvProdId)
        : isSet(object.isv_prod_id)
        ? globalThis.Number(object.isv_prod_id)
        : 0,
      isvSvn: isSet(object.isvSvn)
        ? globalThis.Number(object.isvSvn)
        : isSet(object.isv_svn)
        ? globalThis.Number(object.isv_svn)
        : 0,
      reserved4: isSet(object.reserved4) ? bytesFromBase64(object.reserved4) : new Uint8Array(0),
      reportData: isSet(object.reportData)
        ? bytesFromBase64(object.reportData)
        : isSet(object.report_data)
        ? bytesFromBase64(object.report_data)
        : new Uint8Array(0),
    };
  },

  toJSON(message: EnclaveReport): unknown {
    const obj: any = {};
    if (message.cpuSvn.length !== 0) {
      obj.cpuSvn = base64FromBytes(message.cpuSvn);
    }
    if (message.miscSelect !== 0) {
      obj.miscSelect = Math.round(message.miscSelect);
    }
    if (message.reserved1.length !== 0) {
      obj.reserved1 = base64FromBytes(message.reserved1);
    }
    if (message.attributes.length !== 0) {
      obj.attributes = base64FromBytes(message.attributes);
    }
    if (message.mrEnclave.length !== 0) {
      obj.mrEnclave = base64FromBytes(message.mrEnclave);
    }
    if (message.reserved2.length !== 0) {
      obj.reserved2 = base64FromBytes(message.reserved2);
    }
    if (message.mrSigner.length !== 0) {
      obj.mrSigner = base64FromBytes(message.mrSigner);
    }
    if (message.reserved3.length !== 0) {
      obj.reserved3 = base64FromBytes(message.reserved3);
    }
    if (message.isvProdId !== 0) {
      obj.isvProdId = Math.round(message.isvProdId);
    }
    if (message.isvSvn !== 0) {
      obj.isvSvn = Math.round(message.isvSvn);
    }
    if (message.reserved4.length !== 0) {
      obj.reserved4 = base64FromBytes(message.reserved4);
    }
    if (message.reportData.length !== 0) {
      obj.reportData = base64FromBytes(message.reportData);
    }
    return obj;
  },

  create(base?: DeepPartial<EnclaveReport>): EnclaveReport {
    return EnclaveReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EnclaveReport>): EnclaveReport {
    const message = createBaseEnclaveReport();
    message.cpuSvn = object.cpuSvn ?? new Uint8Array(0);
    message.miscSelect = object.miscSelect ?? 0;
    message.reserved1 = object.reserved1 ?? new Uint8Array(0);
    message.attributes = object.attributes ?? new Uint8Array(0);
    message.mrEnclave = object.mrEnclave ?? new Uint8Array(0);
    message.reserved2 = object.reserved2 ?? new Uint8Array(0);
    message.mrSigner = object.mrSigner ?? new Uint8Array(0);
    message.reserved3 = object.reserved3 ?? new Uint8Array(0);
    message.isvProdId = object.isvProdId ?? 0;
    message.isvSvn = object.isvSvn ?? 0;
    message.reserved4 = object.reserved4 ?? new Uint8Array(0);
    message.reportData = object.reportData ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
