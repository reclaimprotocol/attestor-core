// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.31.1
// source: attest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { NvidiaAttestationReport } from "./attestation.ts";
import { Attestation as Attestation1 } from "./sevsnp.ts";
import { QuoteV4 } from "./tdx.ts";
import { HashAlgo, hashAlgoFromJSON, hashAlgoToJSON, Quote } from "./tpm.ts";

export const protobufPackage = "attest";

/** Type of hardware technology used to protect this instance */
export const GCEConfidentialTechnology = {
  NONE: 0,
  AMD_SEV: 1,
  AMD_SEV_ES: 2,
  INTEL_TDX: 3,
  AMD_SEV_SNP: 4,
  UNRECOGNIZED: -1,
} as const;

export type GCEConfidentialTechnology = typeof GCEConfidentialTechnology[keyof typeof GCEConfidentialTechnology];

export namespace GCEConfidentialTechnology {
  export type NONE = typeof GCEConfidentialTechnology.NONE;
  export type AMD_SEV = typeof GCEConfidentialTechnology.AMD_SEV;
  export type AMD_SEV_ES = typeof GCEConfidentialTechnology.AMD_SEV_ES;
  export type INTEL_TDX = typeof GCEConfidentialTechnology.INTEL_TDX;
  export type AMD_SEV_SNP = typeof GCEConfidentialTechnology.AMD_SEV_SNP;
  export type UNRECOGNIZED = typeof GCEConfidentialTechnology.UNRECOGNIZED;
}

export function gCEConfidentialTechnologyFromJSON(object: any): GCEConfidentialTechnology {
  switch (object) {
    case 0:
    case "NONE":
      return GCEConfidentialTechnology.NONE;
    case 1:
    case "AMD_SEV":
      return GCEConfidentialTechnology.AMD_SEV;
    case 2:
    case "AMD_SEV_ES":
      return GCEConfidentialTechnology.AMD_SEV_ES;
    case 3:
    case "INTEL_TDX":
      return GCEConfidentialTechnology.INTEL_TDX;
    case 4:
    case "AMD_SEV_SNP":
      return GCEConfidentialTechnology.AMD_SEV_SNP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GCEConfidentialTechnology.UNRECOGNIZED;
  }
}

export function gCEConfidentialTechnologyToJSON(object: GCEConfidentialTechnology): string {
  switch (object) {
    case GCEConfidentialTechnology.NONE:
      return "NONE";
    case GCEConfidentialTechnology.AMD_SEV:
      return "AMD_SEV";
    case GCEConfidentialTechnology.AMD_SEV_ES:
      return "AMD_SEV_ES";
    case GCEConfidentialTechnology.INTEL_TDX:
      return "INTEL_TDX";
    case GCEConfidentialTechnology.AMD_SEV_SNP:
      return "AMD_SEV_SNP";
    case GCEConfidentialTechnology.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Common, publicly-listed certificates by different vendors. */
export const WellKnownCertificate = {
  UNKNOWN: 0,
  /**
   * MS_WINDOWS_PROD_PCA_2011 - Microsoft certs:
   * https://go.microsoft.com/fwlink/p/?linkid=321192
   */
  MS_WINDOWS_PROD_PCA_2011: 1,
  /** MS_THIRD_PARTY_UEFI_CA_2011 - https://go.microsoft.com/fwlink/p/?linkid=321194 */
  MS_THIRD_PARTY_UEFI_CA_2011: 2,
  /** MS_THIRD_PARTY_KEK_CA_2011 - https://go.microsoft.com/fwlink/p/?linkid=321185 */
  MS_THIRD_PARTY_KEK_CA_2011: 3,
  /** GCE_DEFAULT_PK - GCE certs: */
  GCE_DEFAULT_PK: 4,
  UNRECOGNIZED: -1,
} as const;

export type WellKnownCertificate = typeof WellKnownCertificate[keyof typeof WellKnownCertificate];

export namespace WellKnownCertificate {
  export type UNKNOWN = typeof WellKnownCertificate.UNKNOWN;
  export type MS_WINDOWS_PROD_PCA_2011 = typeof WellKnownCertificate.MS_WINDOWS_PROD_PCA_2011;
  export type MS_THIRD_PARTY_UEFI_CA_2011 = typeof WellKnownCertificate.MS_THIRD_PARTY_UEFI_CA_2011;
  export type MS_THIRD_PARTY_KEK_CA_2011 = typeof WellKnownCertificate.MS_THIRD_PARTY_KEK_CA_2011;
  export type GCE_DEFAULT_PK = typeof WellKnownCertificate.GCE_DEFAULT_PK;
  export type UNRECOGNIZED = typeof WellKnownCertificate.UNRECOGNIZED;
}

export function wellKnownCertificateFromJSON(object: any): WellKnownCertificate {
  switch (object) {
    case 0:
    case "UNKNOWN":
      return WellKnownCertificate.UNKNOWN;
    case 1:
    case "MS_WINDOWS_PROD_PCA_2011":
      return WellKnownCertificate.MS_WINDOWS_PROD_PCA_2011;
    case 2:
    case "MS_THIRD_PARTY_UEFI_CA_2011":
      return WellKnownCertificate.MS_THIRD_PARTY_UEFI_CA_2011;
    case 3:
    case "MS_THIRD_PARTY_KEK_CA_2011":
      return WellKnownCertificate.MS_THIRD_PARTY_KEK_CA_2011;
    case 4:
    case "GCE_DEFAULT_PK":
      return WellKnownCertificate.GCE_DEFAULT_PK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WellKnownCertificate.UNRECOGNIZED;
  }
}

export function wellKnownCertificateToJSON(object: WellKnownCertificate): string {
  switch (object) {
    case WellKnownCertificate.UNKNOWN:
      return "UNKNOWN";
    case WellKnownCertificate.MS_WINDOWS_PROD_PCA_2011:
      return "MS_WINDOWS_PROD_PCA_2011";
    case WellKnownCertificate.MS_THIRD_PARTY_UEFI_CA_2011:
      return "MS_THIRD_PARTY_UEFI_CA_2011";
    case WellKnownCertificate.MS_THIRD_PARTY_KEK_CA_2011:
      return "MS_THIRD_PARTY_KEK_CA_2011";
    case WellKnownCertificate.GCE_DEFAULT_PK:
      return "GCE_DEFAULT_PK";
    case WellKnownCertificate.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * The container's restart policy.
 * See the following Kubernetes documentation for more details:
 * https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/#restart-policy
 * Note that these enum variants do not conform to the standard Protocol Buffers
 * Style Guide so that RestartPolicy_name, RestartPolicy_value, and
 * RestartPolicy.String() match the values used by Kubernetes and others.
 */
export const RestartPolicy = { Always: 0, OnFailure: 1, Never: 2, UNRECOGNIZED: -1 } as const;

export type RestartPolicy = typeof RestartPolicy[keyof typeof RestartPolicy];

export namespace RestartPolicy {
  export type Always = typeof RestartPolicy.Always;
  export type OnFailure = typeof RestartPolicy.OnFailure;
  export type Never = typeof RestartPolicy.Never;
  export type UNRECOGNIZED = typeof RestartPolicy.UNRECOGNIZED;
}

export function restartPolicyFromJSON(object: any): RestartPolicy {
  switch (object) {
    case 0:
    case "Always":
      return RestartPolicy.Always;
    case 1:
    case "OnFailure":
      return RestartPolicy.OnFailure;
    case 2:
    case "Never":
      return RestartPolicy.Never;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RestartPolicy.UNRECOGNIZED;
  }
}

export function restartPolicyToJSON(object: RestartPolicy): string {
  switch (object) {
    case RestartPolicy.Always:
      return "Always";
    case RestartPolicy.OnFailure:
      return "OnFailure";
    case RestartPolicy.Never:
      return "Never";
    case RestartPolicy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Confidential Computing mode for GPU device. Reference for these CC mode values: https://developer.nvidia.com/blog/confidential-computing-on-h100-gpus-for-secure-and-trustworthy-ai/#hardware_security_for_nvidia_h100_gpus */
export const GPUDeviceCCMode = {
  UNSET: 0,
  /** ON - The NVIDIA H100 hardware, firmware, and software have fully activated all the confidential computing features. */
  ON: 1,
  /** OFF - None of the confidential computing-specific features are active. */
  OFF: 2,
  /** DEVTOOLS - The GPU is in a partial CC mode that will match the workflows of CC mode ON, but with security protections disabled. */
  DEVTOOLS: 3,
  UNRECOGNIZED: -1,
} as const;

export type GPUDeviceCCMode = typeof GPUDeviceCCMode[keyof typeof GPUDeviceCCMode];

export namespace GPUDeviceCCMode {
  export type UNSET = typeof GPUDeviceCCMode.UNSET;
  export type ON = typeof GPUDeviceCCMode.ON;
  export type OFF = typeof GPUDeviceCCMode.OFF;
  export type DEVTOOLS = typeof GPUDeviceCCMode.DEVTOOLS;
  export type UNRECOGNIZED = typeof GPUDeviceCCMode.UNRECOGNIZED;
}

export function gPUDeviceCCModeFromJSON(object: any): GPUDeviceCCMode {
  switch (object) {
    case 0:
    case "UNSET":
      return GPUDeviceCCMode.UNSET;
    case 1:
    case "ON":
      return GPUDeviceCCMode.ON;
    case 2:
    case "OFF":
      return GPUDeviceCCMode.OFF;
    case 3:
    case "DEVTOOLS":
      return GPUDeviceCCMode.DEVTOOLS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GPUDeviceCCMode.UNRECOGNIZED;
  }
}

export function gPUDeviceCCModeToJSON(object: GPUDeviceCCMode): string {
  switch (object) {
    case GPUDeviceCCMode.UNSET:
      return "UNSET";
    case GPUDeviceCCMode.ON:
      return "ON";
    case GPUDeviceCCMode.OFF:
      return "OFF";
    case GPUDeviceCCMode.DEVTOOLS:
      return "DEVTOOLS";
    case GPUDeviceCCMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Information uniquely identifying a GCE instance. Can be used to create an
 * instance URL, which can then be used with GCE APIs. Formatted like:
 *   https://www.googleapis.com/compute/v1/projects/{project_id}/zones/{zone}/instances/{instance_name}
 */
export interface GCEInstanceInfo {
  zone: string;
  projectId: string;
  projectNumber: bigint;
  instanceName: string;
  instanceId: bigint;
}

export interface Attestation {
  /** Attestation Key (AK) Public Area, encoded as a TPMT_PUBLIC */
  akPub: Uint8Array;
  /** Quotes over all supported PCR banks */
  quotes: Quote[];
  /**
   * TCG Event Log, encoded in the raw binary format.
   * Can be SHA-1 or crypto-agile.
   */
  eventLog: Uint8Array;
  /** Optional information about a GCE instance, unused outside of GCE */
  instanceInfo:
    | GCEInstanceInfo
    | undefined;
  /** A COS event log using the TCG Canonical Event Log format */
  canonicalEventLog: Uint8Array;
  /**
   * Attestation Key (AK) Certificate, encoded as ASN.1 DER.
   * Optional.
   */
  akCert: Uint8Array;
  /**
   * Intermediate Certificates for verifying the AK Certificate, encoded as
   * ASN.1 DER. Optional.
   */
  intermediateCerts: Uint8Array[];
  sevSnpAttestation?: Attestation1 | undefined;
  tdxAttestation?: QuoteV4 | undefined;
}

/**
 * For VMs running SEV-SNP with an SVSM-based e-vTPM, this contains a TPM quote
 * and all the information needed to verify it.
 */
export interface SevSnpSvsmAttestation {
  attestation: Attestation | undefined;
  sevSnpAttestation:
    | Attestation1
    | undefined;
  /**
   * vTPM service manifest is retrieved from configfs-tsm-report via manifest blob
   * See https://github.com/torvalds/linux/blob/v6.16/Documentation/ABI/testing/configfs-tsm-report
   * The manifest format is defined by the SVSM spec at https://www.amd.com/en/developer/sev.html
   */
  vtpmServiceManifest: Uint8Array;
  vtpmServiceManifestVersion: string;
  /** This contains a launch endorsement for guest firmware that includes SVSM. */
  launchEndorsement: Uint8Array;
}

/** The platform/firmware state for this instance */
export interface PlatformState {
  /** Raw S-CRTM version identifier (EV_S_CRTM_VERSION) */
  scrtmVersionId?:
    | Uint8Array
    | undefined;
  /** Virtual GCE firmware version (parsed from S-CRTM version id) */
  gceVersion?:
    | number
    | undefined;
  /** Set to NONE on non-GCE instances or non-Confidential Shielded GCE instances */
  technology: GCEConfidentialTechnology;
  /** Only set for GCE instances */
  instanceInfo: GCEInstanceInfo | undefined;
}

export interface GrubFile {
  /** The digest of the file (pulled from the raw event digest). */
  digest: Uint8Array;
  /** The event data. This is not measured, so it is untrusted. */
  untrustedFilename: Uint8Array;
}

export interface GrubState {
  /** All GRUB-read and measured files, including grub.cfg. */
  files: GrubFile[];
  /**
   * A list of executed GRUB commands and command lines passed to the kernel
   * and kernel modules.
   */
  commands: string[];
}

/**
 * The state of the Linux kernel.
 * At the moment, parsing LinuxKernelState relies on parsing the GrubState.
 * To do so, use ParseOpts{Loader: GRUB} when calling ParseMachineState.
 */
export interface LinuxKernelState {
  /** The kernel command line. */
  commandLine: string;
}

/** A parsed event from the TCG event log */
export interface Event {
  /** The Platform Control Register (PCR) this event was extended into. */
  pcrIndex: number;
  /**
   * The type of this event. Note that this value is not verified, so it should
   * only be used as a hint during event parsing.
   */
  untrustedType: number;
  /**
   * The raw data associated to this event. The meaning of this data is
   * specific to the type of the event.
   */
  data: Uint8Array;
  /**
   * The event digest actually extended into the TPM. This is often the hash of
   * the data field, but in some cases it may have a type-specific calculation.
   */
  digest: Uint8Array;
  /** This is true if hash(data) == digest. */
  digestVerified: boolean;
}

export interface Certificate {
  /** DER representation of the certificate. */
  der?: Uint8Array | undefined;
  wellKnown?: WellKnownCertificate | undefined;
}

/**
 * A Secure Boot database containing lists of hashes and certificates,
 * as defined by section 32.4.1 Signature Database in the UEFI spec.
 */
export interface Database {
  certs: Certificate[];
  hashes: Uint8Array[];
}

/** The Secure Boot state for this instance. */
export interface SecureBootState {
  /** Whether Secure Boot is enabled. */
  enabled: boolean;
  /** The Secure Boot signature (allowed) database. */
  db:
    | Database
    | undefined;
  /** The Secure Boot revoked signature (forbidden) database. */
  dbx:
    | Database
    | undefined;
  /**
   * Authority events post-separator. Pre-separator authorities
   * are currently not supported.
   */
  authority:
    | Database
    | undefined;
  /** The Secure Boot Platform key, used to sign key exchange keys. */
  pk:
    | Database
    | undefined;
  /** The Secure Boot Key Exchange Keys, used to sign db and dbx updates. */
  kek: Database | undefined;
}

export interface ContainerState {
  imageReference: string;
  /**
   * Digest of the registry's image manifest, which contains a list of the
   * layers comprising the image.
   */
  imageDigest: string;
  restartPolicy: RestartPolicy;
  /**
   * Digest of the local image configuration object, containing config items
   * such as local layer digests.
   */
  imageId: string;
  args: string[];
  envVars: { [key: string]: string };
  /**
   * Record operator input Env Vars and Args, should be subsets of the above
   * Env Vars and Args.
   */
  overriddenArgs: string[];
  overriddenEnvVars: { [key: string]: string };
}

export interface ContainerState_EnvVarsEntry {
  key: string;
  value: string;
}

export interface ContainerState_OverriddenEnvVarsEntry {
  key: string;
  value: string;
}

export interface SemanticVersion {
  major: number;
  minor: number;
  patch: number;
}

export interface HealthMonitoringState {
  /** Whether memory monitoring is enabled. */
  memoryEnabled?: boolean | undefined;
}

export interface GpuDeviceState {
  /** Whether Confidential Computing mode is enabled for GPU. */
  ccMode: GPUDeviceCCMode;
  nvidiaAttestationReport: NvidiaAttestationReport | undefined;
}

export interface AttestedCosState {
  container: ContainerState | undefined;
  cosVersion: SemanticVersion | undefined;
  launcherVersion: SemanticVersion | undefined;
  healthMonitoring: HealthMonitoringState | undefined;
  gpuDeviceState: GpuDeviceState | undefined;
}

export interface EfiApp {
  /** The PE/COFF digest of the EFI application (pulled from the raw event digest). */
  digest: Uint8Array;
}

/**
 * The verified state of EFI Applications. Policy usage on this machine state
 * should check the entire set of EFI App digests matches, not a subset.
 */
export interface EfiState {
  /**
   * UEFI's OS Loader code is required to measure attempts to load and execute
   * UEFI applications.
   * UEFI applications are typically bootloaders such as shim and GRUB.
   * These run and are measured using the UEFI LoadImage() service.
   */
  apps: EfiApp[];
  /**
   * The EFI drivers,
   * obtained from https://trustedcomputinggroup.org/wp-content/uploads/TCG_EFI_Platform_1_22_Final_-v15.pdf#page=22.
   * The EFI Boot Services Drivers from adapter or loaded bydriver in adapter.
   */
  bootServicesDrivers: EfiApp[];
  /** The EFI Runtime Drivers from adapter or loaded bydriver in adapter. */
  runtimeServicesDrivers: EfiApp[];
}

/** The verified state of a booted machine, obtained from an Attestation */
export interface MachineState {
  platform: PlatformState | undefined;
  secureBoot:
    | SecureBootState
    | undefined;
  /**
   * The complete parsed TCG Event Log, including those events used to
   * create the PlatformState.
   */
  rawEvents: Event[];
  /**
   * The hash algorithm used when verifying the Attestation. This indicates:
   *   - which PCR bank was used for for quote validation and event log replay
   *   - the hash algorithm used to calculate event digests
   */
  hash: HashAlgo;
  grub: GrubState | undefined;
  linuxKernel: LinuxKernelState | undefined;
  cos: AttestedCosState | undefined;
  efi: EfiState | undefined;
  sevSnpAttestation?: Attestation1 | undefined;
  tdxAttestation?: QuoteV4 | undefined;
}

/** A policy dictating which values of PlatformState to allow */
export interface PlatformPolicy {
  /**
   * If PlatformState.firmware contains a scrtm_version_id, it must appear
   * in this list. For use with a GCE VM, minimum_gce_firmware_version is
   * often a better alternative.
   */
  allowedScrtmVersionIds: Uint8Array[];
  /**
   * If PlatformState.firmware contains a minimum_gce_firmware_version, it must
   * be greater than or equal to this value. Currently, the max version is 1.
   */
  minimumGceFirmwareVersion: number;
  /**
   * The PlatformState's technology must be at least as secure as
   * the specified minimum_technology (i.e. AMD_SEV_ES > AMD_SEV > NONE).
   */
  minimumTechnology: GCEConfidentialTechnology;
}

/**
 * A policy about what parts of a RIM to compare against machine state as
 * reflected in a quote or (verified) event log. Reference measurements for
 * a component are expected to be addressable by the machine state's reported
 * digest, or otherwise presented as cached collateral with the attestation
 * itself. The method of delivery is vendor-specific.
 */
export interface RIMPolicy {
  /**
   * If true, the signed measurement must be available (by means that can be
   * vendor-specific), and the target measurement must be among the listed
   * signed measurements.  If false, then only error if there is a problem
   * verifying the signed measurements when they are available.
   */
  requireSigned: boolean;
  /** x.509 certificates in ASN.1 DER format. */
  rootCerts: Uint8Array[];
}

/**
 * Represent minimal decisions about attestation fields until necessary to
 * add for policy reasons.
 */
export interface SevSnpPolicy {
  /** The policy for checking the signed reference values for the UEFI at launch. */
  uefi: RIMPolicy | undefined;
}

/** A policy dictating which type of MachineStates to allow */
export interface Policy {
  platform:
    | PlatformPolicy
    | undefined;
  /**
   * When the attestation is on SEV-SNP, this is the policy. Unset means no
   * constraints.
   */
  sevSnp: SevSnpPolicy | undefined;
}

function createBaseGCEInstanceInfo(): GCEInstanceInfo {
  return { zone: "", projectId: "", projectNumber: 0n, instanceName: "", instanceId: 0n };
}

export const GCEInstanceInfo: MessageFns<GCEInstanceInfo> = {
  encode(message: GCEInstanceInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.zone !== "") {
      writer.uint32(10).string(message.zone);
    }
    if (message.projectId !== "") {
      writer.uint32(18).string(message.projectId);
    }
    if (message.projectNumber !== 0n) {
      if (BigInt.asUintN(64, message.projectNumber) !== message.projectNumber) {
        throw new globalThis.Error("value provided for field message.projectNumber of type uint64 too large");
      }
      writer.uint32(24).uint64(message.projectNumber);
    }
    if (message.instanceName !== "") {
      writer.uint32(34).string(message.instanceName);
    }
    if (message.instanceId !== 0n) {
      if (BigInt.asUintN(64, message.instanceId) !== message.instanceId) {
        throw new globalThis.Error("value provided for field message.instanceId of type uint64 too large");
      }
      writer.uint32(40).uint64(message.instanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GCEInstanceInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGCEInstanceInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.zone = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.projectNumber = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.instanceName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.instanceId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GCEInstanceInfo {
    return {
      zone: isSet(object.zone) ? globalThis.String(object.zone) : "",
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      projectNumber: isSet(object.projectNumber)
        ? BigInt(object.projectNumber)
        : isSet(object.project_number)
        ? BigInt(object.project_number)
        : 0n,
      instanceName: isSet(object.instanceName)
        ? globalThis.String(object.instanceName)
        : isSet(object.instance_name)
        ? globalThis.String(object.instance_name)
        : "",
      instanceId: isSet(object.instanceId)
        ? BigInt(object.instanceId)
        : isSet(object.instance_id)
        ? BigInt(object.instance_id)
        : 0n,
    };
  },

  toJSON(message: GCEInstanceInfo): unknown {
    const obj: any = {};
    if (message.zone !== "") {
      obj.zone = message.zone;
    }
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.projectNumber !== 0n) {
      obj.projectNumber = message.projectNumber.toString();
    }
    if (message.instanceName !== "") {
      obj.instanceName = message.instanceName;
    }
    if (message.instanceId !== 0n) {
      obj.instanceId = message.instanceId.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<GCEInstanceInfo>): GCEInstanceInfo {
    return GCEInstanceInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GCEInstanceInfo>): GCEInstanceInfo {
    const message = createBaseGCEInstanceInfo();
    message.zone = object.zone ?? "";
    message.projectId = object.projectId ?? "";
    message.projectNumber = object.projectNumber ?? 0n;
    message.instanceName = object.instanceName ?? "";
    message.instanceId = object.instanceId ?? 0n;
    return message;
  },
};

function createBaseAttestation(): Attestation {
  return {
    akPub: new Uint8Array(0),
    quotes: [],
    eventLog: new Uint8Array(0),
    instanceInfo: undefined,
    canonicalEventLog: new Uint8Array(0),
    akCert: new Uint8Array(0),
    intermediateCerts: [],
    sevSnpAttestation: undefined,
    tdxAttestation: undefined,
  };
}

export const Attestation: MessageFns<Attestation> = {
  encode(message: Attestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.akPub.length !== 0) {
      writer.uint32(10).bytes(message.akPub);
    }
    for (const v of message.quotes) {
      Quote.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.eventLog.length !== 0) {
      writer.uint32(26).bytes(message.eventLog);
    }
    if (message.instanceInfo !== undefined) {
      GCEInstanceInfo.encode(message.instanceInfo, writer.uint32(34).fork()).join();
    }
    if (message.canonicalEventLog.length !== 0) {
      writer.uint32(42).bytes(message.canonicalEventLog);
    }
    if (message.akCert.length !== 0) {
      writer.uint32(50).bytes(message.akCert);
    }
    for (const v of message.intermediateCerts) {
      writer.uint32(58).bytes(v!);
    }
    if (message.sevSnpAttestation !== undefined) {
      Attestation1.encode(message.sevSnpAttestation, writer.uint32(66).fork()).join();
    }
    if (message.tdxAttestation !== undefined) {
      QuoteV4.encode(message.tdxAttestation, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Attestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.akPub = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.quotes.push(Quote.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.eventLog = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.instanceInfo = GCEInstanceInfo.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.canonicalEventLog = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.akCert = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.intermediateCerts.push(reader.bytes());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.sevSnpAttestation = Attestation1.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.tdxAttestation = QuoteV4.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Attestation {
    return {
      akPub: isSet(object.akPub)
        ? bytesFromBase64(object.akPub)
        : isSet(object.ak_pub)
        ? bytesFromBase64(object.ak_pub)
        : new Uint8Array(0),
      quotes: globalThis.Array.isArray(object?.quotes) ? object.quotes.map((e: any) => Quote.fromJSON(e)) : [],
      eventLog: isSet(object.eventLog)
        ? bytesFromBase64(object.eventLog)
        : isSet(object.event_log)
        ? bytesFromBase64(object.event_log)
        : new Uint8Array(0),
      instanceInfo: isSet(object.instanceInfo)
        ? GCEInstanceInfo.fromJSON(object.instanceInfo)
        : isSet(object.instance_info)
        ? GCEInstanceInfo.fromJSON(object.instance_info)
        : undefined,
      canonicalEventLog: isSet(object.canonicalEventLog)
        ? bytesFromBase64(object.canonicalEventLog)
        : isSet(object.canonical_event_log)
        ? bytesFromBase64(object.canonical_event_log)
        : new Uint8Array(0),
      akCert: isSet(object.akCert)
        ? bytesFromBase64(object.akCert)
        : isSet(object.ak_cert)
        ? bytesFromBase64(object.ak_cert)
        : new Uint8Array(0),
      intermediateCerts: globalThis.Array.isArray(object?.intermediateCerts)
        ? object.intermediateCerts.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.intermediate_certs)
        ? object.intermediate_certs.map((e: any) => bytesFromBase64(e))
        : [],
      sevSnpAttestation: isSet(object.sevSnpAttestation)
        ? Attestation1.fromJSON(object.sevSnpAttestation)
        : isSet(object.sev_snp_attestation)
        ? Attestation1.fromJSON(object.sev_snp_attestation)
        : undefined,
      tdxAttestation: isSet(object.tdxAttestation)
        ? QuoteV4.fromJSON(object.tdxAttestation)
        : isSet(object.tdx_attestation)
        ? QuoteV4.fromJSON(object.tdx_attestation)
        : undefined,
    };
  },

  toJSON(message: Attestation): unknown {
    const obj: any = {};
    if (message.akPub.length !== 0) {
      obj.akPub = base64FromBytes(message.akPub);
    }
    if (message.quotes?.length) {
      obj.quotes = message.quotes.map((e) => Quote.toJSON(e));
    }
    if (message.eventLog.length !== 0) {
      obj.eventLog = base64FromBytes(message.eventLog);
    }
    if (message.instanceInfo !== undefined) {
      obj.instanceInfo = GCEInstanceInfo.toJSON(message.instanceInfo);
    }
    if (message.canonicalEventLog.length !== 0) {
      obj.canonicalEventLog = base64FromBytes(message.canonicalEventLog);
    }
    if (message.akCert.length !== 0) {
      obj.akCert = base64FromBytes(message.akCert);
    }
    if (message.intermediateCerts?.length) {
      obj.intermediateCerts = message.intermediateCerts.map((e) => base64FromBytes(e));
    }
    if (message.sevSnpAttestation !== undefined) {
      obj.sevSnpAttestation = Attestation1.toJSON(message.sevSnpAttestation);
    }
    if (message.tdxAttestation !== undefined) {
      obj.tdxAttestation = QuoteV4.toJSON(message.tdxAttestation);
    }
    return obj;
  },

  create(base?: DeepPartial<Attestation>): Attestation {
    return Attestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Attestation>): Attestation {
    const message = createBaseAttestation();
    message.akPub = object.akPub ?? new Uint8Array(0);
    message.quotes = object.quotes?.map((e) => Quote.fromPartial(e)) || [];
    message.eventLog = object.eventLog ?? new Uint8Array(0);
    message.instanceInfo = (object.instanceInfo !== undefined && object.instanceInfo !== null)
      ? GCEInstanceInfo.fromPartial(object.instanceInfo)
      : undefined;
    message.canonicalEventLog = object.canonicalEventLog ?? new Uint8Array(0);
    message.akCert = object.akCert ?? new Uint8Array(0);
    message.intermediateCerts = object.intermediateCerts?.map((e) => e) || [];
    message.sevSnpAttestation = (object.sevSnpAttestation !== undefined && object.sevSnpAttestation !== null)
      ? Attestation1.fromPartial(object.sevSnpAttestation)
      : undefined;
    message.tdxAttestation = (object.tdxAttestation !== undefined && object.tdxAttestation !== null)
      ? QuoteV4.fromPartial(object.tdxAttestation)
      : undefined;
    return message;
  },
};

function createBaseSevSnpSvsmAttestation(): SevSnpSvsmAttestation {
  return {
    attestation: undefined,
    sevSnpAttestation: undefined,
    vtpmServiceManifest: new Uint8Array(0),
    vtpmServiceManifestVersion: "",
    launchEndorsement: new Uint8Array(0),
  };
}

export const SevSnpSvsmAttestation: MessageFns<SevSnpSvsmAttestation> = {
  encode(message: SevSnpSvsmAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attestation !== undefined) {
      Attestation.encode(message.attestation, writer.uint32(10).fork()).join();
    }
    if (message.sevSnpAttestation !== undefined) {
      Attestation1.encode(message.sevSnpAttestation, writer.uint32(18).fork()).join();
    }
    if (message.vtpmServiceManifest.length !== 0) {
      writer.uint32(26).bytes(message.vtpmServiceManifest);
    }
    if (message.vtpmServiceManifestVersion !== "") {
      writer.uint32(34).string(message.vtpmServiceManifestVersion);
    }
    if (message.launchEndorsement.length !== 0) {
      writer.uint32(42).bytes(message.launchEndorsement);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SevSnpSvsmAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSevSnpSvsmAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attestation = Attestation.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sevSnpAttestation = Attestation1.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vtpmServiceManifest = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vtpmServiceManifestVersion = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.launchEndorsement = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SevSnpSvsmAttestation {
    return {
      attestation: isSet(object.attestation) ? Attestation.fromJSON(object.attestation) : undefined,
      sevSnpAttestation: isSet(object.sevSnpAttestation)
        ? Attestation1.fromJSON(object.sevSnpAttestation)
        : isSet(object.sev_snp_attestation)
        ? Attestation1.fromJSON(object.sev_snp_attestation)
        : undefined,
      vtpmServiceManifest: isSet(object.vtpmServiceManifest)
        ? bytesFromBase64(object.vtpmServiceManifest)
        : isSet(object.vtpm_service_manifest)
        ? bytesFromBase64(object.vtpm_service_manifest)
        : new Uint8Array(0),
      vtpmServiceManifestVersion: isSet(object.vtpmServiceManifestVersion)
        ? globalThis.String(object.vtpmServiceManifestVersion)
        : isSet(object.vtpm_service_manifest_version)
        ? globalThis.String(object.vtpm_service_manifest_version)
        : "",
      launchEndorsement: isSet(object.launchEndorsement)
        ? bytesFromBase64(object.launchEndorsement)
        : isSet(object.launch_endorsement)
        ? bytesFromBase64(object.launch_endorsement)
        : new Uint8Array(0),
    };
  },

  toJSON(message: SevSnpSvsmAttestation): unknown {
    const obj: any = {};
    if (message.attestation !== undefined) {
      obj.attestation = Attestation.toJSON(message.attestation);
    }
    if (message.sevSnpAttestation !== undefined) {
      obj.sevSnpAttestation = Attestation1.toJSON(message.sevSnpAttestation);
    }
    if (message.vtpmServiceManifest.length !== 0) {
      obj.vtpmServiceManifest = base64FromBytes(message.vtpmServiceManifest);
    }
    if (message.vtpmServiceManifestVersion !== "") {
      obj.vtpmServiceManifestVersion = message.vtpmServiceManifestVersion;
    }
    if (message.launchEndorsement.length !== 0) {
      obj.launchEndorsement = base64FromBytes(message.launchEndorsement);
    }
    return obj;
  },

  create(base?: DeepPartial<SevSnpSvsmAttestation>): SevSnpSvsmAttestation {
    return SevSnpSvsmAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SevSnpSvsmAttestation>): SevSnpSvsmAttestation {
    const message = createBaseSevSnpSvsmAttestation();
    message.attestation = (object.attestation !== undefined && object.attestation !== null)
      ? Attestation.fromPartial(object.attestation)
      : undefined;
    message.sevSnpAttestation = (object.sevSnpAttestation !== undefined && object.sevSnpAttestation !== null)
      ? Attestation1.fromPartial(object.sevSnpAttestation)
      : undefined;
    message.vtpmServiceManifest = object.vtpmServiceManifest ?? new Uint8Array(0);
    message.vtpmServiceManifestVersion = object.vtpmServiceManifestVersion ?? "";
    message.launchEndorsement = object.launchEndorsement ?? new Uint8Array(0);
    return message;
  },
};

function createBasePlatformState(): PlatformState {
  return { scrtmVersionId: undefined, gceVersion: undefined, technology: 0, instanceInfo: undefined };
}

export const PlatformState: MessageFns<PlatformState> = {
  encode(message: PlatformState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scrtmVersionId !== undefined) {
      writer.uint32(10).bytes(message.scrtmVersionId);
    }
    if (message.gceVersion !== undefined) {
      writer.uint32(16).uint32(message.gceVersion);
    }
    if (message.technology !== 0) {
      writer.uint32(24).int32(message.technology);
    }
    if (message.instanceInfo !== undefined) {
      GCEInstanceInfo.encode(message.instanceInfo, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlatformState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlatformState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scrtmVersionId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.gceVersion = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.technology = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.instanceInfo = GCEInstanceInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlatformState {
    return {
      scrtmVersionId: isSet(object.scrtmVersionId)
        ? bytesFromBase64(object.scrtmVersionId)
        : isSet(object.scrtm_version_id)
        ? bytesFromBase64(object.scrtm_version_id)
        : undefined,
      gceVersion: isSet(object.gceVersion)
        ? globalThis.Number(object.gceVersion)
        : isSet(object.gce_version)
        ? globalThis.Number(object.gce_version)
        : undefined,
      technology: isSet(object.technology) ? gCEConfidentialTechnologyFromJSON(object.technology) : 0,
      instanceInfo: isSet(object.instanceInfo)
        ? GCEInstanceInfo.fromJSON(object.instanceInfo)
        : isSet(object.instance_info)
        ? GCEInstanceInfo.fromJSON(object.instance_info)
        : undefined,
    };
  },

  toJSON(message: PlatformState): unknown {
    const obj: any = {};
    if (message.scrtmVersionId !== undefined) {
      obj.scrtmVersionId = base64FromBytes(message.scrtmVersionId);
    }
    if (message.gceVersion !== undefined) {
      obj.gceVersion = Math.round(message.gceVersion);
    }
    if (message.technology !== 0) {
      obj.technology = gCEConfidentialTechnologyToJSON(message.technology);
    }
    if (message.instanceInfo !== undefined) {
      obj.instanceInfo = GCEInstanceInfo.toJSON(message.instanceInfo);
    }
    return obj;
  },

  create(base?: DeepPartial<PlatformState>): PlatformState {
    return PlatformState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlatformState>): PlatformState {
    const message = createBasePlatformState();
    message.scrtmVersionId = object.scrtmVersionId ?? undefined;
    message.gceVersion = object.gceVersion ?? undefined;
    message.technology = object.technology ?? 0;
    message.instanceInfo = (object.instanceInfo !== undefined && object.instanceInfo !== null)
      ? GCEInstanceInfo.fromPartial(object.instanceInfo)
      : undefined;
    return message;
  },
};

function createBaseGrubFile(): GrubFile {
  return { digest: new Uint8Array(0), untrustedFilename: new Uint8Array(0) };
}

export const GrubFile: MessageFns<GrubFile> = {
  encode(message: GrubFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.digest.length !== 0) {
      writer.uint32(10).bytes(message.digest);
    }
    if (message.untrustedFilename.length !== 0) {
      writer.uint32(18).bytes(message.untrustedFilename);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrubFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrubFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.digest = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.untrustedFilename = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrubFile {
    return {
      digest: isSet(object.digest) ? bytesFromBase64(object.digest) : new Uint8Array(0),
      untrustedFilename: isSet(object.untrustedFilename)
        ? bytesFromBase64(object.untrustedFilename)
        : isSet(object.untrusted_filename)
        ? bytesFromBase64(object.untrusted_filename)
        : new Uint8Array(0),
    };
  },

  toJSON(message: GrubFile): unknown {
    const obj: any = {};
    if (message.digest.length !== 0) {
      obj.digest = base64FromBytes(message.digest);
    }
    if (message.untrustedFilename.length !== 0) {
      obj.untrustedFilename = base64FromBytes(message.untrustedFilename);
    }
    return obj;
  },

  create(base?: DeepPartial<GrubFile>): GrubFile {
    return GrubFile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GrubFile>): GrubFile {
    const message = createBaseGrubFile();
    message.digest = object.digest ?? new Uint8Array(0);
    message.untrustedFilename = object.untrustedFilename ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGrubState(): GrubState {
  return { files: [], commands: [] };
}

export const GrubState: MessageFns<GrubState> = {
  encode(message: GrubState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      GrubFile.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.commands) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GrubState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrubState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.files.push(GrubFile.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commands.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrubState {
    return {
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => GrubFile.fromJSON(e)) : [],
      commands: globalThis.Array.isArray(object?.commands) ? object.commands.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: GrubState): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => GrubFile.toJSON(e));
    }
    if (message.commands?.length) {
      obj.commands = message.commands;
    }
    return obj;
  },

  create(base?: DeepPartial<GrubState>): GrubState {
    return GrubState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GrubState>): GrubState {
    const message = createBaseGrubState();
    message.files = object.files?.map((e) => GrubFile.fromPartial(e)) || [];
    message.commands = object.commands?.map((e) => e) || [];
    return message;
  },
};

function createBaseLinuxKernelState(): LinuxKernelState {
  return { commandLine: "" };
}

export const LinuxKernelState: MessageFns<LinuxKernelState> = {
  encode(message: LinuxKernelState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commandLine !== "") {
      writer.uint32(10).string(message.commandLine);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LinuxKernelState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLinuxKernelState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commandLine = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LinuxKernelState {
    return {
      commandLine: isSet(object.commandLine)
        ? globalThis.String(object.commandLine)
        : isSet(object.command_line)
        ? globalThis.String(object.command_line)
        : "",
    };
  },

  toJSON(message: LinuxKernelState): unknown {
    const obj: any = {};
    if (message.commandLine !== "") {
      obj.commandLine = message.commandLine;
    }
    return obj;
  },

  create(base?: DeepPartial<LinuxKernelState>): LinuxKernelState {
    return LinuxKernelState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LinuxKernelState>): LinuxKernelState {
    const message = createBaseLinuxKernelState();
    message.commandLine = object.commandLine ?? "";
    return message;
  },
};

function createBaseEvent(): Event {
  return { pcrIndex: 0, untrustedType: 0, data: new Uint8Array(0), digest: new Uint8Array(0), digestVerified: false };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pcrIndex !== 0) {
      writer.uint32(8).uint32(message.pcrIndex);
    }
    if (message.untrustedType !== 0) {
      writer.uint32(16).uint32(message.untrustedType);
    }
    if (message.data.length !== 0) {
      writer.uint32(26).bytes(message.data);
    }
    if (message.digest.length !== 0) {
      writer.uint32(34).bytes(message.digest);
    }
    if (message.digestVerified !== false) {
      writer.uint32(40).bool(message.digestVerified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pcrIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.untrustedType = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.digest = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.digestVerified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Event {
    return {
      pcrIndex: isSet(object.pcrIndex)
        ? globalThis.Number(object.pcrIndex)
        : isSet(object.pcr_index)
        ? globalThis.Number(object.pcr_index)
        : 0,
      untrustedType: isSet(object.untrustedType)
        ? globalThis.Number(object.untrustedType)
        : isSet(object.untrusted_type)
        ? globalThis.Number(object.untrusted_type)
        : 0,
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      digest: isSet(object.digest) ? bytesFromBase64(object.digest) : new Uint8Array(0),
      digestVerified: isSet(object.digestVerified)
        ? globalThis.Boolean(object.digestVerified)
        : isSet(object.digest_verified)
        ? globalThis.Boolean(object.digest_verified)
        : false,
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.pcrIndex !== 0) {
      obj.pcrIndex = Math.round(message.pcrIndex);
    }
    if (message.untrustedType !== 0) {
      obj.untrustedType = Math.round(message.untrustedType);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.digest.length !== 0) {
      obj.digest = base64FromBytes(message.digest);
    }
    if (message.digestVerified !== false) {
      obj.digestVerified = message.digestVerified;
    }
    return obj;
  },

  create(base?: DeepPartial<Event>): Event {
    return Event.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Event>): Event {
    const message = createBaseEvent();
    message.pcrIndex = object.pcrIndex ?? 0;
    message.untrustedType = object.untrustedType ?? 0;
    message.data = object.data ?? new Uint8Array(0);
    message.digest = object.digest ?? new Uint8Array(0);
    message.digestVerified = object.digestVerified ?? false;
    return message;
  },
};

function createBaseCertificate(): Certificate {
  return { der: undefined, wellKnown: undefined };
}

export const Certificate: MessageFns<Certificate> = {
  encode(message: Certificate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.der !== undefined) {
      writer.uint32(10).bytes(message.der);
    }
    if (message.wellKnown !== undefined) {
      writer.uint32(16).int32(message.wellKnown);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Certificate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCertificate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.der = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.wellKnown = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Certificate {
    return {
      der: isSet(object.der) ? bytesFromBase64(object.der) : undefined,
      wellKnown: isSet(object.wellKnown)
        ? wellKnownCertificateFromJSON(object.wellKnown)
        : isSet(object.well_known)
        ? wellKnownCertificateFromJSON(object.well_known)
        : undefined,
    };
  },

  toJSON(message: Certificate): unknown {
    const obj: any = {};
    if (message.der !== undefined) {
      obj.der = base64FromBytes(message.der);
    }
    if (message.wellKnown !== undefined) {
      obj.wellKnown = wellKnownCertificateToJSON(message.wellKnown);
    }
    return obj;
  },

  create(base?: DeepPartial<Certificate>): Certificate {
    return Certificate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Certificate>): Certificate {
    const message = createBaseCertificate();
    message.der = object.der ?? undefined;
    message.wellKnown = object.wellKnown ?? undefined;
    return message;
  },
};

function createBaseDatabase(): Database {
  return { certs: [], hashes: [] };
}

export const Database: MessageFns<Database> = {
  encode(message: Database, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.certs) {
      Certificate.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.hashes) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Database {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDatabase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.certs.push(Certificate.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hashes.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Database {
    return {
      certs: globalThis.Array.isArray(object?.certs) ? object.certs.map((e: any) => Certificate.fromJSON(e)) : [],
      hashes: globalThis.Array.isArray(object?.hashes) ? object.hashes.map((e: any) => bytesFromBase64(e)) : [],
    };
  },

  toJSON(message: Database): unknown {
    const obj: any = {};
    if (message.certs?.length) {
      obj.certs = message.certs.map((e) => Certificate.toJSON(e));
    }
    if (message.hashes?.length) {
      obj.hashes = message.hashes.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Database>): Database {
    return Database.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Database>): Database {
    const message = createBaseDatabase();
    message.certs = object.certs?.map((e) => Certificate.fromPartial(e)) || [];
    message.hashes = object.hashes?.map((e) => e) || [];
    return message;
  },
};

function createBaseSecureBootState(): SecureBootState {
  return { enabled: false, db: undefined, dbx: undefined, authority: undefined, pk: undefined, kek: undefined };
}

export const SecureBootState: MessageFns<SecureBootState> = {
  encode(message: SecureBootState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.db !== undefined) {
      Database.encode(message.db, writer.uint32(18).fork()).join();
    }
    if (message.dbx !== undefined) {
      Database.encode(message.dbx, writer.uint32(26).fork()).join();
    }
    if (message.authority !== undefined) {
      Database.encode(message.authority, writer.uint32(34).fork()).join();
    }
    if (message.pk !== undefined) {
      Database.encode(message.pk, writer.uint32(42).fork()).join();
    }
    if (message.kek !== undefined) {
      Database.encode(message.kek, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SecureBootState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSecureBootState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.db = Database.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dbx = Database.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.authority = Database.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pk = Database.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kek = Database.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SecureBootState {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      db: isSet(object.db) ? Database.fromJSON(object.db) : undefined,
      dbx: isSet(object.dbx) ? Database.fromJSON(object.dbx) : undefined,
      authority: isSet(object.authority) ? Database.fromJSON(object.authority) : undefined,
      pk: isSet(object.pk) ? Database.fromJSON(object.pk) : undefined,
      kek: isSet(object.kek) ? Database.fromJSON(object.kek) : undefined,
    };
  },

  toJSON(message: SecureBootState): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.db !== undefined) {
      obj.db = Database.toJSON(message.db);
    }
    if (message.dbx !== undefined) {
      obj.dbx = Database.toJSON(message.dbx);
    }
    if (message.authority !== undefined) {
      obj.authority = Database.toJSON(message.authority);
    }
    if (message.pk !== undefined) {
      obj.pk = Database.toJSON(message.pk);
    }
    if (message.kek !== undefined) {
      obj.kek = Database.toJSON(message.kek);
    }
    return obj;
  },

  create(base?: DeepPartial<SecureBootState>): SecureBootState {
    return SecureBootState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SecureBootState>): SecureBootState {
    const message = createBaseSecureBootState();
    message.enabled = object.enabled ?? false;
    message.db = (object.db !== undefined && object.db !== null) ? Database.fromPartial(object.db) : undefined;
    message.dbx = (object.dbx !== undefined && object.dbx !== null) ? Database.fromPartial(object.dbx) : undefined;
    message.authority = (object.authority !== undefined && object.authority !== null)
      ? Database.fromPartial(object.authority)
      : undefined;
    message.pk = (object.pk !== undefined && object.pk !== null) ? Database.fromPartial(object.pk) : undefined;
    message.kek = (object.kek !== undefined && object.kek !== null) ? Database.fromPartial(object.kek) : undefined;
    return message;
  },
};

function createBaseContainerState(): ContainerState {
  return {
    imageReference: "",
    imageDigest: "",
    restartPolicy: 0,
    imageId: "",
    args: [],
    envVars: {},
    overriddenArgs: [],
    overriddenEnvVars: {},
  };
}

export const ContainerState: MessageFns<ContainerState> = {
  encode(message: ContainerState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imageReference !== "") {
      writer.uint32(10).string(message.imageReference);
    }
    if (message.imageDigest !== "") {
      writer.uint32(18).string(message.imageDigest);
    }
    if (message.restartPolicy !== 0) {
      writer.uint32(24).int32(message.restartPolicy);
    }
    if (message.imageId !== "") {
      writer.uint32(34).string(message.imageId);
    }
    for (const v of message.args) {
      writer.uint32(42).string(v!);
    }
    globalThis.Object.entries(message.envVars).forEach(([key, value]: [string, string]) => {
      ContainerState_EnvVarsEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    for (const v of message.overriddenArgs) {
      writer.uint32(58).string(v!);
    }
    globalThis.Object.entries(message.overriddenEnvVars).forEach(([key, value]: [string, string]) => {
      ContainerState_OverriddenEnvVarsEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imageReference = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.imageDigest = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.restartPolicy = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.imageId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = ContainerState_EnvVarsEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.envVars[entry6.key] = entry6.value;
          }
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.overriddenArgs.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = ContainerState_OverriddenEnvVarsEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.overriddenEnvVars[entry8.key] = entry8.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerState {
    return {
      imageReference: isSet(object.imageReference)
        ? globalThis.String(object.imageReference)
        : isSet(object.image_reference)
        ? globalThis.String(object.image_reference)
        : "",
      imageDigest: isSet(object.imageDigest)
        ? globalThis.String(object.imageDigest)
        : isSet(object.image_digest)
        ? globalThis.String(object.image_digest)
        : "",
      restartPolicy: isSet(object.restartPolicy)
        ? restartPolicyFromJSON(object.restartPolicy)
        : isSet(object.restart_policy)
        ? restartPolicyFromJSON(object.restart_policy)
        : 0,
      imageId: isSet(object.imageId)
        ? globalThis.String(object.imageId)
        : isSet(object.image_id)
        ? globalThis.String(object.image_id)
        : "",
      args: globalThis.Array.isArray(object?.args)
        ? object.args.map((e: any) => globalThis.String(e))
        : [],
      envVars: isObject(object.envVars)
        ? (globalThis.Object.entries(object.envVars) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.env_vars)
        ? (globalThis.Object.entries(object.env_vars) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      overriddenArgs: globalThis.Array.isArray(object?.overriddenArgs)
        ? object.overriddenArgs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.overridden_args)
        ? object.overridden_args.map((e: any) => globalThis.String(e))
        : [],
      overriddenEnvVars: isObject(object.overriddenEnvVars)
        ? (globalThis.Object.entries(object.overriddenEnvVars) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.overridden_env_vars)
        ? (globalThis.Object.entries(object.overridden_env_vars) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ContainerState): unknown {
    const obj: any = {};
    if (message.imageReference !== "") {
      obj.imageReference = message.imageReference;
    }
    if (message.imageDigest !== "") {
      obj.imageDigest = message.imageDigest;
    }
    if (message.restartPolicy !== 0) {
      obj.restartPolicy = restartPolicyToJSON(message.restartPolicy);
    }
    if (message.imageId !== "") {
      obj.imageId = message.imageId;
    }
    if (message.args?.length) {
      obj.args = message.args;
    }
    if (message.envVars) {
      const entries = globalThis.Object.entries(message.envVars) as [string, string][];
      if (entries.length > 0) {
        obj.envVars = {};
        entries.forEach(([k, v]) => {
          obj.envVars[k] = v;
        });
      }
    }
    if (message.overriddenArgs?.length) {
      obj.overriddenArgs = message.overriddenArgs;
    }
    if (message.overriddenEnvVars) {
      const entries = globalThis.Object.entries(message.overriddenEnvVars) as [string, string][];
      if (entries.length > 0) {
        obj.overriddenEnvVars = {};
        entries.forEach(([k, v]) => {
          obj.overriddenEnvVars[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerState>): ContainerState {
    return ContainerState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerState>): ContainerState {
    const message = createBaseContainerState();
    message.imageReference = object.imageReference ?? "";
    message.imageDigest = object.imageDigest ?? "";
    message.restartPolicy = object.restartPolicy ?? 0;
    message.imageId = object.imageId ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.envVars = (globalThis.Object.entries(object.envVars ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.overriddenArgs = object.overriddenArgs?.map((e) => e) || [];
    message.overriddenEnvVars = (globalThis.Object.entries(object.overriddenEnvVars ?? {}) as [string, string][])
      .reduce((acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      }, {});
    return message;
  },
};

function createBaseContainerState_EnvVarsEntry(): ContainerState_EnvVarsEntry {
  return { key: "", value: "" };
}

export const ContainerState_EnvVarsEntry: MessageFns<ContainerState_EnvVarsEntry> = {
  encode(message: ContainerState_EnvVarsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerState_EnvVarsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerState_EnvVarsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerState_EnvVarsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ContainerState_EnvVarsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerState_EnvVarsEntry>): ContainerState_EnvVarsEntry {
    return ContainerState_EnvVarsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerState_EnvVarsEntry>): ContainerState_EnvVarsEntry {
    const message = createBaseContainerState_EnvVarsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseContainerState_OverriddenEnvVarsEntry(): ContainerState_OverriddenEnvVarsEntry {
  return { key: "", value: "" };
}

export const ContainerState_OverriddenEnvVarsEntry: MessageFns<ContainerState_OverriddenEnvVarsEntry> = {
  encode(message: ContainerState_OverriddenEnvVarsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerState_OverriddenEnvVarsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerState_OverriddenEnvVarsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerState_OverriddenEnvVarsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ContainerState_OverriddenEnvVarsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerState_OverriddenEnvVarsEntry>): ContainerState_OverriddenEnvVarsEntry {
    return ContainerState_OverriddenEnvVarsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerState_OverriddenEnvVarsEntry>): ContainerState_OverriddenEnvVarsEntry {
    const message = createBaseContainerState_OverriddenEnvVarsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseSemanticVersion(): SemanticVersion {
  return { major: 0, minor: 0, patch: 0 };
}

export const SemanticVersion: MessageFns<SemanticVersion> = {
  encode(message: SemanticVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.major !== 0) {
      writer.uint32(8).uint32(message.major);
    }
    if (message.minor !== 0) {
      writer.uint32(16).uint32(message.minor);
    }
    if (message.patch !== 0) {
      writer.uint32(24).uint32(message.patch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SemanticVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSemanticVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.major = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minor = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.patch = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SemanticVersion {
    return {
      major: isSet(object.major) ? globalThis.Number(object.major) : 0,
      minor: isSet(object.minor) ? globalThis.Number(object.minor) : 0,
      patch: isSet(object.patch) ? globalThis.Number(object.patch) : 0,
    };
  },

  toJSON(message: SemanticVersion): unknown {
    const obj: any = {};
    if (message.major !== 0) {
      obj.major = Math.round(message.major);
    }
    if (message.minor !== 0) {
      obj.minor = Math.round(message.minor);
    }
    if (message.patch !== 0) {
      obj.patch = Math.round(message.patch);
    }
    return obj;
  },

  create(base?: DeepPartial<SemanticVersion>): SemanticVersion {
    return SemanticVersion.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SemanticVersion>): SemanticVersion {
    const message = createBaseSemanticVersion();
    message.major = object.major ?? 0;
    message.minor = object.minor ?? 0;
    message.patch = object.patch ?? 0;
    return message;
  },
};

function createBaseHealthMonitoringState(): HealthMonitoringState {
  return { memoryEnabled: undefined };
}

export const HealthMonitoringState: MessageFns<HealthMonitoringState> = {
  encode(message: HealthMonitoringState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.memoryEnabled !== undefined) {
      writer.uint32(8).bool(message.memoryEnabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthMonitoringState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthMonitoringState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.memoryEnabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthMonitoringState {
    return {
      memoryEnabled: isSet(object.memoryEnabled)
        ? globalThis.Boolean(object.memoryEnabled)
        : isSet(object.memory_enabled)
        ? globalThis.Boolean(object.memory_enabled)
        : undefined,
    };
  },

  toJSON(message: HealthMonitoringState): unknown {
    const obj: any = {};
    if (message.memoryEnabled !== undefined) {
      obj.memoryEnabled = message.memoryEnabled;
    }
    return obj;
  },

  create(base?: DeepPartial<HealthMonitoringState>): HealthMonitoringState {
    return HealthMonitoringState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HealthMonitoringState>): HealthMonitoringState {
    const message = createBaseHealthMonitoringState();
    message.memoryEnabled = object.memoryEnabled ?? undefined;
    return message;
  },
};

function createBaseGpuDeviceState(): GpuDeviceState {
  return { ccMode: 0, nvidiaAttestationReport: undefined };
}

export const GpuDeviceState: MessageFns<GpuDeviceState> = {
  encode(message: GpuDeviceState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ccMode !== 0) {
      writer.uint32(8).int32(message.ccMode);
    }
    if (message.nvidiaAttestationReport !== undefined) {
      NvidiaAttestationReport.encode(message.nvidiaAttestationReport, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GpuDeviceState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGpuDeviceState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ccMode = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nvidiaAttestationReport = NvidiaAttestationReport.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GpuDeviceState {
    return {
      ccMode: isSet(object.ccMode)
        ? gPUDeviceCCModeFromJSON(object.ccMode)
        : isSet(object.cc_mode)
        ? gPUDeviceCCModeFromJSON(object.cc_mode)
        : 0,
      nvidiaAttestationReport: isSet(object.nvidiaAttestationReport)
        ? NvidiaAttestationReport.fromJSON(object.nvidiaAttestationReport)
        : isSet(object.nvidia_attestation_report)
        ? NvidiaAttestationReport.fromJSON(object.nvidia_attestation_report)
        : undefined,
    };
  },

  toJSON(message: GpuDeviceState): unknown {
    const obj: any = {};
    if (message.ccMode !== 0) {
      obj.ccMode = gPUDeviceCCModeToJSON(message.ccMode);
    }
    if (message.nvidiaAttestationReport !== undefined) {
      obj.nvidiaAttestationReport = NvidiaAttestationReport.toJSON(message.nvidiaAttestationReport);
    }
    return obj;
  },

  create(base?: DeepPartial<GpuDeviceState>): GpuDeviceState {
    return GpuDeviceState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GpuDeviceState>): GpuDeviceState {
    const message = createBaseGpuDeviceState();
    message.ccMode = object.ccMode ?? 0;
    message.nvidiaAttestationReport =
      (object.nvidiaAttestationReport !== undefined && object.nvidiaAttestationReport !== null)
        ? NvidiaAttestationReport.fromPartial(object.nvidiaAttestationReport)
        : undefined;
    return message;
  },
};

function createBaseAttestedCosState(): AttestedCosState {
  return {
    container: undefined,
    cosVersion: undefined,
    launcherVersion: undefined,
    healthMonitoring: undefined,
    gpuDeviceState: undefined,
  };
}

export const AttestedCosState: MessageFns<AttestedCosState> = {
  encode(message: AttestedCosState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.container !== undefined) {
      ContainerState.encode(message.container, writer.uint32(10).fork()).join();
    }
    if (message.cosVersion !== undefined) {
      SemanticVersion.encode(message.cosVersion, writer.uint32(18).fork()).join();
    }
    if (message.launcherVersion !== undefined) {
      SemanticVersion.encode(message.launcherVersion, writer.uint32(26).fork()).join();
    }
    if (message.healthMonitoring !== undefined) {
      HealthMonitoringState.encode(message.healthMonitoring, writer.uint32(34).fork()).join();
    }
    if (message.gpuDeviceState !== undefined) {
      GpuDeviceState.encode(message.gpuDeviceState, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AttestedCosState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestedCosState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.container = ContainerState.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cosVersion = SemanticVersion.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.launcherVersion = SemanticVersion.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.healthMonitoring = HealthMonitoringState.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.gpuDeviceState = GpuDeviceState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AttestedCosState {
    return {
      container: isSet(object.container) ? ContainerState.fromJSON(object.container) : undefined,
      cosVersion: isSet(object.cosVersion)
        ? SemanticVersion.fromJSON(object.cosVersion)
        : isSet(object.cos_version)
        ? SemanticVersion.fromJSON(object.cos_version)
        : undefined,
      launcherVersion: isSet(object.launcherVersion)
        ? SemanticVersion.fromJSON(object.launcherVersion)
        : isSet(object.launcher_version)
        ? SemanticVersion.fromJSON(object.launcher_version)
        : undefined,
      healthMonitoring: isSet(object.healthMonitoring)
        ? HealthMonitoringState.fromJSON(object.healthMonitoring)
        : isSet(object.health_monitoring)
        ? HealthMonitoringState.fromJSON(object.health_monitoring)
        : undefined,
      gpuDeviceState: isSet(object.gpuDeviceState)
        ? GpuDeviceState.fromJSON(object.gpuDeviceState)
        : isSet(object.gpu_device_state)
        ? GpuDeviceState.fromJSON(object.gpu_device_state)
        : undefined,
    };
  },

  toJSON(message: AttestedCosState): unknown {
    const obj: any = {};
    if (message.container !== undefined) {
      obj.container = ContainerState.toJSON(message.container);
    }
    if (message.cosVersion !== undefined) {
      obj.cosVersion = SemanticVersion.toJSON(message.cosVersion);
    }
    if (message.launcherVersion !== undefined) {
      obj.launcherVersion = SemanticVersion.toJSON(message.launcherVersion);
    }
    if (message.healthMonitoring !== undefined) {
      obj.healthMonitoring = HealthMonitoringState.toJSON(message.healthMonitoring);
    }
    if (message.gpuDeviceState !== undefined) {
      obj.gpuDeviceState = GpuDeviceState.toJSON(message.gpuDeviceState);
    }
    return obj;
  },

  create(base?: DeepPartial<AttestedCosState>): AttestedCosState {
    return AttestedCosState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AttestedCosState>): AttestedCosState {
    const message = createBaseAttestedCosState();
    message.container = (object.container !== undefined && object.container !== null)
      ? ContainerState.fromPartial(object.container)
      : undefined;
    message.cosVersion = (object.cosVersion !== undefined && object.cosVersion !== null)
      ? SemanticVersion.fromPartial(object.cosVersion)
      : undefined;
    message.launcherVersion = (object.launcherVersion !== undefined && object.launcherVersion !== null)
      ? SemanticVersion.fromPartial(object.launcherVersion)
      : undefined;
    message.healthMonitoring = (object.healthMonitoring !== undefined && object.healthMonitoring !== null)
      ? HealthMonitoringState.fromPartial(object.healthMonitoring)
      : undefined;
    message.gpuDeviceState = (object.gpuDeviceState !== undefined && object.gpuDeviceState !== null)
      ? GpuDeviceState.fromPartial(object.gpuDeviceState)
      : undefined;
    return message;
  },
};

function createBaseEfiApp(): EfiApp {
  return { digest: new Uint8Array(0) };
}

export const EfiApp: MessageFns<EfiApp> = {
  encode(message: EfiApp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.digest.length !== 0) {
      writer.uint32(10).bytes(message.digest);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EfiApp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEfiApp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.digest = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EfiApp {
    return { digest: isSet(object.digest) ? bytesFromBase64(object.digest) : new Uint8Array(0) };
  },

  toJSON(message: EfiApp): unknown {
    const obj: any = {};
    if (message.digest.length !== 0) {
      obj.digest = base64FromBytes(message.digest);
    }
    return obj;
  },

  create(base?: DeepPartial<EfiApp>): EfiApp {
    return EfiApp.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EfiApp>): EfiApp {
    const message = createBaseEfiApp();
    message.digest = object.digest ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEfiState(): EfiState {
  return { apps: [], bootServicesDrivers: [], runtimeServicesDrivers: [] };
}

export const EfiState: MessageFns<EfiState> = {
  encode(message: EfiState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.apps) {
      EfiApp.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.bootServicesDrivers) {
      EfiApp.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.runtimeServicesDrivers) {
      EfiApp.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EfiState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEfiState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apps.push(EfiApp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bootServicesDrivers.push(EfiApp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.runtimeServicesDrivers.push(EfiApp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EfiState {
    return {
      apps: globalThis.Array.isArray(object?.apps) ? object.apps.map((e: any) => EfiApp.fromJSON(e)) : [],
      bootServicesDrivers: globalThis.Array.isArray(object?.bootServicesDrivers)
        ? object.bootServicesDrivers.map((e: any) => EfiApp.fromJSON(e))
        : globalThis.Array.isArray(object?.boot_services_drivers)
        ? object.boot_services_drivers.map((e: any) => EfiApp.fromJSON(e))
        : [],
      runtimeServicesDrivers: globalThis.Array.isArray(object?.runtimeServicesDrivers)
        ? object.runtimeServicesDrivers.map((e: any) => EfiApp.fromJSON(e))
        : globalThis.Array.isArray(object?.runtime_services_drivers)
        ? object.runtime_services_drivers.map((e: any) => EfiApp.fromJSON(e))
        : [],
    };
  },

  toJSON(message: EfiState): unknown {
    const obj: any = {};
    if (message.apps?.length) {
      obj.apps = message.apps.map((e) => EfiApp.toJSON(e));
    }
    if (message.bootServicesDrivers?.length) {
      obj.bootServicesDrivers = message.bootServicesDrivers.map((e) => EfiApp.toJSON(e));
    }
    if (message.runtimeServicesDrivers?.length) {
      obj.runtimeServicesDrivers = message.runtimeServicesDrivers.map((e) => EfiApp.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<EfiState>): EfiState {
    return EfiState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EfiState>): EfiState {
    const message = createBaseEfiState();
    message.apps = object.apps?.map((e) => EfiApp.fromPartial(e)) || [];
    message.bootServicesDrivers = object.bootServicesDrivers?.map((e) => EfiApp.fromPartial(e)) || [];
    message.runtimeServicesDrivers = object.runtimeServicesDrivers?.map((e) => EfiApp.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMachineState(): MachineState {
  return {
    platform: undefined,
    secureBoot: undefined,
    rawEvents: [],
    hash: 0,
    grub: undefined,
    linuxKernel: undefined,
    cos: undefined,
    efi: undefined,
    sevSnpAttestation: undefined,
    tdxAttestation: undefined,
  };
}

export const MachineState: MessageFns<MachineState> = {
  encode(message: MachineState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.platform !== undefined) {
      PlatformState.encode(message.platform, writer.uint32(10).fork()).join();
    }
    if (message.secureBoot !== undefined) {
      SecureBootState.encode(message.secureBoot, writer.uint32(18).fork()).join();
    }
    for (const v of message.rawEvents) {
      Event.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.hash !== 0) {
      writer.uint32(32).int32(message.hash);
    }
    if (message.grub !== undefined) {
      GrubState.encode(message.grub, writer.uint32(42).fork()).join();
    }
    if (message.linuxKernel !== undefined) {
      LinuxKernelState.encode(message.linuxKernel, writer.uint32(50).fork()).join();
    }
    if (message.cos !== undefined) {
      AttestedCosState.encode(message.cos, writer.uint32(58).fork()).join();
    }
    if (message.efi !== undefined) {
      EfiState.encode(message.efi, writer.uint32(66).fork()).join();
    }
    if (message.sevSnpAttestation !== undefined) {
      Attestation1.encode(message.sevSnpAttestation, writer.uint32(74).fork()).join();
    }
    if (message.tdxAttestation !== undefined) {
      QuoteV4.encode(message.tdxAttestation, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MachineState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMachineState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.platform = PlatformState.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.secureBoot = SecureBootState.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rawEvents.push(Event.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hash = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.grub = GrubState.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.linuxKernel = LinuxKernelState.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.cos = AttestedCosState.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.efi = EfiState.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.sevSnpAttestation = Attestation1.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.tdxAttestation = QuoteV4.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MachineState {
    return {
      platform: isSet(object.platform) ? PlatformState.fromJSON(object.platform) : undefined,
      secureBoot: isSet(object.secureBoot)
        ? SecureBootState.fromJSON(object.secureBoot)
        : isSet(object.secure_boot)
        ? SecureBootState.fromJSON(object.secure_boot)
        : undefined,
      rawEvents: globalThis.Array.isArray(object?.rawEvents)
        ? object.rawEvents.map((e: any) => Event.fromJSON(e))
        : globalThis.Array.isArray(object?.raw_events)
        ? object.raw_events.map((e: any) => Event.fromJSON(e))
        : [],
      hash: isSet(object.hash) ? hashAlgoFromJSON(object.hash) : 0,
      grub: isSet(object.grub) ? GrubState.fromJSON(object.grub) : undefined,
      linuxKernel: isSet(object.linuxKernel)
        ? LinuxKernelState.fromJSON(object.linuxKernel)
        : isSet(object.linux_kernel)
        ? LinuxKernelState.fromJSON(object.linux_kernel)
        : undefined,
      cos: isSet(object.cos) ? AttestedCosState.fromJSON(object.cos) : undefined,
      efi: isSet(object.efi) ? EfiState.fromJSON(object.efi) : undefined,
      sevSnpAttestation: isSet(object.sevSnpAttestation)
        ? Attestation1.fromJSON(object.sevSnpAttestation)
        : isSet(object.sev_snp_attestation)
        ? Attestation1.fromJSON(object.sev_snp_attestation)
        : undefined,
      tdxAttestation: isSet(object.tdxAttestation)
        ? QuoteV4.fromJSON(object.tdxAttestation)
        : isSet(object.tdx_attestation)
        ? QuoteV4.fromJSON(object.tdx_attestation)
        : undefined,
    };
  },

  toJSON(message: MachineState): unknown {
    const obj: any = {};
    if (message.platform !== undefined) {
      obj.platform = PlatformState.toJSON(message.platform);
    }
    if (message.secureBoot !== undefined) {
      obj.secureBoot = SecureBootState.toJSON(message.secureBoot);
    }
    if (message.rawEvents?.length) {
      obj.rawEvents = message.rawEvents.map((e) => Event.toJSON(e));
    }
    if (message.hash !== 0) {
      obj.hash = hashAlgoToJSON(message.hash);
    }
    if (message.grub !== undefined) {
      obj.grub = GrubState.toJSON(message.grub);
    }
    if (message.linuxKernel !== undefined) {
      obj.linuxKernel = LinuxKernelState.toJSON(message.linuxKernel);
    }
    if (message.cos !== undefined) {
      obj.cos = AttestedCosState.toJSON(message.cos);
    }
    if (message.efi !== undefined) {
      obj.efi = EfiState.toJSON(message.efi);
    }
    if (message.sevSnpAttestation !== undefined) {
      obj.sevSnpAttestation = Attestation1.toJSON(message.sevSnpAttestation);
    }
    if (message.tdxAttestation !== undefined) {
      obj.tdxAttestation = QuoteV4.toJSON(message.tdxAttestation);
    }
    return obj;
  },

  create(base?: DeepPartial<MachineState>): MachineState {
    return MachineState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MachineState>): MachineState {
    const message = createBaseMachineState();
    message.platform = (object.platform !== undefined && object.platform !== null)
      ? PlatformState.fromPartial(object.platform)
      : undefined;
    message.secureBoot = (object.secureBoot !== undefined && object.secureBoot !== null)
      ? SecureBootState.fromPartial(object.secureBoot)
      : undefined;
    message.rawEvents = object.rawEvents?.map((e) => Event.fromPartial(e)) || [];
    message.hash = object.hash ?? 0;
    message.grub = (object.grub !== undefined && object.grub !== null) ? GrubState.fromPartial(object.grub) : undefined;
    message.linuxKernel = (object.linuxKernel !== undefined && object.linuxKernel !== null)
      ? LinuxKernelState.fromPartial(object.linuxKernel)
      : undefined;
    message.cos = (object.cos !== undefined && object.cos !== null)
      ? AttestedCosState.fromPartial(object.cos)
      : undefined;
    message.efi = (object.efi !== undefined && object.efi !== null) ? EfiState.fromPartial(object.efi) : undefined;
    message.sevSnpAttestation = (object.sevSnpAttestation !== undefined && object.sevSnpAttestation !== null)
      ? Attestation1.fromPartial(object.sevSnpAttestation)
      : undefined;
    message.tdxAttestation = (object.tdxAttestation !== undefined && object.tdxAttestation !== null)
      ? QuoteV4.fromPartial(object.tdxAttestation)
      : undefined;
    return message;
  },
};

function createBasePlatformPolicy(): PlatformPolicy {
  return { allowedScrtmVersionIds: [], minimumGceFirmwareVersion: 0, minimumTechnology: 0 };
}

export const PlatformPolicy: MessageFns<PlatformPolicy> = {
  encode(message: PlatformPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.allowedScrtmVersionIds) {
      writer.uint32(10).bytes(v!);
    }
    if (message.minimumGceFirmwareVersion !== 0) {
      writer.uint32(16).uint32(message.minimumGceFirmwareVersion);
    }
    if (message.minimumTechnology !== 0) {
      writer.uint32(24).int32(message.minimumTechnology);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlatformPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlatformPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.allowedScrtmVersionIds.push(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minimumGceFirmwareVersion = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.minimumTechnology = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlatformPolicy {
    return {
      allowedScrtmVersionIds: globalThis.Array.isArray(object?.allowedScrtmVersionIds)
        ? object.allowedScrtmVersionIds.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.allowed_scrtm_version_ids)
        ? object.allowed_scrtm_version_ids.map((e: any) => bytesFromBase64(e))
        : [],
      minimumGceFirmwareVersion: isSet(object.minimumGceFirmwareVersion)
        ? globalThis.Number(object.minimumGceFirmwareVersion)
        : isSet(object.minimum_gce_firmware_version)
        ? globalThis.Number(object.minimum_gce_firmware_version)
        : 0,
      minimumTechnology: isSet(object.minimumTechnology)
        ? gCEConfidentialTechnologyFromJSON(object.minimumTechnology)
        : isSet(object.minimum_technology)
        ? gCEConfidentialTechnologyFromJSON(object.minimum_technology)
        : 0,
    };
  },

  toJSON(message: PlatformPolicy): unknown {
    const obj: any = {};
    if (message.allowedScrtmVersionIds?.length) {
      obj.allowedScrtmVersionIds = message.allowedScrtmVersionIds.map((e) => base64FromBytes(e));
    }
    if (message.minimumGceFirmwareVersion !== 0) {
      obj.minimumGceFirmwareVersion = Math.round(message.minimumGceFirmwareVersion);
    }
    if (message.minimumTechnology !== 0) {
      obj.minimumTechnology = gCEConfidentialTechnologyToJSON(message.minimumTechnology);
    }
    return obj;
  },

  create(base?: DeepPartial<PlatformPolicy>): PlatformPolicy {
    return PlatformPolicy.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlatformPolicy>): PlatformPolicy {
    const message = createBasePlatformPolicy();
    message.allowedScrtmVersionIds = object.allowedScrtmVersionIds?.map((e) => e) || [];
    message.minimumGceFirmwareVersion = object.minimumGceFirmwareVersion ?? 0;
    message.minimumTechnology = object.minimumTechnology ?? 0;
    return message;
  },
};

function createBaseRIMPolicy(): RIMPolicy {
  return { requireSigned: false, rootCerts: [] };
}

export const RIMPolicy: MessageFns<RIMPolicy> = {
  encode(message: RIMPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requireSigned !== false) {
      writer.uint32(8).bool(message.requireSigned);
    }
    for (const v of message.rootCerts) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RIMPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRIMPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requireSigned = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rootCerts.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RIMPolicy {
    return {
      requireSigned: isSet(object.requireSigned)
        ? globalThis.Boolean(object.requireSigned)
        : isSet(object.require_signed)
        ? globalThis.Boolean(object.require_signed)
        : false,
      rootCerts: globalThis.Array.isArray(object?.rootCerts)
        ? object.rootCerts.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.root_certs)
        ? object.root_certs.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: RIMPolicy): unknown {
    const obj: any = {};
    if (message.requireSigned !== false) {
      obj.requireSigned = message.requireSigned;
    }
    if (message.rootCerts?.length) {
      obj.rootCerts = message.rootCerts.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RIMPolicy>): RIMPolicy {
    return RIMPolicy.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RIMPolicy>): RIMPolicy {
    const message = createBaseRIMPolicy();
    message.requireSigned = object.requireSigned ?? false;
    message.rootCerts = object.rootCerts?.map((e) => e) || [];
    return message;
  },
};

function createBaseSevSnpPolicy(): SevSnpPolicy {
  return { uefi: undefined };
}

export const SevSnpPolicy: MessageFns<SevSnpPolicy> = {
  encode(message: SevSnpPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uefi !== undefined) {
      RIMPolicy.encode(message.uefi, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SevSnpPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSevSnpPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uefi = RIMPolicy.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SevSnpPolicy {
    return { uefi: isSet(object.uefi) ? RIMPolicy.fromJSON(object.uefi) : undefined };
  },

  toJSON(message: SevSnpPolicy): unknown {
    const obj: any = {};
    if (message.uefi !== undefined) {
      obj.uefi = RIMPolicy.toJSON(message.uefi);
    }
    return obj;
  },

  create(base?: DeepPartial<SevSnpPolicy>): SevSnpPolicy {
    return SevSnpPolicy.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SevSnpPolicy>): SevSnpPolicy {
    const message = createBaseSevSnpPolicy();
    message.uefi = (object.uefi !== undefined && object.uefi !== null) ? RIMPolicy.fromPartial(object.uefi) : undefined;
    return message;
  },
};

function createBasePolicy(): Policy {
  return { platform: undefined, sevSnp: undefined };
}

export const Policy: MessageFns<Policy> = {
  encode(message: Policy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.platform !== undefined) {
      PlatformPolicy.encode(message.platform, writer.uint32(10).fork()).join();
    }
    if (message.sevSnp !== undefined) {
      SevSnpPolicy.encode(message.sevSnp, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Policy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.platform = PlatformPolicy.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sevSnp = SevSnpPolicy.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Policy {
    return {
      platform: isSet(object.platform) ? PlatformPolicy.fromJSON(object.platform) : undefined,
      sevSnp: isSet(object.sevSnp)
        ? SevSnpPolicy.fromJSON(object.sevSnp)
        : isSet(object.sev_snp)
        ? SevSnpPolicy.fromJSON(object.sev_snp)
        : undefined,
    };
  },

  toJSON(message: Policy): unknown {
    const obj: any = {};
    if (message.platform !== undefined) {
      obj.platform = PlatformPolicy.toJSON(message.platform);
    }
    if (message.sevSnp !== undefined) {
      obj.sevSnp = SevSnpPolicy.toJSON(message.sevSnp);
    }
    return obj;
  },

  create(base?: DeepPartial<Policy>): Policy {
    return Policy.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Policy>): Policy {
    const message = createBasePolicy();
    message.platform = (object.platform !== undefined && object.platform !== null)
      ? PlatformPolicy.fromPartial(object.platform)
      : undefined;
    message.sevSnp = (object.sevSnp !== undefined && object.sevSnp !== null)
      ? SevSnpPolicy.fromPartial(object.sevSnp)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
