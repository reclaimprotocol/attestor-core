// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.31.1
// source: tpm.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "tpm";

/** Enum values come from TCG Algorithm Registry - v1.27 - Table 3 */
export const ObjectType = { OBJECT_INVALID: 0, RSA: 1, ECC: 35, UNRECOGNIZED: -1 } as const;

export type ObjectType = typeof ObjectType[keyof typeof ObjectType];

export namespace ObjectType {
  export type OBJECT_INVALID = typeof ObjectType.OBJECT_INVALID;
  export type RSA = typeof ObjectType.RSA;
  export type ECC = typeof ObjectType.ECC;
  export type UNRECOGNIZED = typeof ObjectType.UNRECOGNIZED;
}

export function objectTypeFromJSON(object: any): ObjectType {
  switch (object) {
    case 0:
    case "OBJECT_INVALID":
      return ObjectType.OBJECT_INVALID;
    case 1:
    case "RSA":
      return ObjectType.RSA;
    case 35:
    case "ECC":
      return ObjectType.ECC;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ObjectType.UNRECOGNIZED;
  }
}

export function objectTypeToJSON(object: ObjectType): string {
  switch (object) {
    case ObjectType.OBJECT_INVALID:
      return "OBJECT_INVALID";
    case ObjectType.RSA:
      return "RSA";
    case ObjectType.ECC:
      return "ECC";
    case ObjectType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export const HashAlgo = { HASH_INVALID: 0, SHA1: 4, SHA256: 11, SHA384: 12, SHA512: 13, UNRECOGNIZED: -1 } as const;

export type HashAlgo = typeof HashAlgo[keyof typeof HashAlgo];

export namespace HashAlgo {
  export type HASH_INVALID = typeof HashAlgo.HASH_INVALID;
  export type SHA1 = typeof HashAlgo.SHA1;
  export type SHA256 = typeof HashAlgo.SHA256;
  export type SHA384 = typeof HashAlgo.SHA384;
  export type SHA512 = typeof HashAlgo.SHA512;
  export type UNRECOGNIZED = typeof HashAlgo.UNRECOGNIZED;
}

export function hashAlgoFromJSON(object: any): HashAlgo {
  switch (object) {
    case 0:
    case "HASH_INVALID":
      return HashAlgo.HASH_INVALID;
    case 4:
    case "SHA1":
      return HashAlgo.SHA1;
    case 11:
    case "SHA256":
      return HashAlgo.SHA256;
    case 12:
    case "SHA384":
      return HashAlgo.SHA384;
    case 13:
    case "SHA512":
      return HashAlgo.SHA512;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HashAlgo.UNRECOGNIZED;
  }
}

export function hashAlgoToJSON(object: HashAlgo): string {
  switch (object) {
    case HashAlgo.HASH_INVALID:
      return "HASH_INVALID";
    case HashAlgo.SHA1:
      return "SHA1";
    case HashAlgo.SHA256:
      return "SHA256";
    case HashAlgo.SHA384:
      return "SHA384";
    case HashAlgo.SHA512:
      return "SHA512";
    case HashAlgo.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * SealedBytes stores the result of a TPM2_Seal. The private portion (priv) has
 * already been encrypted and is no longer sensitive. The hash algorithm is
 * assumed to be SHA256.
 */
export interface SealedBytes {
  priv: Uint8Array;
  pub: Uint8Array;
  pcrs: number[];
  hash: HashAlgo;
  srk: ObjectType;
  certifiedPcrs: PCRs | undefined;
  creationData: Uint8Array;
  ticket: Uint8Array;
}

export interface ImportBlob {
  duplicate: Uint8Array;
  encryptedSeed: Uint8Array;
  publicArea: Uint8Array;
  pcrs: PCRs | undefined;
}

export interface Quote {
  /** TPM2 quote, encoded as a TPMS_ATTEST */
  quote: Uint8Array;
  /** TPM2 signature, encoded as a TPMT_SIGNATURE */
  rawSig: Uint8Array;
  /** PCR values of the bank being quoted */
  pcrs: PCRs | undefined;
}

export interface PCRs {
  hash: HashAlgo;
  pcrs: { [key: number]: Uint8Array };
}

export interface PCRs_PcrsEntry {
  key: number;
  value: Uint8Array;
}

/** Contains information corresponding an object certified with TPM2_Certify. */
export interface CertifiedBlob {
  /** Public area of certified object, encoded as a TPMT_PUBLIC */
  pubArea: Uint8Array;
  /** TPM2 certification, encoded as a TPMS_ATTEST */
  certifyInfo: Uint8Array;
  /** TPM2 signature, encoded as a TPMT_Signature */
  rawSig: Uint8Array;
}

function createBaseSealedBytes(): SealedBytes {
  return {
    priv: new Uint8Array(0),
    pub: new Uint8Array(0),
    pcrs: [],
    hash: 0,
    srk: 0,
    certifiedPcrs: undefined,
    creationData: new Uint8Array(0),
    ticket: new Uint8Array(0),
  };
}

export const SealedBytes: MessageFns<SealedBytes> = {
  encode(message: SealedBytes, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.priv.length !== 0) {
      writer.uint32(10).bytes(message.priv);
    }
    if (message.pub.length !== 0) {
      writer.uint32(18).bytes(message.pub);
    }
    writer.uint32(26).fork();
    for (const v of message.pcrs) {
      writer.uint32(v);
    }
    writer.join();
    if (message.hash !== 0) {
      writer.uint32(32).int32(message.hash);
    }
    if (message.srk !== 0) {
      writer.uint32(40).int32(message.srk);
    }
    if (message.certifiedPcrs !== undefined) {
      PCRs.encode(message.certifiedPcrs, writer.uint32(50).fork()).join();
    }
    if (message.creationData.length !== 0) {
      writer.uint32(58).bytes(message.creationData);
    }
    if (message.ticket.length !== 0) {
      writer.uint32(66).bytes(message.ticket);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SealedBytes {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSealedBytes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.priv = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pub = reader.bytes();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.pcrs.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.pcrs.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.hash = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.srk = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.certifiedPcrs = PCRs.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.creationData = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.ticket = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SealedBytes {
    return {
      priv: isSet(object.priv) ? bytesFromBase64(object.priv) : new Uint8Array(0),
      pub: isSet(object.pub) ? bytesFromBase64(object.pub) : new Uint8Array(0),
      pcrs: globalThis.Array.isArray(object?.pcrs) ? object.pcrs.map((e: any) => globalThis.Number(e)) : [],
      hash: isSet(object.hash) ? hashAlgoFromJSON(object.hash) : 0,
      srk: isSet(object.srk) ? objectTypeFromJSON(object.srk) : 0,
      certifiedPcrs: isSet(object.certifiedPcrs)
        ? PCRs.fromJSON(object.certifiedPcrs)
        : isSet(object.certified_pcrs)
        ? PCRs.fromJSON(object.certified_pcrs)
        : undefined,
      creationData: isSet(object.creationData)
        ? bytesFromBase64(object.creationData)
        : isSet(object.creation_data)
        ? bytesFromBase64(object.creation_data)
        : new Uint8Array(0),
      ticket: isSet(object.ticket) ? bytesFromBase64(object.ticket) : new Uint8Array(0),
    };
  },

  toJSON(message: SealedBytes): unknown {
    const obj: any = {};
    if (message.priv.length !== 0) {
      obj.priv = base64FromBytes(message.priv);
    }
    if (message.pub.length !== 0) {
      obj.pub = base64FromBytes(message.pub);
    }
    if (message.pcrs?.length) {
      obj.pcrs = message.pcrs.map((e) => Math.round(e));
    }
    if (message.hash !== 0) {
      obj.hash = hashAlgoToJSON(message.hash);
    }
    if (message.srk !== 0) {
      obj.srk = objectTypeToJSON(message.srk);
    }
    if (message.certifiedPcrs !== undefined) {
      obj.certifiedPcrs = PCRs.toJSON(message.certifiedPcrs);
    }
    if (message.creationData.length !== 0) {
      obj.creationData = base64FromBytes(message.creationData);
    }
    if (message.ticket.length !== 0) {
      obj.ticket = base64FromBytes(message.ticket);
    }
    return obj;
  },

  create(base?: DeepPartial<SealedBytes>): SealedBytes {
    return SealedBytes.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SealedBytes>): SealedBytes {
    const message = createBaseSealedBytes();
    message.priv = object.priv ?? new Uint8Array(0);
    message.pub = object.pub ?? new Uint8Array(0);
    message.pcrs = object.pcrs?.map((e) => e) || [];
    message.hash = object.hash ?? 0;
    message.srk = object.srk ?? 0;
    message.certifiedPcrs = (object.certifiedPcrs !== undefined && object.certifiedPcrs !== null)
      ? PCRs.fromPartial(object.certifiedPcrs)
      : undefined;
    message.creationData = object.creationData ?? new Uint8Array(0);
    message.ticket = object.ticket ?? new Uint8Array(0);
    return message;
  },
};

function createBaseImportBlob(): ImportBlob {
  return {
    duplicate: new Uint8Array(0),
    encryptedSeed: new Uint8Array(0),
    publicArea: new Uint8Array(0),
    pcrs: undefined,
  };
}

export const ImportBlob: MessageFns<ImportBlob> = {
  encode(message: ImportBlob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.duplicate.length !== 0) {
      writer.uint32(10).bytes(message.duplicate);
    }
    if (message.encryptedSeed.length !== 0) {
      writer.uint32(18).bytes(message.encryptedSeed);
    }
    if (message.publicArea.length !== 0) {
      writer.uint32(26).bytes(message.publicArea);
    }
    if (message.pcrs !== undefined) {
      PCRs.encode(message.pcrs, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportBlob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportBlob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.duplicate = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.encryptedSeed = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.publicArea = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pcrs = PCRs.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportBlob {
    return {
      duplicate: isSet(object.duplicate) ? bytesFromBase64(object.duplicate) : new Uint8Array(0),
      encryptedSeed: isSet(object.encryptedSeed)
        ? bytesFromBase64(object.encryptedSeed)
        : isSet(object.encrypted_seed)
        ? bytesFromBase64(object.encrypted_seed)
        : new Uint8Array(0),
      publicArea: isSet(object.publicArea)
        ? bytesFromBase64(object.publicArea)
        : isSet(object.public_area)
        ? bytesFromBase64(object.public_area)
        : new Uint8Array(0),
      pcrs: isSet(object.pcrs) ? PCRs.fromJSON(object.pcrs) : undefined,
    };
  },

  toJSON(message: ImportBlob): unknown {
    const obj: any = {};
    if (message.duplicate.length !== 0) {
      obj.duplicate = base64FromBytes(message.duplicate);
    }
    if (message.encryptedSeed.length !== 0) {
      obj.encryptedSeed = base64FromBytes(message.encryptedSeed);
    }
    if (message.publicArea.length !== 0) {
      obj.publicArea = base64FromBytes(message.publicArea);
    }
    if (message.pcrs !== undefined) {
      obj.pcrs = PCRs.toJSON(message.pcrs);
    }
    return obj;
  },

  create(base?: DeepPartial<ImportBlob>): ImportBlob {
    return ImportBlob.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImportBlob>): ImportBlob {
    const message = createBaseImportBlob();
    message.duplicate = object.duplicate ?? new Uint8Array(0);
    message.encryptedSeed = object.encryptedSeed ?? new Uint8Array(0);
    message.publicArea = object.publicArea ?? new Uint8Array(0);
    message.pcrs = (object.pcrs !== undefined && object.pcrs !== null) ? PCRs.fromPartial(object.pcrs) : undefined;
    return message;
  },
};

function createBaseQuote(): Quote {
  return { quote: new Uint8Array(0), rawSig: new Uint8Array(0), pcrs: undefined };
}

export const Quote: MessageFns<Quote> = {
  encode(message: Quote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quote.length !== 0) {
      writer.uint32(10).bytes(message.quote);
    }
    if (message.rawSig.length !== 0) {
      writer.uint32(18).bytes(message.rawSig);
    }
    if (message.pcrs !== undefined) {
      PCRs.encode(message.pcrs, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Quote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quote = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rawSig = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pcrs = PCRs.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Quote {
    return {
      quote: isSet(object.quote) ? bytesFromBase64(object.quote) : new Uint8Array(0),
      rawSig: isSet(object.rawSig)
        ? bytesFromBase64(object.rawSig)
        : isSet(object.raw_sig)
        ? bytesFromBase64(object.raw_sig)
        : new Uint8Array(0),
      pcrs: isSet(object.pcrs) ? PCRs.fromJSON(object.pcrs) : undefined,
    };
  },

  toJSON(message: Quote): unknown {
    const obj: any = {};
    if (message.quote.length !== 0) {
      obj.quote = base64FromBytes(message.quote);
    }
    if (message.rawSig.length !== 0) {
      obj.rawSig = base64FromBytes(message.rawSig);
    }
    if (message.pcrs !== undefined) {
      obj.pcrs = PCRs.toJSON(message.pcrs);
    }
    return obj;
  },

  create(base?: DeepPartial<Quote>): Quote {
    return Quote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Quote>): Quote {
    const message = createBaseQuote();
    message.quote = object.quote ?? new Uint8Array(0);
    message.rawSig = object.rawSig ?? new Uint8Array(0);
    message.pcrs = (object.pcrs !== undefined && object.pcrs !== null) ? PCRs.fromPartial(object.pcrs) : undefined;
    return message;
  },
};

function createBasePCRs(): PCRs {
  return { hash: 0, pcrs: {} };
}

export const PCRs: MessageFns<PCRs> = {
  encode(message: PCRs, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash !== 0) {
      writer.uint32(8).int32(message.hash);
    }
    globalThis.Object.entries(message.pcrs).forEach(([key, value]: [string, Uint8Array]) => {
      PCRs_PcrsEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PCRs {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePCRs();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hash = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = PCRs_PcrsEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.pcrs[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PCRs {
    return {
      hash: isSet(object.hash) ? hashAlgoFromJSON(object.hash) : 0,
      pcrs: isObject(object.pcrs)
        ? (globalThis.Object.entries(object.pcrs) as [string, any][]).reduce(
          (acc: { [key: number]: Uint8Array }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = bytesFromBase64(value as string);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: PCRs): unknown {
    const obj: any = {};
    if (message.hash !== 0) {
      obj.hash = hashAlgoToJSON(message.hash);
    }
    if (message.pcrs) {
      const entries = globalThis.Object.entries(message.pcrs) as [string, Uint8Array][];
      if (entries.length > 0) {
        obj.pcrs = {};
        entries.forEach(([k, v]) => {
          obj.pcrs[k] = base64FromBytes(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<PCRs>): PCRs {
    return PCRs.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PCRs>): PCRs {
    const message = createBasePCRs();
    message.hash = object.hash ?? 0;
    message.pcrs = (globalThis.Object.entries(object.pcrs ?? {}) as [string, Uint8Array][]).reduce(
      (acc: { [key: number]: Uint8Array }, [key, value]: [string, Uint8Array]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = value;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBasePCRs_PcrsEntry(): PCRs_PcrsEntry {
  return { key: 0, value: new Uint8Array(0) };
}

export const PCRs_PcrsEntry: MessageFns<PCRs_PcrsEntry> = {
  encode(message: PCRs_PcrsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PCRs_PcrsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePCRs_PcrsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PCRs_PcrsEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0),
    };
  },

  toJSON(message: PCRs_PcrsEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<PCRs_PcrsEntry>): PCRs_PcrsEntry {
    return PCRs_PcrsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PCRs_PcrsEntry>): PCRs_PcrsEntry {
    const message = createBasePCRs_PcrsEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCertifiedBlob(): CertifiedBlob {
  return { pubArea: new Uint8Array(0), certifyInfo: new Uint8Array(0), rawSig: new Uint8Array(0) };
}

export const CertifiedBlob: MessageFns<CertifiedBlob> = {
  encode(message: CertifiedBlob, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pubArea.length !== 0) {
      writer.uint32(10).bytes(message.pubArea);
    }
    if (message.certifyInfo.length !== 0) {
      writer.uint32(18).bytes(message.certifyInfo);
    }
    if (message.rawSig.length !== 0) {
      writer.uint32(26).bytes(message.rawSig);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CertifiedBlob {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCertifiedBlob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pubArea = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.certifyInfo = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.rawSig = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CertifiedBlob {
    return {
      pubArea: isSet(object.pubArea)
        ? bytesFromBase64(object.pubArea)
        : isSet(object.pub_area)
        ? bytesFromBase64(object.pub_area)
        : new Uint8Array(0),
      certifyInfo: isSet(object.certifyInfo)
        ? bytesFromBase64(object.certifyInfo)
        : isSet(object.certify_info)
        ? bytesFromBase64(object.certify_info)
        : new Uint8Array(0),
      rawSig: isSet(object.rawSig)
        ? bytesFromBase64(object.rawSig)
        : isSet(object.raw_sig)
        ? bytesFromBase64(object.raw_sig)
        : new Uint8Array(0),
    };
  },

  toJSON(message: CertifiedBlob): unknown {
    const obj: any = {};
    if (message.pubArea.length !== 0) {
      obj.pubArea = base64FromBytes(message.pubArea);
    }
    if (message.certifyInfo.length !== 0) {
      obj.certifyInfo = base64FromBytes(message.certifyInfo);
    }
    if (message.rawSig.length !== 0) {
      obj.rawSig = base64FromBytes(message.rawSig);
    }
    return obj;
  },

  create(base?: DeepPartial<CertifiedBlob>): CertifiedBlob {
    return CertifiedBlob.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CertifiedBlob>): CertifiedBlob {
    const message = createBaseCertifiedBlob();
    message.pubArea = object.pubArea ?? new Uint8Array(0);
    message.certifyInfo = object.certifyInfo ?? new Uint8Array(0);
    message.rawSig = object.rawSig ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
