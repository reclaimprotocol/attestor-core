// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.31.1
// source: attestation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GMESState } from "./state.ts";

export const protobufPackage = "confidential_space";

/** Enumerates the supported GPU architecture types. */
export const GpuArchitectureType = {
  /** GPU_ARCHITECTURE_TYPE_UNSPECIFIED - Unspecified GPU architecture type. */
  GPU_ARCHITECTURE_TYPE_UNSPECIFIED: 0,
  /** GPU_ARCHITECTURE_TYPE_HOPPER - Hopper GPU architecture type. */
  GPU_ARCHITECTURE_TYPE_HOPPER: 8,
  /** GPU_ARCHITECTURE_TYPE_BLACKWELL - Blackwell GPU architecture type. */
  GPU_ARCHITECTURE_TYPE_BLACKWELL: 10,
  UNRECOGNIZED: -1,
} as const;

export type GpuArchitectureType = typeof GpuArchitectureType[keyof typeof GpuArchitectureType];

export namespace GpuArchitectureType {
  export type GPU_ARCHITECTURE_TYPE_UNSPECIFIED = typeof GpuArchitectureType.GPU_ARCHITECTURE_TYPE_UNSPECIFIED;
  export type GPU_ARCHITECTURE_TYPE_HOPPER = typeof GpuArchitectureType.GPU_ARCHITECTURE_TYPE_HOPPER;
  export type GPU_ARCHITECTURE_TYPE_BLACKWELL = typeof GpuArchitectureType.GPU_ARCHITECTURE_TYPE_BLACKWELL;
  export type UNRECOGNIZED = typeof GpuArchitectureType.UNRECOGNIZED;
}

export function gpuArchitectureTypeFromJSON(object: any): GpuArchitectureType {
  switch (object) {
    case 0:
    case "GPU_ARCHITECTURE_TYPE_UNSPECIFIED":
      return GpuArchitectureType.GPU_ARCHITECTURE_TYPE_UNSPECIFIED;
    case 8:
    case "GPU_ARCHITECTURE_TYPE_HOPPER":
      return GpuArchitectureType.GPU_ARCHITECTURE_TYPE_HOPPER;
    case 10:
    case "GPU_ARCHITECTURE_TYPE_BLACKWELL":
      return GpuArchitectureType.GPU_ARCHITECTURE_TYPE_BLACKWELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GpuArchitectureType.UNRECOGNIZED;
  }
}

export function gpuArchitectureTypeToJSON(object: GpuArchitectureType): string {
  switch (object) {
    case GpuArchitectureType.GPU_ARCHITECTURE_TYPE_UNSPECIFIED:
      return "GPU_ARCHITECTURE_TYPE_UNSPECIFIED";
    case GpuArchitectureType.GPU_ARCHITECTURE_TYPE_HOPPER:
      return "GPU_ARCHITECTURE_TYPE_HOPPER";
    case GpuArchitectureType.GPU_ARCHITECTURE_TYPE_BLACKWELL:
      return "GPU_ARCHITECTURE_TYPE_BLACKWELL";
    case GpuArchitectureType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GpuInfo {
  /** The unique identifier of the GPU. */
  uuid: string;
  /** Driver version obtained from the GPU's attestation report. */
  driverVersion: string;
  /** VBIOS version obtained from the GPU's attestation report. */
  vbiosVersion: string;
  /** The architecture type of the GPU. */
  gpuArchitectureType: GpuArchitectureType;
  /** The verified attestation certificate chain for the GPU device. */
  attestationCertificateChain: Uint8Array;
  /**
   * This field contains SPDM request/response defined in
   * https://www.dmtf.org/sites/default/files/standards/documents/DSP0274_1.1.0.pdf
   */
  attestationReport: Uint8Array;
}

/**
 * An Nvidia attestation report for GPU and NVSwitch devices.
 * Contains necessary attestation evidence that the client collects for
 * verification.
 */
export interface NvidiaAttestationReport {
  /** Single GPU Passthrough (SPT) attestation. */
  spt?:
    | NvidiaAttestationReport_SinglePassthroughAttestation
    | undefined;
  /** Multi-GPU Secure Passthrough (MPT) attestation. */
  mpt?:
    | NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation
    | undefined;
  /** The nonce used for GPU attestation. */
  nonce: Uint8Array;
}

/** Single GPU Passthrough (SPT) attestation. */
export interface NvidiaAttestationReport_SinglePassthroughAttestation {
  /** Single GPU quote. */
  gpuQuote: GpuInfo | undefined;
}

/**
 * MultiGpuSecurePassthroughAttestation contains the attestation evidence
 * for a Multi-GPU Secure Passthrough (MPT) attestation.
 */
export interface NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation {
  /** A list of GPU quotes. */
  gpuQuotes: GpuInfo[];
}

export interface DeviceAttestationReport {
  /** An Nvidia attestation report for GPU and NVSwitch devices. */
  nvidiaReport?: NvidiaAttestationReport | undefined;
}

export interface TdxCcelQuote {
  /**
   * The CCEL event log. Formatted as described in the UEFI 2.10.
   * Contains events for guest OS boot.
   */
  ccelBootEventLog: Uint8Array;
  /**
   * Formatted as a Canonical Event Log.
   * The event log containing Attested COS launcher events.
   */
  celLaunchEventLog: Uint8Array;
  /**
   * The TDX attestation quote from the guest. A serialized Quote
   * from https://github.com/google/go-tdx-guest/blob/main/proto/tdx.proto
   */
  tdQuote: Uint8Array;
}

export interface VmAttestationQuote {
  /** A TDX with CCEL and RTMR Attestation Quote. */
  tdxCcelQuote?:
    | TdxCcelQuote
    | undefined;
  /** A standalone vTPM Attestation Quote. */
  tpmQuote?: TpmQuote | undefined;
}

/** Represents an attestation over a challenge provided by the workload. */
export interface VmAttestation {
  /** Chosen by WSD. */
  label: Uint8Array;
  /** Provided by the workload. */
  challenge: Uint8Array;
  /** Optional, provided by WSD. */
  extraData: Uint8Array;
  /** report_data is `SHA512(label || SHA512(challenge || SHA512(extra_data)))` */
  quote:
    | VmAttestationQuote
    | undefined;
  /** Runtime attestations for attached devices. */
  deviceReports: DeviceAttestationReport[];
}

export interface KeyAttestation {
  /**
   * label is "KEY_ATTESTATION" (defined as KeyAttestationLabel constant).
   * extra_data is a binary-serialized instance of KeyClaims.
   */
  attestation: VmAttestation | undefined;
}

/** Container for keys whose protection mechanism is KEY_PROTECTION_VM. */
export interface VmProtectedKeyEndorsement {
  /** Contains a VmProtectionBindingClaims. */
  bindingKeyAttestation:
    | KeyAttestation
    | undefined;
  /** Contains a VmProtectionKeyClaims. */
  protectedKeyAttestation: KeyAttestation | undefined;
}

/** Container message for endorsing keys with various levels of protection. */
export interface KeyEndorsement {
  vmProtectedKeyEndorsement?: VmProtectedKeyEndorsement | undefined;
}

/** Host and vTPM attestation evidence */
export interface TpmAttestationEndorsement {
  /** An attestation key (AK) certificate and cert chain. */
  akCertEndorsement?:
    | TpmAttestationEndorsement_AkCertEndorsement
    | undefined;
  /** Endorsement certificate chain for the Titan chip. */
  titanEndorsement?: TpmAttestationEndorsement_TitanEndorsement | undefined;
}

/** An attestation key (AK) certificate and cert chain. */
export interface TpmAttestationEndorsement_AkCertEndorsement {
  /** An attestation key (AK) certificate. */
  akCert: Uint8Array;
  /**
   * List of DER-encoded X.509 certificates which, together with the ak_cert,
   * chain back to a trusted Root Certificate.
   */
  akCertChain: Uint8Array[];
}

export interface TpmAttestationEndorsement_TitanEndorsement {
  /**
   * Certificate signed by Titan's DICE alias key over the EK used to generate
   * the quotes. On Titan, the EK is a signing key that can be used directly.
   */
  ekCert: Uint8Array;
  /** Certificate chain rooted by Titan's DeviceID public key. */
  diceCertChain: Uint8Array;
}

export interface TpmQuote {
  /** Generated by calling TPM2_Quote on each PCR bank. */
  quotes: TpmQuote_SignedQuote[];
  /**
   * The binary TCG Event Log containing events measured into the TPM by the
   * platform firmware and operating system. Formatted as described in the
   * "TCG PC Client Platform Firmware Profile Specification" as a series of
   * TCG_PCR_EVENT2 entries.
   */
  pcclientBootEventLog: Uint8Array;
  /**
   * Formatted as a Canonical Event Log.
   * The event log containing Attested COS launcher events.
   */
  celLaunchEventLog: Uint8Array;
  /** Endorsement for the TPM attestation. */
  endorsement: TpmAttestationEndorsement | undefined;
}

export interface TpmQuote_SignedQuote {
  /** Encoded as a TPM_ALG_ID. */
  hashAlgorithm: number;
  /** Raw binary values of each PCR being quoted. */
  pcrValues: { [key: number]: Uint8Array };
  /** Contains a TPMS_QUOTE_INFO. */
  tpmsAttest: Uint8Array;
  /** Contains the signature. */
  tpmtSignature: Uint8Array;
}

export interface TpmQuote_SignedQuote_PcrValuesEntry {
  key: number;
  value: Uint8Array;
}

/**
 * Provides attestations for values not present in PCRs. For example, the Titan
 * TPM on certain platforms is electrically integrated such that it can report,
 * via reserved NV index, the number of warm resets the CPU has undergone since
 * the last power cycle.
 *
 * Each signature within this message can be verified using the TpmQuote's
 * `endorsement` field.
 */
export interface TpmAuxiliaryAttestation {
  /** The signed attestations over the NV indices. */
  signedNvs: TpmAuxiliaryAttestation_SignedNvCertify[];
}

export interface TpmAuxiliaryAttestation_SignedNvCertify {
  /** Used to compute the Name of the NV index. */
  tpmsNvPublic: Uint8Array;
  /** Contains a TPMS_NV_CERTIFY_INFO. */
  tpmsAttest: Uint8Array;
  /** Contains the signature. */
  tpmtSignature: Uint8Array;
}

export interface HostAttestation {
  /** Shall be "HOST_ATTESTATION" (defined as HostAttestationLabel constant). */
  label: Uint8Array;
  /** Provided by the workload. */
  challenge: Uint8Array;
  /** Shall be empty (set by WSD, present for future expansion). */
  extraData: Uint8Array;
  /**
   * Within each of the below fields, the TPMS_ATTEST's extraData contains
   * `SHA256(label || SHA256(challenge || SHA256(extra_data)))`
   */
  tpmQuote:
    | TpmQuote
    | undefined;
  /** Auxiliary information not stored in PCRs, such as warm reset data. */
  auxAttestation: TpmAuxiliaryAttestation | undefined;
}

export interface HostACOSState {
  /** The Google Measurement State of the host machine. */
  gmes:
    | GMESState
    | undefined;
  /** The TDX CPU PIID. */
  cpuPiid: Uint8Array;
  /** Number of warm resets since the last power cycle. */
  warmResetCount: bigint;
}

function createBaseGpuInfo(): GpuInfo {
  return {
    uuid: "",
    driverVersion: "",
    vbiosVersion: "",
    gpuArchitectureType: 0,
    attestationCertificateChain: new Uint8Array(0),
    attestationReport: new Uint8Array(0),
  };
}

export const GpuInfo: MessageFns<GpuInfo> = {
  encode(message: GpuInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uuid !== "") {
      writer.uint32(10).string(message.uuid);
    }
    if (message.driverVersion !== "") {
      writer.uint32(18).string(message.driverVersion);
    }
    if (message.vbiosVersion !== "") {
      writer.uint32(26).string(message.vbiosVersion);
    }
    if (message.gpuArchitectureType !== 0) {
      writer.uint32(32).int32(message.gpuArchitectureType);
    }
    if (message.attestationCertificateChain.length !== 0) {
      writer.uint32(42).bytes(message.attestationCertificateChain);
    }
    if (message.attestationReport.length !== 0) {
      writer.uint32(50).bytes(message.attestationReport);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GpuInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGpuInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.driverVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vbiosVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.gpuArchitectureType = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.attestationCertificateChain = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.attestationReport = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GpuInfo {
    return {
      uuid: isSet(object.uuid) ? globalThis.String(object.uuid) : "",
      driverVersion: isSet(object.driverVersion)
        ? globalThis.String(object.driverVersion)
        : isSet(object.driver_version)
        ? globalThis.String(object.driver_version)
        : "",
      vbiosVersion: isSet(object.vbiosVersion)
        ? globalThis.String(object.vbiosVersion)
        : isSet(object.vbios_version)
        ? globalThis.String(object.vbios_version)
        : "",
      gpuArchitectureType: isSet(object.gpuArchitectureType)
        ? gpuArchitectureTypeFromJSON(object.gpuArchitectureType)
        : isSet(object.gpu_architecture_type)
        ? gpuArchitectureTypeFromJSON(object.gpu_architecture_type)
        : 0,
      attestationCertificateChain: isSet(object.attestationCertificateChain)
        ? bytesFromBase64(object.attestationCertificateChain)
        : isSet(object.attestation_certificate_chain)
        ? bytesFromBase64(object.attestation_certificate_chain)
        : new Uint8Array(0),
      attestationReport: isSet(object.attestationReport)
        ? bytesFromBase64(object.attestationReport)
        : isSet(object.attestation_report)
        ? bytesFromBase64(object.attestation_report)
        : new Uint8Array(0),
    };
  },

  toJSON(message: GpuInfo): unknown {
    const obj: any = {};
    if (message.uuid !== "") {
      obj.uuid = message.uuid;
    }
    if (message.driverVersion !== "") {
      obj.driverVersion = message.driverVersion;
    }
    if (message.vbiosVersion !== "") {
      obj.vbiosVersion = message.vbiosVersion;
    }
    if (message.gpuArchitectureType !== 0) {
      obj.gpuArchitectureType = gpuArchitectureTypeToJSON(message.gpuArchitectureType);
    }
    if (message.attestationCertificateChain.length !== 0) {
      obj.attestationCertificateChain = base64FromBytes(message.attestationCertificateChain);
    }
    if (message.attestationReport.length !== 0) {
      obj.attestationReport = base64FromBytes(message.attestationReport);
    }
    return obj;
  },

  create(base?: DeepPartial<GpuInfo>): GpuInfo {
    return GpuInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GpuInfo>): GpuInfo {
    const message = createBaseGpuInfo();
    message.uuid = object.uuid ?? "";
    message.driverVersion = object.driverVersion ?? "";
    message.vbiosVersion = object.vbiosVersion ?? "";
    message.gpuArchitectureType = object.gpuArchitectureType ?? 0;
    message.attestationCertificateChain = object.attestationCertificateChain ?? new Uint8Array(0);
    message.attestationReport = object.attestationReport ?? new Uint8Array(0);
    return message;
  },
};

function createBaseNvidiaAttestationReport(): NvidiaAttestationReport {
  return { spt: undefined, mpt: undefined, nonce: new Uint8Array(0) };
}

export const NvidiaAttestationReport: MessageFns<NvidiaAttestationReport> = {
  encode(message: NvidiaAttestationReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.spt !== undefined) {
      NvidiaAttestationReport_SinglePassthroughAttestation.encode(message.spt, writer.uint32(10).fork()).join();
    }
    if (message.mpt !== undefined) {
      NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation.encode(message.mpt, writer.uint32(26).fork()).join();
    }
    if (message.nonce.length !== 0) {
      writer.uint32(34).bytes(message.nonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NvidiaAttestationReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNvidiaAttestationReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.spt = NvidiaAttestationReport_SinglePassthroughAttestation.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mpt = NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nonce = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NvidiaAttestationReport {
    return {
      spt: isSet(object.spt) ? NvidiaAttestationReport_SinglePassthroughAttestation.fromJSON(object.spt) : undefined,
      mpt: isSet(object.mpt)
        ? NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation.fromJSON(object.mpt)
        : undefined,
      nonce: isSet(object.nonce) ? bytesFromBase64(object.nonce) : new Uint8Array(0),
    };
  },

  toJSON(message: NvidiaAttestationReport): unknown {
    const obj: any = {};
    if (message.spt !== undefined) {
      obj.spt = NvidiaAttestationReport_SinglePassthroughAttestation.toJSON(message.spt);
    }
    if (message.mpt !== undefined) {
      obj.mpt = NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation.toJSON(message.mpt);
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    return obj;
  },

  create(base?: DeepPartial<NvidiaAttestationReport>): NvidiaAttestationReport {
    return NvidiaAttestationReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NvidiaAttestationReport>): NvidiaAttestationReport {
    const message = createBaseNvidiaAttestationReport();
    message.spt = (object.spt !== undefined && object.spt !== null)
      ? NvidiaAttestationReport_SinglePassthroughAttestation.fromPartial(object.spt)
      : undefined;
    message.mpt = (object.mpt !== undefined && object.mpt !== null)
      ? NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation.fromPartial(object.mpt)
      : undefined;
    message.nonce = object.nonce ?? new Uint8Array(0);
    return message;
  },
};

function createBaseNvidiaAttestationReport_SinglePassthroughAttestation(): NvidiaAttestationReport_SinglePassthroughAttestation {
  return { gpuQuote: undefined };
}

export const NvidiaAttestationReport_SinglePassthroughAttestation: MessageFns<
  NvidiaAttestationReport_SinglePassthroughAttestation
> = {
  encode(
    message: NvidiaAttestationReport_SinglePassthroughAttestation,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.gpuQuote !== undefined) {
      GpuInfo.encode(message.gpuQuote, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NvidiaAttestationReport_SinglePassthroughAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNvidiaAttestationReport_SinglePassthroughAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gpuQuote = GpuInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NvidiaAttestationReport_SinglePassthroughAttestation {
    return {
      gpuQuote: isSet(object.gpuQuote)
        ? GpuInfo.fromJSON(object.gpuQuote)
        : isSet(object.gpu_quote)
        ? GpuInfo.fromJSON(object.gpu_quote)
        : undefined,
    };
  },

  toJSON(message: NvidiaAttestationReport_SinglePassthroughAttestation): unknown {
    const obj: any = {};
    if (message.gpuQuote !== undefined) {
      obj.gpuQuote = GpuInfo.toJSON(message.gpuQuote);
    }
    return obj;
  },

  create(
    base?: DeepPartial<NvidiaAttestationReport_SinglePassthroughAttestation>,
  ): NvidiaAttestationReport_SinglePassthroughAttestation {
    return NvidiaAttestationReport_SinglePassthroughAttestation.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<NvidiaAttestationReport_SinglePassthroughAttestation>,
  ): NvidiaAttestationReport_SinglePassthroughAttestation {
    const message = createBaseNvidiaAttestationReport_SinglePassthroughAttestation();
    message.gpuQuote = (object.gpuQuote !== undefined && object.gpuQuote !== null)
      ? GpuInfo.fromPartial(object.gpuQuote)
      : undefined;
    return message;
  },
};

function createBaseNvidiaAttestationReport_MultiGpuSecurePassthroughAttestation(): NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation {
  return { gpuQuotes: [] };
}

export const NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation: MessageFns<
  NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation
> = {
  encode(
    message: NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    for (const v of message.gpuQuotes) {
      GpuInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNvidiaAttestationReport_MultiGpuSecurePassthroughAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gpuQuotes.push(GpuInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation {
    return {
      gpuQuotes: globalThis.Array.isArray(object?.gpuQuotes)
        ? object.gpuQuotes.map((e: any) => GpuInfo.fromJSON(e))
        : globalThis.Array.isArray(object?.gpu_quotes)
        ? object.gpu_quotes.map((e: any) => GpuInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation): unknown {
    const obj: any = {};
    if (message.gpuQuotes?.length) {
      obj.gpuQuotes = message.gpuQuotes.map((e) => GpuInfo.toJSON(e));
    }
    return obj;
  },

  create(
    base?: DeepPartial<NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation>,
  ): NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation {
    return NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation>,
  ): NvidiaAttestationReport_MultiGpuSecurePassthroughAttestation {
    const message = createBaseNvidiaAttestationReport_MultiGpuSecurePassthroughAttestation();
    message.gpuQuotes = object.gpuQuotes?.map((e) => GpuInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeviceAttestationReport(): DeviceAttestationReport {
  return { nvidiaReport: undefined };
}

export const DeviceAttestationReport: MessageFns<DeviceAttestationReport> = {
  encode(message: DeviceAttestationReport, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nvidiaReport !== undefined) {
      NvidiaAttestationReport.encode(message.nvidiaReport, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceAttestationReport {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceAttestationReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nvidiaReport = NvidiaAttestationReport.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceAttestationReport {
    return {
      nvidiaReport: isSet(object.nvidiaReport)
        ? NvidiaAttestationReport.fromJSON(object.nvidiaReport)
        : isSet(object.nvidia_report)
        ? NvidiaAttestationReport.fromJSON(object.nvidia_report)
        : undefined,
    };
  },

  toJSON(message: DeviceAttestationReport): unknown {
    const obj: any = {};
    if (message.nvidiaReport !== undefined) {
      obj.nvidiaReport = NvidiaAttestationReport.toJSON(message.nvidiaReport);
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceAttestationReport>): DeviceAttestationReport {
    return DeviceAttestationReport.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceAttestationReport>): DeviceAttestationReport {
    const message = createBaseDeviceAttestationReport();
    message.nvidiaReport = (object.nvidiaReport !== undefined && object.nvidiaReport !== null)
      ? NvidiaAttestationReport.fromPartial(object.nvidiaReport)
      : undefined;
    return message;
  },
};

function createBaseTdxCcelQuote(): TdxCcelQuote {
  return { ccelBootEventLog: new Uint8Array(0), celLaunchEventLog: new Uint8Array(0), tdQuote: new Uint8Array(0) };
}

export const TdxCcelQuote: MessageFns<TdxCcelQuote> = {
  encode(message: TdxCcelQuote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ccelBootEventLog.length !== 0) {
      writer.uint32(10).bytes(message.ccelBootEventLog);
    }
    if (message.celLaunchEventLog.length !== 0) {
      writer.uint32(18).bytes(message.celLaunchEventLog);
    }
    if (message.tdQuote.length !== 0) {
      writer.uint32(26).bytes(message.tdQuote);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TdxCcelQuote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTdxCcelQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ccelBootEventLog = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.celLaunchEventLog = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tdQuote = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TdxCcelQuote {
    return {
      ccelBootEventLog: isSet(object.ccelBootEventLog)
        ? bytesFromBase64(object.ccelBootEventLog)
        : isSet(object.ccel_boot_event_log)
        ? bytesFromBase64(object.ccel_boot_event_log)
        : new Uint8Array(0),
      celLaunchEventLog: isSet(object.celLaunchEventLog)
        ? bytesFromBase64(object.celLaunchEventLog)
        : isSet(object.cel_launch_event_log)
        ? bytesFromBase64(object.cel_launch_event_log)
        : new Uint8Array(0),
      tdQuote: isSet(object.tdQuote)
        ? bytesFromBase64(object.tdQuote)
        : isSet(object.td_quote)
        ? bytesFromBase64(object.td_quote)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TdxCcelQuote): unknown {
    const obj: any = {};
    if (message.ccelBootEventLog.length !== 0) {
      obj.ccelBootEventLog = base64FromBytes(message.ccelBootEventLog);
    }
    if (message.celLaunchEventLog.length !== 0) {
      obj.celLaunchEventLog = base64FromBytes(message.celLaunchEventLog);
    }
    if (message.tdQuote.length !== 0) {
      obj.tdQuote = base64FromBytes(message.tdQuote);
    }
    return obj;
  },

  create(base?: DeepPartial<TdxCcelQuote>): TdxCcelQuote {
    return TdxCcelQuote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TdxCcelQuote>): TdxCcelQuote {
    const message = createBaseTdxCcelQuote();
    message.ccelBootEventLog = object.ccelBootEventLog ?? new Uint8Array(0);
    message.celLaunchEventLog = object.celLaunchEventLog ?? new Uint8Array(0);
    message.tdQuote = object.tdQuote ?? new Uint8Array(0);
    return message;
  },
};

function createBaseVmAttestationQuote(): VmAttestationQuote {
  return { tdxCcelQuote: undefined, tpmQuote: undefined };
}

export const VmAttestationQuote: MessageFns<VmAttestationQuote> = {
  encode(message: VmAttestationQuote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tdxCcelQuote !== undefined) {
      TdxCcelQuote.encode(message.tdxCcelQuote, writer.uint32(10).fork()).join();
    }
    if (message.tpmQuote !== undefined) {
      TpmQuote.encode(message.tpmQuote, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VmAttestationQuote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVmAttestationQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tdxCcelQuote = TdxCcelQuote.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tpmQuote = TpmQuote.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VmAttestationQuote {
    return {
      tdxCcelQuote: isSet(object.tdxCcelQuote)
        ? TdxCcelQuote.fromJSON(object.tdxCcelQuote)
        : isSet(object.tdx_ccel_quote)
        ? TdxCcelQuote.fromJSON(object.tdx_ccel_quote)
        : undefined,
      tpmQuote: isSet(object.tpmQuote)
        ? TpmQuote.fromJSON(object.tpmQuote)
        : isSet(object.tpm_quote)
        ? TpmQuote.fromJSON(object.tpm_quote)
        : undefined,
    };
  },

  toJSON(message: VmAttestationQuote): unknown {
    const obj: any = {};
    if (message.tdxCcelQuote !== undefined) {
      obj.tdxCcelQuote = TdxCcelQuote.toJSON(message.tdxCcelQuote);
    }
    if (message.tpmQuote !== undefined) {
      obj.tpmQuote = TpmQuote.toJSON(message.tpmQuote);
    }
    return obj;
  },

  create(base?: DeepPartial<VmAttestationQuote>): VmAttestationQuote {
    return VmAttestationQuote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VmAttestationQuote>): VmAttestationQuote {
    const message = createBaseVmAttestationQuote();
    message.tdxCcelQuote = (object.tdxCcelQuote !== undefined && object.tdxCcelQuote !== null)
      ? TdxCcelQuote.fromPartial(object.tdxCcelQuote)
      : undefined;
    message.tpmQuote = (object.tpmQuote !== undefined && object.tpmQuote !== null)
      ? TpmQuote.fromPartial(object.tpmQuote)
      : undefined;
    return message;
  },
};

function createBaseVmAttestation(): VmAttestation {
  return {
    label: new Uint8Array(0),
    challenge: new Uint8Array(0),
    extraData: new Uint8Array(0),
    quote: undefined,
    deviceReports: [],
  };
}

export const VmAttestation: MessageFns<VmAttestation> = {
  encode(message: VmAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.label.length !== 0) {
      writer.uint32(10).bytes(message.label);
    }
    if (message.challenge.length !== 0) {
      writer.uint32(18).bytes(message.challenge);
    }
    if (message.extraData.length !== 0) {
      writer.uint32(26).bytes(message.extraData);
    }
    if (message.quote !== undefined) {
      VmAttestationQuote.encode(message.quote, writer.uint32(34).fork()).join();
    }
    for (const v of message.deviceReports) {
      DeviceAttestationReport.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VmAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVmAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.label = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.challenge = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extraData = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.quote = VmAttestationQuote.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deviceReports.push(DeviceAttestationReport.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VmAttestation {
    return {
      label: isSet(object.label) ? bytesFromBase64(object.label) : new Uint8Array(0),
      challenge: isSet(object.challenge) ? bytesFromBase64(object.challenge) : new Uint8Array(0),
      extraData: isSet(object.extraData)
        ? bytesFromBase64(object.extraData)
        : isSet(object.extra_data)
        ? bytesFromBase64(object.extra_data)
        : new Uint8Array(0),
      quote: isSet(object.quote) ? VmAttestationQuote.fromJSON(object.quote) : undefined,
      deviceReports: globalThis.Array.isArray(object?.deviceReports)
        ? object.deviceReports.map((e: any) => DeviceAttestationReport.fromJSON(e))
        : globalThis.Array.isArray(object?.device_reports)
        ? object.device_reports.map((e: any) => DeviceAttestationReport.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VmAttestation): unknown {
    const obj: any = {};
    if (message.label.length !== 0) {
      obj.label = base64FromBytes(message.label);
    }
    if (message.challenge.length !== 0) {
      obj.challenge = base64FromBytes(message.challenge);
    }
    if (message.extraData.length !== 0) {
      obj.extraData = base64FromBytes(message.extraData);
    }
    if (message.quote !== undefined) {
      obj.quote = VmAttestationQuote.toJSON(message.quote);
    }
    if (message.deviceReports?.length) {
      obj.deviceReports = message.deviceReports.map((e) => DeviceAttestationReport.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<VmAttestation>): VmAttestation {
    return VmAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VmAttestation>): VmAttestation {
    const message = createBaseVmAttestation();
    message.label = object.label ?? new Uint8Array(0);
    message.challenge = object.challenge ?? new Uint8Array(0);
    message.extraData = object.extraData ?? new Uint8Array(0);
    message.quote = (object.quote !== undefined && object.quote !== null)
      ? VmAttestationQuote.fromPartial(object.quote)
      : undefined;
    message.deviceReports = object.deviceReports?.map((e) => DeviceAttestationReport.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKeyAttestation(): KeyAttestation {
  return { attestation: undefined };
}

export const KeyAttestation: MessageFns<KeyAttestation> = {
  encode(message: KeyAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attestation !== undefined) {
      VmAttestation.encode(message.attestation, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.attestation = VmAttestation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyAttestation {
    return { attestation: isSet(object.attestation) ? VmAttestation.fromJSON(object.attestation) : undefined };
  },

  toJSON(message: KeyAttestation): unknown {
    const obj: any = {};
    if (message.attestation !== undefined) {
      obj.attestation = VmAttestation.toJSON(message.attestation);
    }
    return obj;
  },

  create(base?: DeepPartial<KeyAttestation>): KeyAttestation {
    return KeyAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeyAttestation>): KeyAttestation {
    const message = createBaseKeyAttestation();
    message.attestation = (object.attestation !== undefined && object.attestation !== null)
      ? VmAttestation.fromPartial(object.attestation)
      : undefined;
    return message;
  },
};

function createBaseVmProtectedKeyEndorsement(): VmProtectedKeyEndorsement {
  return { bindingKeyAttestation: undefined, protectedKeyAttestation: undefined };
}

export const VmProtectedKeyEndorsement: MessageFns<VmProtectedKeyEndorsement> = {
  encode(message: VmProtectedKeyEndorsement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.bindingKeyAttestation !== undefined) {
      KeyAttestation.encode(message.bindingKeyAttestation, writer.uint32(10).fork()).join();
    }
    if (message.protectedKeyAttestation !== undefined) {
      KeyAttestation.encode(message.protectedKeyAttestation, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VmProtectedKeyEndorsement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVmProtectedKeyEndorsement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.bindingKeyAttestation = KeyAttestation.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.protectedKeyAttestation = KeyAttestation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VmProtectedKeyEndorsement {
    return {
      bindingKeyAttestation: isSet(object.bindingKeyAttestation)
        ? KeyAttestation.fromJSON(object.bindingKeyAttestation)
        : isSet(object.binding_key_attestation)
        ? KeyAttestation.fromJSON(object.binding_key_attestation)
        : undefined,
      protectedKeyAttestation: isSet(object.protectedKeyAttestation)
        ? KeyAttestation.fromJSON(object.protectedKeyAttestation)
        : isSet(object.protected_key_attestation)
        ? KeyAttestation.fromJSON(object.protected_key_attestation)
        : undefined,
    };
  },

  toJSON(message: VmProtectedKeyEndorsement): unknown {
    const obj: any = {};
    if (message.bindingKeyAttestation !== undefined) {
      obj.bindingKeyAttestation = KeyAttestation.toJSON(message.bindingKeyAttestation);
    }
    if (message.protectedKeyAttestation !== undefined) {
      obj.protectedKeyAttestation = KeyAttestation.toJSON(message.protectedKeyAttestation);
    }
    return obj;
  },

  create(base?: DeepPartial<VmProtectedKeyEndorsement>): VmProtectedKeyEndorsement {
    return VmProtectedKeyEndorsement.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VmProtectedKeyEndorsement>): VmProtectedKeyEndorsement {
    const message = createBaseVmProtectedKeyEndorsement();
    message.bindingKeyAttestation =
      (object.bindingKeyAttestation !== undefined && object.bindingKeyAttestation !== null)
        ? KeyAttestation.fromPartial(object.bindingKeyAttestation)
        : undefined;
    message.protectedKeyAttestation =
      (object.protectedKeyAttestation !== undefined && object.protectedKeyAttestation !== null)
        ? KeyAttestation.fromPartial(object.protectedKeyAttestation)
        : undefined;
    return message;
  },
};

function createBaseKeyEndorsement(): KeyEndorsement {
  return { vmProtectedKeyEndorsement: undefined };
}

export const KeyEndorsement: MessageFns<KeyEndorsement> = {
  encode(message: KeyEndorsement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vmProtectedKeyEndorsement !== undefined) {
      VmProtectedKeyEndorsement.encode(message.vmProtectedKeyEndorsement, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyEndorsement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyEndorsement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vmProtectedKeyEndorsement = VmProtectedKeyEndorsement.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KeyEndorsement {
    return {
      vmProtectedKeyEndorsement: isSet(object.vmProtectedKeyEndorsement)
        ? VmProtectedKeyEndorsement.fromJSON(object.vmProtectedKeyEndorsement)
        : isSet(object.vm_protected_key_endorsement)
        ? VmProtectedKeyEndorsement.fromJSON(object.vm_protected_key_endorsement)
        : undefined,
    };
  },

  toJSON(message: KeyEndorsement): unknown {
    const obj: any = {};
    if (message.vmProtectedKeyEndorsement !== undefined) {
      obj.vmProtectedKeyEndorsement = VmProtectedKeyEndorsement.toJSON(message.vmProtectedKeyEndorsement);
    }
    return obj;
  },

  create(base?: DeepPartial<KeyEndorsement>): KeyEndorsement {
    return KeyEndorsement.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KeyEndorsement>): KeyEndorsement {
    const message = createBaseKeyEndorsement();
    message.vmProtectedKeyEndorsement =
      (object.vmProtectedKeyEndorsement !== undefined && object.vmProtectedKeyEndorsement !== null)
        ? VmProtectedKeyEndorsement.fromPartial(object.vmProtectedKeyEndorsement)
        : undefined;
    return message;
  },
};

function createBaseTpmAttestationEndorsement(): TpmAttestationEndorsement {
  return { akCertEndorsement: undefined, titanEndorsement: undefined };
}

export const TpmAttestationEndorsement: MessageFns<TpmAttestationEndorsement> = {
  encode(message: TpmAttestationEndorsement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.akCertEndorsement !== undefined) {
      TpmAttestationEndorsement_AkCertEndorsement.encode(message.akCertEndorsement, writer.uint32(10).fork()).join();
    }
    if (message.titanEndorsement !== undefined) {
      TpmAttestationEndorsement_TitanEndorsement.encode(message.titanEndorsement, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAttestationEndorsement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAttestationEndorsement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.akCertEndorsement = TpmAttestationEndorsement_AkCertEndorsement.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.titanEndorsement = TpmAttestationEndorsement_TitanEndorsement.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAttestationEndorsement {
    return {
      akCertEndorsement: isSet(object.akCertEndorsement)
        ? TpmAttestationEndorsement_AkCertEndorsement.fromJSON(object.akCertEndorsement)
        : isSet(object.ak_cert_endorsement)
        ? TpmAttestationEndorsement_AkCertEndorsement.fromJSON(object.ak_cert_endorsement)
        : undefined,
      titanEndorsement: isSet(object.titanEndorsement)
        ? TpmAttestationEndorsement_TitanEndorsement.fromJSON(object.titanEndorsement)
        : isSet(object.titan_endorsement)
        ? TpmAttestationEndorsement_TitanEndorsement.fromJSON(object.titan_endorsement)
        : undefined,
    };
  },

  toJSON(message: TpmAttestationEndorsement): unknown {
    const obj: any = {};
    if (message.akCertEndorsement !== undefined) {
      obj.akCertEndorsement = TpmAttestationEndorsement_AkCertEndorsement.toJSON(message.akCertEndorsement);
    }
    if (message.titanEndorsement !== undefined) {
      obj.titanEndorsement = TpmAttestationEndorsement_TitanEndorsement.toJSON(message.titanEndorsement);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAttestationEndorsement>): TpmAttestationEndorsement {
    return TpmAttestationEndorsement.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmAttestationEndorsement>): TpmAttestationEndorsement {
    const message = createBaseTpmAttestationEndorsement();
    message.akCertEndorsement = (object.akCertEndorsement !== undefined && object.akCertEndorsement !== null)
      ? TpmAttestationEndorsement_AkCertEndorsement.fromPartial(object.akCertEndorsement)
      : undefined;
    message.titanEndorsement = (object.titanEndorsement !== undefined && object.titanEndorsement !== null)
      ? TpmAttestationEndorsement_TitanEndorsement.fromPartial(object.titanEndorsement)
      : undefined;
    return message;
  },
};

function createBaseTpmAttestationEndorsement_AkCertEndorsement(): TpmAttestationEndorsement_AkCertEndorsement {
  return { akCert: new Uint8Array(0), akCertChain: [] };
}

export const TpmAttestationEndorsement_AkCertEndorsement: MessageFns<TpmAttestationEndorsement_AkCertEndorsement> = {
  encode(
    message: TpmAttestationEndorsement_AkCertEndorsement,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.akCert.length !== 0) {
      writer.uint32(10).bytes(message.akCert);
    }
    for (const v of message.akCertChain) {
      writer.uint32(18).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAttestationEndorsement_AkCertEndorsement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAttestationEndorsement_AkCertEndorsement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.akCert = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.akCertChain.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAttestationEndorsement_AkCertEndorsement {
    return {
      akCert: isSet(object.akCert)
        ? bytesFromBase64(object.akCert)
        : isSet(object.ak_cert)
        ? bytesFromBase64(object.ak_cert)
        : new Uint8Array(0),
      akCertChain: globalThis.Array.isArray(object?.akCertChain)
        ? object.akCertChain.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.ak_cert_chain)
        ? object.ak_cert_chain.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: TpmAttestationEndorsement_AkCertEndorsement): unknown {
    const obj: any = {};
    if (message.akCert.length !== 0) {
      obj.akCert = base64FromBytes(message.akCert);
    }
    if (message.akCertChain?.length) {
      obj.akCertChain = message.akCertChain.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAttestationEndorsement_AkCertEndorsement>): TpmAttestationEndorsement_AkCertEndorsement {
    return TpmAttestationEndorsement_AkCertEndorsement.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<TpmAttestationEndorsement_AkCertEndorsement>,
  ): TpmAttestationEndorsement_AkCertEndorsement {
    const message = createBaseTpmAttestationEndorsement_AkCertEndorsement();
    message.akCert = object.akCert ?? new Uint8Array(0);
    message.akCertChain = object.akCertChain?.map((e) => e) || [];
    return message;
  },
};

function createBaseTpmAttestationEndorsement_TitanEndorsement(): TpmAttestationEndorsement_TitanEndorsement {
  return { ekCert: new Uint8Array(0), diceCertChain: new Uint8Array(0) };
}

export const TpmAttestationEndorsement_TitanEndorsement: MessageFns<TpmAttestationEndorsement_TitanEndorsement> = {
  encode(message: TpmAttestationEndorsement_TitanEndorsement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ekCert.length !== 0) {
      writer.uint32(10).bytes(message.ekCert);
    }
    if (message.diceCertChain.length !== 0) {
      writer.uint32(18).bytes(message.diceCertChain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAttestationEndorsement_TitanEndorsement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAttestationEndorsement_TitanEndorsement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ekCert = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.diceCertChain = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAttestationEndorsement_TitanEndorsement {
    return {
      ekCert: isSet(object.ekCert)
        ? bytesFromBase64(object.ekCert)
        : isSet(object.ek_cert)
        ? bytesFromBase64(object.ek_cert)
        : new Uint8Array(0),
      diceCertChain: isSet(object.diceCertChain)
        ? bytesFromBase64(object.diceCertChain)
        : isSet(object.dice_cert_chain)
        ? bytesFromBase64(object.dice_cert_chain)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TpmAttestationEndorsement_TitanEndorsement): unknown {
    const obj: any = {};
    if (message.ekCert.length !== 0) {
      obj.ekCert = base64FromBytes(message.ekCert);
    }
    if (message.diceCertChain.length !== 0) {
      obj.diceCertChain = base64FromBytes(message.diceCertChain);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAttestationEndorsement_TitanEndorsement>): TpmAttestationEndorsement_TitanEndorsement {
    return TpmAttestationEndorsement_TitanEndorsement.fromPartial(base ?? {});
  },
  fromPartial(
    object: DeepPartial<TpmAttestationEndorsement_TitanEndorsement>,
  ): TpmAttestationEndorsement_TitanEndorsement {
    const message = createBaseTpmAttestationEndorsement_TitanEndorsement();
    message.ekCert = object.ekCert ?? new Uint8Array(0);
    message.diceCertChain = object.diceCertChain ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTpmQuote(): TpmQuote {
  return {
    quotes: [],
    pcclientBootEventLog: new Uint8Array(0),
    celLaunchEventLog: new Uint8Array(0),
    endorsement: undefined,
  };
}

export const TpmQuote: MessageFns<TpmQuote> = {
  encode(message: TpmQuote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.quotes) {
      TpmQuote_SignedQuote.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pcclientBootEventLog.length !== 0) {
      writer.uint32(18).bytes(message.pcclientBootEventLog);
    }
    if (message.celLaunchEventLog.length !== 0) {
      writer.uint32(26).bytes(message.celLaunchEventLog);
    }
    if (message.endorsement !== undefined) {
      TpmAttestationEndorsement.encode(message.endorsement, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmQuote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quotes.push(TpmQuote_SignedQuote.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pcclientBootEventLog = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.celLaunchEventLog = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endorsement = TpmAttestationEndorsement.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmQuote {
    return {
      quotes: globalThis.Array.isArray(object?.quotes)
        ? object.quotes.map((e: any) => TpmQuote_SignedQuote.fromJSON(e))
        : [],
      pcclientBootEventLog: isSet(object.pcclientBootEventLog)
        ? bytesFromBase64(object.pcclientBootEventLog)
        : isSet(object.pcclient_boot_event_log)
        ? bytesFromBase64(object.pcclient_boot_event_log)
        : new Uint8Array(0),
      celLaunchEventLog: isSet(object.celLaunchEventLog)
        ? bytesFromBase64(object.celLaunchEventLog)
        : isSet(object.cel_launch_event_log)
        ? bytesFromBase64(object.cel_launch_event_log)
        : new Uint8Array(0),
      endorsement: isSet(object.endorsement) ? TpmAttestationEndorsement.fromJSON(object.endorsement) : undefined,
    };
  },

  toJSON(message: TpmQuote): unknown {
    const obj: any = {};
    if (message.quotes?.length) {
      obj.quotes = message.quotes.map((e) => TpmQuote_SignedQuote.toJSON(e));
    }
    if (message.pcclientBootEventLog.length !== 0) {
      obj.pcclientBootEventLog = base64FromBytes(message.pcclientBootEventLog);
    }
    if (message.celLaunchEventLog.length !== 0) {
      obj.celLaunchEventLog = base64FromBytes(message.celLaunchEventLog);
    }
    if (message.endorsement !== undefined) {
      obj.endorsement = TpmAttestationEndorsement.toJSON(message.endorsement);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmQuote>): TpmQuote {
    return TpmQuote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmQuote>): TpmQuote {
    const message = createBaseTpmQuote();
    message.quotes = object.quotes?.map((e) => TpmQuote_SignedQuote.fromPartial(e)) || [];
    message.pcclientBootEventLog = object.pcclientBootEventLog ?? new Uint8Array(0);
    message.celLaunchEventLog = object.celLaunchEventLog ?? new Uint8Array(0);
    message.endorsement = (object.endorsement !== undefined && object.endorsement !== null)
      ? TpmAttestationEndorsement.fromPartial(object.endorsement)
      : undefined;
    return message;
  },
};

function createBaseTpmQuote_SignedQuote(): TpmQuote_SignedQuote {
  return { hashAlgorithm: 0, pcrValues: {}, tpmsAttest: new Uint8Array(0), tpmtSignature: new Uint8Array(0) };
}

export const TpmQuote_SignedQuote: MessageFns<TpmQuote_SignedQuote> = {
  encode(message: TpmQuote_SignedQuote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hashAlgorithm !== 0) {
      writer.uint32(8).uint32(message.hashAlgorithm);
    }
    globalThis.Object.entries(message.pcrValues).forEach(([key, value]: [string, Uint8Array]) => {
      TpmQuote_SignedQuote_PcrValuesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.tpmsAttest.length !== 0) {
      writer.uint32(26).bytes(message.tpmsAttest);
    }
    if (message.tpmtSignature.length !== 0) {
      writer.uint32(34).bytes(message.tpmtSignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmQuote_SignedQuote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmQuote_SignedQuote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hashAlgorithm = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = TpmQuote_SignedQuote_PcrValuesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.pcrValues[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tpmsAttest = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tpmtSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmQuote_SignedQuote {
    return {
      hashAlgorithm: isSet(object.hashAlgorithm)
        ? globalThis.Number(object.hashAlgorithm)
        : isSet(object.hash_algorithm)
        ? globalThis.Number(object.hash_algorithm)
        : 0,
      pcrValues: isObject(object.pcrValues)
        ? (globalThis.Object.entries(object.pcrValues) as [string, any][]).reduce(
          (acc: { [key: number]: Uint8Array }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = bytesFromBase64(value as string);
            return acc;
          },
          {},
        )
        : isObject(object.pcr_values)
        ? (globalThis.Object.entries(object.pcr_values) as [string, any][]).reduce(
          (acc: { [key: number]: Uint8Array }, [key, value]: [string, any]) => {
            acc[globalThis.Number(key)] = bytesFromBase64(value as string);
            return acc;
          },
          {},
        )
        : {},
      tpmsAttest: isSet(object.tpmsAttest)
        ? bytesFromBase64(object.tpmsAttest)
        : isSet(object.tpms_attest)
        ? bytesFromBase64(object.tpms_attest)
        : new Uint8Array(0),
      tpmtSignature: isSet(object.tpmtSignature)
        ? bytesFromBase64(object.tpmtSignature)
        : isSet(object.tpmt_signature)
        ? bytesFromBase64(object.tpmt_signature)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TpmQuote_SignedQuote): unknown {
    const obj: any = {};
    if (message.hashAlgorithm !== 0) {
      obj.hashAlgorithm = Math.round(message.hashAlgorithm);
    }
    if (message.pcrValues) {
      const entries = globalThis.Object.entries(message.pcrValues) as [string, Uint8Array][];
      if (entries.length > 0) {
        obj.pcrValues = {};
        entries.forEach(([k, v]) => {
          obj.pcrValues[k] = base64FromBytes(v);
        });
      }
    }
    if (message.tpmsAttest.length !== 0) {
      obj.tpmsAttest = base64FromBytes(message.tpmsAttest);
    }
    if (message.tpmtSignature.length !== 0) {
      obj.tpmtSignature = base64FromBytes(message.tpmtSignature);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmQuote_SignedQuote>): TpmQuote_SignedQuote {
    return TpmQuote_SignedQuote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmQuote_SignedQuote>): TpmQuote_SignedQuote {
    const message = createBaseTpmQuote_SignedQuote();
    message.hashAlgorithm = object.hashAlgorithm ?? 0;
    message.pcrValues = (globalThis.Object.entries(object.pcrValues ?? {}) as [string, Uint8Array][]).reduce(
      (acc: { [key: number]: Uint8Array }, [key, value]: [string, Uint8Array]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = value;
        }
        return acc;
      },
      {},
    );
    message.tpmsAttest = object.tpmsAttest ?? new Uint8Array(0);
    message.tpmtSignature = object.tpmtSignature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTpmQuote_SignedQuote_PcrValuesEntry(): TpmQuote_SignedQuote_PcrValuesEntry {
  return { key: 0, value: new Uint8Array(0) };
}

export const TpmQuote_SignedQuote_PcrValuesEntry: MessageFns<TpmQuote_SignedQuote_PcrValuesEntry> = {
  encode(message: TpmQuote_SignedQuote_PcrValuesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmQuote_SignedQuote_PcrValuesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmQuote_SignedQuote_PcrValuesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmQuote_SignedQuote_PcrValuesEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0),
    };
  },

  toJSON(message: TpmQuote_SignedQuote_PcrValuesEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmQuote_SignedQuote_PcrValuesEntry>): TpmQuote_SignedQuote_PcrValuesEntry {
    return TpmQuote_SignedQuote_PcrValuesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmQuote_SignedQuote_PcrValuesEntry>): TpmQuote_SignedQuote_PcrValuesEntry {
    const message = createBaseTpmQuote_SignedQuote_PcrValuesEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTpmAuxiliaryAttestation(): TpmAuxiliaryAttestation {
  return { signedNvs: [] };
}

export const TpmAuxiliaryAttestation: MessageFns<TpmAuxiliaryAttestation> = {
  encode(message: TpmAuxiliaryAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.signedNvs) {
      TpmAuxiliaryAttestation_SignedNvCertify.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAuxiliaryAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAuxiliaryAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signedNvs.push(TpmAuxiliaryAttestation_SignedNvCertify.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAuxiliaryAttestation {
    return {
      signedNvs: globalThis.Array.isArray(object?.signedNvs)
        ? object.signedNvs.map((e: any) => TpmAuxiliaryAttestation_SignedNvCertify.fromJSON(e))
        : globalThis.Array.isArray(object?.signed_nvs)
        ? object.signed_nvs.map((e: any) => TpmAuxiliaryAttestation_SignedNvCertify.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TpmAuxiliaryAttestation): unknown {
    const obj: any = {};
    if (message.signedNvs?.length) {
      obj.signedNvs = message.signedNvs.map((e) => TpmAuxiliaryAttestation_SignedNvCertify.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAuxiliaryAttestation>): TpmAuxiliaryAttestation {
    return TpmAuxiliaryAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmAuxiliaryAttestation>): TpmAuxiliaryAttestation {
    const message = createBaseTpmAuxiliaryAttestation();
    message.signedNvs = object.signedNvs?.map((e) => TpmAuxiliaryAttestation_SignedNvCertify.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTpmAuxiliaryAttestation_SignedNvCertify(): TpmAuxiliaryAttestation_SignedNvCertify {
  return { tpmsNvPublic: new Uint8Array(0), tpmsAttest: new Uint8Array(0), tpmtSignature: new Uint8Array(0) };
}

export const TpmAuxiliaryAttestation_SignedNvCertify: MessageFns<TpmAuxiliaryAttestation_SignedNvCertify> = {
  encode(message: TpmAuxiliaryAttestation_SignedNvCertify, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tpmsNvPublic.length !== 0) {
      writer.uint32(26).bytes(message.tpmsNvPublic);
    }
    if (message.tpmsAttest.length !== 0) {
      writer.uint32(34).bytes(message.tpmsAttest);
    }
    if (message.tpmtSignature.length !== 0) {
      writer.uint32(42).bytes(message.tpmtSignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAuxiliaryAttestation_SignedNvCertify {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAuxiliaryAttestation_SignedNvCertify();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tpmsNvPublic = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tpmsAttest = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tpmtSignature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAuxiliaryAttestation_SignedNvCertify {
    return {
      tpmsNvPublic: isSet(object.tpmsNvPublic)
        ? bytesFromBase64(object.tpmsNvPublic)
        : isSet(object.tpms_nv_public)
        ? bytesFromBase64(object.tpms_nv_public)
        : new Uint8Array(0),
      tpmsAttest: isSet(object.tpmsAttest)
        ? bytesFromBase64(object.tpmsAttest)
        : isSet(object.tpms_attest)
        ? bytesFromBase64(object.tpms_attest)
        : new Uint8Array(0),
      tpmtSignature: isSet(object.tpmtSignature)
        ? bytesFromBase64(object.tpmtSignature)
        : isSet(object.tpmt_signature)
        ? bytesFromBase64(object.tpmt_signature)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TpmAuxiliaryAttestation_SignedNvCertify): unknown {
    const obj: any = {};
    if (message.tpmsNvPublic.length !== 0) {
      obj.tpmsNvPublic = base64FromBytes(message.tpmsNvPublic);
    }
    if (message.tpmsAttest.length !== 0) {
      obj.tpmsAttest = base64FromBytes(message.tpmsAttest);
    }
    if (message.tpmtSignature.length !== 0) {
      obj.tpmtSignature = base64FromBytes(message.tpmtSignature);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAuxiliaryAttestation_SignedNvCertify>): TpmAuxiliaryAttestation_SignedNvCertify {
    return TpmAuxiliaryAttestation_SignedNvCertify.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmAuxiliaryAttestation_SignedNvCertify>): TpmAuxiliaryAttestation_SignedNvCertify {
    const message = createBaseTpmAuxiliaryAttestation_SignedNvCertify();
    message.tpmsNvPublic = object.tpmsNvPublic ?? new Uint8Array(0);
    message.tpmsAttest = object.tpmsAttest ?? new Uint8Array(0);
    message.tpmtSignature = object.tpmtSignature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseHostAttestation(): HostAttestation {
  return {
    label: new Uint8Array(0),
    challenge: new Uint8Array(0),
    extraData: new Uint8Array(0),
    tpmQuote: undefined,
    auxAttestation: undefined,
  };
}

export const HostAttestation: MessageFns<HostAttestation> = {
  encode(message: HostAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.label.length !== 0) {
      writer.uint32(10).bytes(message.label);
    }
    if (message.challenge.length !== 0) {
      writer.uint32(18).bytes(message.challenge);
    }
    if (message.extraData.length !== 0) {
      writer.uint32(26).bytes(message.extraData);
    }
    if (message.tpmQuote !== undefined) {
      TpmQuote.encode(message.tpmQuote, writer.uint32(34).fork()).join();
    }
    if (message.auxAttestation !== undefined) {
      TpmAuxiliaryAttestation.encode(message.auxAttestation, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HostAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHostAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.label = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.challenge = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extraData = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.tpmQuote = TpmQuote.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.auxAttestation = TpmAuxiliaryAttestation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HostAttestation {
    return {
      label: isSet(object.label) ? bytesFromBase64(object.label) : new Uint8Array(0),
      challenge: isSet(object.challenge) ? bytesFromBase64(object.challenge) : new Uint8Array(0),
      extraData: isSet(object.extraData)
        ? bytesFromBase64(object.extraData)
        : isSet(object.extra_data)
        ? bytesFromBase64(object.extra_data)
        : new Uint8Array(0),
      tpmQuote: isSet(object.tpmQuote)
        ? TpmQuote.fromJSON(object.tpmQuote)
        : isSet(object.tpm_quote)
        ? TpmQuote.fromJSON(object.tpm_quote)
        : undefined,
      auxAttestation: isSet(object.auxAttestation)
        ? TpmAuxiliaryAttestation.fromJSON(object.auxAttestation)
        : isSet(object.aux_attestation)
        ? TpmAuxiliaryAttestation.fromJSON(object.aux_attestation)
        : undefined,
    };
  },

  toJSON(message: HostAttestation): unknown {
    const obj: any = {};
    if (message.label.length !== 0) {
      obj.label = base64FromBytes(message.label);
    }
    if (message.challenge.length !== 0) {
      obj.challenge = base64FromBytes(message.challenge);
    }
    if (message.extraData.length !== 0) {
      obj.extraData = base64FromBytes(message.extraData);
    }
    if (message.tpmQuote !== undefined) {
      obj.tpmQuote = TpmQuote.toJSON(message.tpmQuote);
    }
    if (message.auxAttestation !== undefined) {
      obj.auxAttestation = TpmAuxiliaryAttestation.toJSON(message.auxAttestation);
    }
    return obj;
  },

  create(base?: DeepPartial<HostAttestation>): HostAttestation {
    return HostAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HostAttestation>): HostAttestation {
    const message = createBaseHostAttestation();
    message.label = object.label ?? new Uint8Array(0);
    message.challenge = object.challenge ?? new Uint8Array(0);
    message.extraData = object.extraData ?? new Uint8Array(0);
    message.tpmQuote = (object.tpmQuote !== undefined && object.tpmQuote !== null)
      ? TpmQuote.fromPartial(object.tpmQuote)
      : undefined;
    message.auxAttestation = (object.auxAttestation !== undefined && object.auxAttestation !== null)
      ? TpmAuxiliaryAttestation.fromPartial(object.auxAttestation)
      : undefined;
    return message;
  },
};

function createBaseHostACOSState(): HostACOSState {
  return { gmes: undefined, cpuPiid: new Uint8Array(0), warmResetCount: 0n };
}

export const HostACOSState: MessageFns<HostACOSState> = {
  encode(message: HostACOSState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gmes !== undefined) {
      GMESState.encode(message.gmes, writer.uint32(10).fork()).join();
    }
    if (message.cpuPiid.length !== 0) {
      writer.uint32(18).bytes(message.cpuPiid);
    }
    if (message.warmResetCount !== 0n) {
      if (BigInt.asIntN(64, message.warmResetCount) !== message.warmResetCount) {
        throw new globalThis.Error("value provided for field message.warmResetCount of type int64 too large");
      }
      writer.uint32(24).int64(message.warmResetCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HostACOSState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHostACOSState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gmes = GMESState.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cpuPiid = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.warmResetCount = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HostACOSState {
    return {
      gmes: isSet(object.gmes) ? GMESState.fromJSON(object.gmes) : undefined,
      cpuPiid: isSet(object.cpuPiid)
        ? bytesFromBase64(object.cpuPiid)
        : isSet(object.cpu_piid)
        ? bytesFromBase64(object.cpu_piid)
        : new Uint8Array(0),
      warmResetCount: isSet(object.warmResetCount)
        ? BigInt(object.warmResetCount)
        : isSet(object.warm_reset_count)
        ? BigInt(object.warm_reset_count)
        : 0n,
    };
  },

  toJSON(message: HostACOSState): unknown {
    const obj: any = {};
    if (message.gmes !== undefined) {
      obj.gmes = GMESState.toJSON(message.gmes);
    }
    if (message.cpuPiid.length !== 0) {
      obj.cpuPiid = base64FromBytes(message.cpuPiid);
    }
    if (message.warmResetCount !== 0n) {
      obj.warmResetCount = message.warmResetCount.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<HostACOSState>): HostACOSState {
    return HostACOSState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HostACOSState>): HostACOSState {
    const message = createBaseHostACOSState();
    message.gmes = (object.gmes !== undefined && object.gmes !== null) ? GMESState.fromPartial(object.gmes) : undefined;
    message.cpuPiid = object.cpuPiid ?? new Uint8Array(0);
    message.warmResetCount = object.warmResetCount ?? 0n;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
