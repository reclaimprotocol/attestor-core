// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.31.1
// source: sevsnp.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { UInt32Value } from "./google/protobuf/wrappers.ts";

export const protobufPackage = "sevsnp";

/**
 * Report represents an SEV-SNP ATTESTATION_REPORT, specified in SEV SNP API
 *  documentation https://www.amd.com/system/files/TechDocs/56860.pdf
 */
export interface Report {
  /** Should be 2 for revision 1.55, and 3 for revision 1.56 */
  version: number;
  guestSvn: number;
  policy: bigint;
  /** Should be 16 bytes long */
  familyId: Uint8Array;
  /** Should be 16 bytes long */
  imageId: Uint8Array;
  vmpl: number;
  signatureAlgo: number;
  currentTcb: bigint;
  platformInfo: bigint;
  /** AuthorKeyEn, MaskChipKey, SigningKey */
  signerInfo: number;
  /** Should be 64 bytes long */
  reportData: Uint8Array;
  /** Should be 48 bytes long */
  measurement: Uint8Array;
  /** Should be 32 bytes long */
  hostData: Uint8Array;
  /** Should be 48 bytes long */
  idKeyDigest: Uint8Array;
  /** Should be 48 bytes long */
  authorKeyDigest: Uint8Array;
  /** Should be 32 bytes long */
  reportId: Uint8Array;
  /** Should be 32 bytes long */
  reportIdMa: Uint8Array;
  reportedTcb: bigint;
  /** Should be 64 bytes long */
  chipId: Uint8Array;
  committedTcb: bigint;
  /**
   * Each build, minor, major triple should be packed together in a uint32
   * packed together at 7:0, 15:8, 23:16 respectively
   */
  currentBuild: number;
  currentMinor: number;
  currentMajor: number;
  committedBuild: number;
  committedMinor: number;
  committedMajor: number;
  launchTcb: bigint;
  /** Should be 512 bytes long */
  signature: Uint8Array;
  /** The cpuid(1).eax & 0x0fff0fff representation of family/model/stepping */
  cpuid1eaxFms: number;
  launchMitVector: bigint;
  currentMitVector: bigint;
}

export interface CertificateChain {
  /**
   * The versioned chip endorsement key's certificate for the
   * key that signed this report.
   */
  vcekCert: Uint8Array;
  /**
   * The versioned loaded endorsement key's certificate for the
   * key that signed this report.
   */
  vlekCert: Uint8Array;
  /** The AMD SEV or AMD SEV-VLEK certificate that signed the V?EK cert. */
  askCert: Uint8Array;
  /** The AMD Root key certificate (signs the ASK cert). */
  arkCert: Uint8Array;
  /**
   * A certificate the host may inject to endorse the measurement of the
   * firmware.
   *
   * @deprecated
   */
  firmwareCert: Uint8Array;
  /** Non-standard certificates the host may inject. */
  extras: { [key: string]: Uint8Array };
}

export interface CertificateChain_ExtrasEntry {
  key: string;
  value: Uint8Array;
}

/**
 * The CPUID[EAX=1] version information includes product info as described in
 * the AMD KDS specification. The product name, model, and stepping values are
 * important for determining the required parameters to KDS when requesting the
 * endorsement key's certificate.
 */
export interface SevProduct {
  name: SevProduct_SevProductName;
  /**
   * Must be a 4-bit number
   *
   * @deprecated
   */
  stepping: number;
  machineStepping: number | undefined;
}

export const SevProduct_SevProductName = {
  SEV_PRODUCT_UNKNOWN: 0,
  SEV_PRODUCT_MILAN: 1,
  SEV_PRODUCT_GENOA: 2,
  SEV_PRODUCT_TURIN: 3,
  UNRECOGNIZED: -1,
} as const;

export type SevProduct_SevProductName = typeof SevProduct_SevProductName[keyof typeof SevProduct_SevProductName];

export namespace SevProduct_SevProductName {
  export type SEV_PRODUCT_UNKNOWN = typeof SevProduct_SevProductName.SEV_PRODUCT_UNKNOWN;
  export type SEV_PRODUCT_MILAN = typeof SevProduct_SevProductName.SEV_PRODUCT_MILAN;
  export type SEV_PRODUCT_GENOA = typeof SevProduct_SevProductName.SEV_PRODUCT_GENOA;
  export type SEV_PRODUCT_TURIN = typeof SevProduct_SevProductName.SEV_PRODUCT_TURIN;
  export type UNRECOGNIZED = typeof SevProduct_SevProductName.UNRECOGNIZED;
}

export function sevProduct_SevProductNameFromJSON(object: any): SevProduct_SevProductName {
  switch (object) {
    case 0:
    case "SEV_PRODUCT_UNKNOWN":
      return SevProduct_SevProductName.SEV_PRODUCT_UNKNOWN;
    case 1:
    case "SEV_PRODUCT_MILAN":
      return SevProduct_SevProductName.SEV_PRODUCT_MILAN;
    case 2:
    case "SEV_PRODUCT_GENOA":
      return SevProduct_SevProductName.SEV_PRODUCT_GENOA;
    case 3:
    case "SEV_PRODUCT_TURIN":
      return SevProduct_SevProductName.SEV_PRODUCT_TURIN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SevProduct_SevProductName.UNRECOGNIZED;
  }
}

export function sevProduct_SevProductNameToJSON(object: SevProduct_SevProductName): string {
  switch (object) {
    case SevProduct_SevProductName.SEV_PRODUCT_UNKNOWN:
      return "SEV_PRODUCT_UNKNOWN";
    case SevProduct_SevProductName.SEV_PRODUCT_MILAN:
      return "SEV_PRODUCT_MILAN";
    case SevProduct_SevProductName.SEV_PRODUCT_GENOA:
      return "SEV_PRODUCT_GENOA";
    case SevProduct_SevProductName.SEV_PRODUCT_TURIN:
      return "SEV_PRODUCT_TURIN";
    case SevProduct_SevProductName.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Attestation {
  report: Report | undefined;
  certificateChain: CertificateChain | undefined;
  product: SevProduct | undefined;
}

function createBaseReport(): Report {
  return {
    version: 0,
    guestSvn: 0,
    policy: 0n,
    familyId: new Uint8Array(0),
    imageId: new Uint8Array(0),
    vmpl: 0,
    signatureAlgo: 0,
    currentTcb: 0n,
    platformInfo: 0n,
    signerInfo: 0,
    reportData: new Uint8Array(0),
    measurement: new Uint8Array(0),
    hostData: new Uint8Array(0),
    idKeyDigest: new Uint8Array(0),
    authorKeyDigest: new Uint8Array(0),
    reportId: new Uint8Array(0),
    reportIdMa: new Uint8Array(0),
    reportedTcb: 0n,
    chipId: new Uint8Array(0),
    committedTcb: 0n,
    currentBuild: 0,
    currentMinor: 0,
    currentMajor: 0,
    committedBuild: 0,
    committedMinor: 0,
    committedMajor: 0,
    launchTcb: 0n,
    signature: new Uint8Array(0),
    cpuid1eaxFms: 0,
    launchMitVector: 0n,
    currentMitVector: 0n,
  };
}

export const Report: MessageFns<Report> = {
  encode(message: Report, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.guestSvn !== 0) {
      writer.uint32(16).uint32(message.guestSvn);
    }
    if (message.policy !== 0n) {
      if (BigInt.asUintN(64, message.policy) !== message.policy) {
        throw new globalThis.Error("value provided for field message.policy of type uint64 too large");
      }
      writer.uint32(24).uint64(message.policy);
    }
    if (message.familyId.length !== 0) {
      writer.uint32(34).bytes(message.familyId);
    }
    if (message.imageId.length !== 0) {
      writer.uint32(42).bytes(message.imageId);
    }
    if (message.vmpl !== 0) {
      writer.uint32(48).uint32(message.vmpl);
    }
    if (message.signatureAlgo !== 0) {
      writer.uint32(56).uint32(message.signatureAlgo);
    }
    if (message.currentTcb !== 0n) {
      if (BigInt.asUintN(64, message.currentTcb) !== message.currentTcb) {
        throw new globalThis.Error("value provided for field message.currentTcb of type uint64 too large");
      }
      writer.uint32(64).uint64(message.currentTcb);
    }
    if (message.platformInfo !== 0n) {
      if (BigInt.asUintN(64, message.platformInfo) !== message.platformInfo) {
        throw new globalThis.Error("value provided for field message.platformInfo of type uint64 too large");
      }
      writer.uint32(72).uint64(message.platformInfo);
    }
    if (message.signerInfo !== 0) {
      writer.uint32(80).uint32(message.signerInfo);
    }
    if (message.reportData.length !== 0) {
      writer.uint32(90).bytes(message.reportData);
    }
    if (message.measurement.length !== 0) {
      writer.uint32(98).bytes(message.measurement);
    }
    if (message.hostData.length !== 0) {
      writer.uint32(106).bytes(message.hostData);
    }
    if (message.idKeyDigest.length !== 0) {
      writer.uint32(114).bytes(message.idKeyDigest);
    }
    if (message.authorKeyDigest.length !== 0) {
      writer.uint32(122).bytes(message.authorKeyDigest);
    }
    if (message.reportId.length !== 0) {
      writer.uint32(130).bytes(message.reportId);
    }
    if (message.reportIdMa.length !== 0) {
      writer.uint32(138).bytes(message.reportIdMa);
    }
    if (message.reportedTcb !== 0n) {
      if (BigInt.asUintN(64, message.reportedTcb) !== message.reportedTcb) {
        throw new globalThis.Error("value provided for field message.reportedTcb of type uint64 too large");
      }
      writer.uint32(144).uint64(message.reportedTcb);
    }
    if (message.chipId.length !== 0) {
      writer.uint32(154).bytes(message.chipId);
    }
    if (message.committedTcb !== 0n) {
      if (BigInt.asUintN(64, message.committedTcb) !== message.committedTcb) {
        throw new globalThis.Error("value provided for field message.committedTcb of type uint64 too large");
      }
      writer.uint32(160).uint64(message.committedTcb);
    }
    if (message.currentBuild !== 0) {
      writer.uint32(168).uint32(message.currentBuild);
    }
    if (message.currentMinor !== 0) {
      writer.uint32(176).uint32(message.currentMinor);
    }
    if (message.currentMajor !== 0) {
      writer.uint32(184).uint32(message.currentMajor);
    }
    if (message.committedBuild !== 0) {
      writer.uint32(192).uint32(message.committedBuild);
    }
    if (message.committedMinor !== 0) {
      writer.uint32(200).uint32(message.committedMinor);
    }
    if (message.committedMajor !== 0) {
      writer.uint32(208).uint32(message.committedMajor);
    }
    if (message.launchTcb !== 0n) {
      if (BigInt.asUintN(64, message.launchTcb) !== message.launchTcb) {
        throw new globalThis.Error("value provided for field message.launchTcb of type uint64 too large");
      }
      writer.uint32(216).uint64(message.launchTcb);
    }
    if (message.signature.length !== 0) {
      writer.uint32(226).bytes(message.signature);
    }
    if (message.cpuid1eaxFms !== 0) {
      writer.uint32(232).uint32(message.cpuid1eaxFms);
    }
    if (message.launchMitVector !== 0n) {
      if (BigInt.asUintN(64, message.launchMitVector) !== message.launchMitVector) {
        throw new globalThis.Error("value provided for field message.launchMitVector of type uint64 too large");
      }
      writer.uint32(240).uint64(message.launchMitVector);
    }
    if (message.currentMitVector !== 0n) {
      if (BigInt.asUintN(64, message.currentMitVector) !== message.currentMitVector) {
        throw new globalThis.Error("value provided for field message.currentMitVector of type uint64 too large");
      }
      writer.uint32(248).uint64(message.currentMitVector);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Report {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReport();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.version = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.guestSvn = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.policy = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.familyId = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.imageId = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.vmpl = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.signatureAlgo = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.currentTcb = reader.uint64() as bigint;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.platformInfo = reader.uint64() as bigint;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.signerInfo = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.reportData = reader.bytes();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.measurement = reader.bytes();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.hostData = reader.bytes();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.idKeyDigest = reader.bytes();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.authorKeyDigest = reader.bytes();
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.reportId = reader.bytes();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.reportIdMa = reader.bytes();
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.reportedTcb = reader.uint64() as bigint;
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.chipId = reader.bytes();
          continue;
        }
        case 20: {
          if (tag !== 160) {
            break;
          }

          message.committedTcb = reader.uint64() as bigint;
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.currentBuild = reader.uint32();
          continue;
        }
        case 22: {
          if (tag !== 176) {
            break;
          }

          message.currentMinor = reader.uint32();
          continue;
        }
        case 23: {
          if (tag !== 184) {
            break;
          }

          message.currentMajor = reader.uint32();
          continue;
        }
        case 24: {
          if (tag !== 192) {
            break;
          }

          message.committedBuild = reader.uint32();
          continue;
        }
        case 25: {
          if (tag !== 200) {
            break;
          }

          message.committedMinor = reader.uint32();
          continue;
        }
        case 26: {
          if (tag !== 208) {
            break;
          }

          message.committedMajor = reader.uint32();
          continue;
        }
        case 27: {
          if (tag !== 216) {
            break;
          }

          message.launchTcb = reader.uint64() as bigint;
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
        case 29: {
          if (tag !== 232) {
            break;
          }

          message.cpuid1eaxFms = reader.uint32();
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.launchMitVector = reader.uint64() as bigint;
          continue;
        }
        case 31: {
          if (tag !== 248) {
            break;
          }

          message.currentMitVector = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Report {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      guestSvn: isSet(object.guestSvn)
        ? globalThis.Number(object.guestSvn)
        : isSet(object.guest_svn)
        ? globalThis.Number(object.guest_svn)
        : 0,
      policy: isSet(object.policy) ? BigInt(object.policy) : 0n,
      familyId: isSet(object.familyId)
        ? bytesFromBase64(object.familyId)
        : isSet(object.family_id)
        ? bytesFromBase64(object.family_id)
        : new Uint8Array(0),
      imageId: isSet(object.imageId)
        ? bytesFromBase64(object.imageId)
        : isSet(object.image_id)
        ? bytesFromBase64(object.image_id)
        : new Uint8Array(0),
      vmpl: isSet(object.vmpl) ? globalThis.Number(object.vmpl) : 0,
      signatureAlgo: isSet(object.signatureAlgo)
        ? globalThis.Number(object.signatureAlgo)
        : isSet(object.signature_algo)
        ? globalThis.Number(object.signature_algo)
        : 0,
      currentTcb: isSet(object.currentTcb)
        ? BigInt(object.currentTcb)
        : isSet(object.current_tcb)
        ? BigInt(object.current_tcb)
        : 0n,
      platformInfo: isSet(object.platformInfo)
        ? BigInt(object.platformInfo)
        : isSet(object.platform_info)
        ? BigInt(object.platform_info)
        : 0n,
      signerInfo: isSet(object.signerInfo)
        ? globalThis.Number(object.signerInfo)
        : isSet(object.signer_info)
        ? globalThis.Number(object.signer_info)
        : 0,
      reportData: isSet(object.reportData)
        ? bytesFromBase64(object.reportData)
        : isSet(object.report_data)
        ? bytesFromBase64(object.report_data)
        : new Uint8Array(0),
      measurement: isSet(object.measurement) ? bytesFromBase64(object.measurement) : new Uint8Array(0),
      hostData: isSet(object.hostData)
        ? bytesFromBase64(object.hostData)
        : isSet(object.host_data)
        ? bytesFromBase64(object.host_data)
        : new Uint8Array(0),
      idKeyDigest: isSet(object.idKeyDigest)
        ? bytesFromBase64(object.idKeyDigest)
        : isSet(object.id_key_digest)
        ? bytesFromBase64(object.id_key_digest)
        : new Uint8Array(0),
      authorKeyDigest: isSet(object.authorKeyDigest)
        ? bytesFromBase64(object.authorKeyDigest)
        : isSet(object.author_key_digest)
        ? bytesFromBase64(object.author_key_digest)
        : new Uint8Array(0),
      reportId: isSet(object.reportId)
        ? bytesFromBase64(object.reportId)
        : isSet(object.report_id)
        ? bytesFromBase64(object.report_id)
        : new Uint8Array(0),
      reportIdMa: isSet(object.reportIdMa)
        ? bytesFromBase64(object.reportIdMa)
        : isSet(object.report_id_ma)
        ? bytesFromBase64(object.report_id_ma)
        : new Uint8Array(0),
      reportedTcb: isSet(object.reportedTcb)
        ? BigInt(object.reportedTcb)
        : isSet(object.reported_tcb)
        ? BigInt(object.reported_tcb)
        : 0n,
      chipId: isSet(object.chipId)
        ? bytesFromBase64(object.chipId)
        : isSet(object.chip_id)
        ? bytesFromBase64(object.chip_id)
        : new Uint8Array(0),
      committedTcb: isSet(object.committedTcb)
        ? BigInt(object.committedTcb)
        : isSet(object.committed_tcb)
        ? BigInt(object.committed_tcb)
        : 0n,
      currentBuild: isSet(object.currentBuild)
        ? globalThis.Number(object.currentBuild)
        : isSet(object.current_build)
        ? globalThis.Number(object.current_build)
        : 0,
      currentMinor: isSet(object.currentMinor)
        ? globalThis.Number(object.currentMinor)
        : isSet(object.current_minor)
        ? globalThis.Number(object.current_minor)
        : 0,
      currentMajor: isSet(object.currentMajor)
        ? globalThis.Number(object.currentMajor)
        : isSet(object.current_major)
        ? globalThis.Number(object.current_major)
        : 0,
      committedBuild: isSet(object.committedBuild)
        ? globalThis.Number(object.committedBuild)
        : isSet(object.committed_build)
        ? globalThis.Number(object.committed_build)
        : 0,
      committedMinor: isSet(object.committedMinor)
        ? globalThis.Number(object.committedMinor)
        : isSet(object.committed_minor)
        ? globalThis.Number(object.committed_minor)
        : 0,
      committedMajor: isSet(object.committedMajor)
        ? globalThis.Number(object.committedMajor)
        : isSet(object.committed_major)
        ? globalThis.Number(object.committed_major)
        : 0,
      launchTcb: isSet(object.launchTcb)
        ? BigInt(object.launchTcb)
        : isSet(object.launch_tcb)
        ? BigInt(object.launch_tcb)
        : 0n,
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
      cpuid1eaxFms: isSet(object.cpuid1eaxFms)
        ? globalThis.Number(object.cpuid1eaxFms)
        : isSet(object.cpuid1eax_fms)
        ? globalThis.Number(object.cpuid1eax_fms)
        : 0,
      launchMitVector: isSet(object.launchMitVector)
        ? BigInt(object.launchMitVector)
        : isSet(object.launch_mit_vector)
        ? BigInt(object.launch_mit_vector)
        : 0n,
      currentMitVector: isSet(object.currentMitVector)
        ? BigInt(object.currentMitVector)
        : isSet(object.current_mit_vector)
        ? BigInt(object.current_mit_vector)
        : 0n,
    };
  },

  toJSON(message: Report): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.guestSvn !== 0) {
      obj.guestSvn = Math.round(message.guestSvn);
    }
    if (message.policy !== 0n) {
      obj.policy = message.policy.toString();
    }
    if (message.familyId.length !== 0) {
      obj.familyId = base64FromBytes(message.familyId);
    }
    if (message.imageId.length !== 0) {
      obj.imageId = base64FromBytes(message.imageId);
    }
    if (message.vmpl !== 0) {
      obj.vmpl = Math.round(message.vmpl);
    }
    if (message.signatureAlgo !== 0) {
      obj.signatureAlgo = Math.round(message.signatureAlgo);
    }
    if (message.currentTcb !== 0n) {
      obj.currentTcb = message.currentTcb.toString();
    }
    if (message.platformInfo !== 0n) {
      obj.platformInfo = message.platformInfo.toString();
    }
    if (message.signerInfo !== 0) {
      obj.signerInfo = Math.round(message.signerInfo);
    }
    if (message.reportData.length !== 0) {
      obj.reportData = base64FromBytes(message.reportData);
    }
    if (message.measurement.length !== 0) {
      obj.measurement = base64FromBytes(message.measurement);
    }
    if (message.hostData.length !== 0) {
      obj.hostData = base64FromBytes(message.hostData);
    }
    if (message.idKeyDigest.length !== 0) {
      obj.idKeyDigest = base64FromBytes(message.idKeyDigest);
    }
    if (message.authorKeyDigest.length !== 0) {
      obj.authorKeyDigest = base64FromBytes(message.authorKeyDigest);
    }
    if (message.reportId.length !== 0) {
      obj.reportId = base64FromBytes(message.reportId);
    }
    if (message.reportIdMa.length !== 0) {
      obj.reportIdMa = base64FromBytes(message.reportIdMa);
    }
    if (message.reportedTcb !== 0n) {
      obj.reportedTcb = message.reportedTcb.toString();
    }
    if (message.chipId.length !== 0) {
      obj.chipId = base64FromBytes(message.chipId);
    }
    if (message.committedTcb !== 0n) {
      obj.committedTcb = message.committedTcb.toString();
    }
    if (message.currentBuild !== 0) {
      obj.currentBuild = Math.round(message.currentBuild);
    }
    if (message.currentMinor !== 0) {
      obj.currentMinor = Math.round(message.currentMinor);
    }
    if (message.currentMajor !== 0) {
      obj.currentMajor = Math.round(message.currentMajor);
    }
    if (message.committedBuild !== 0) {
      obj.committedBuild = Math.round(message.committedBuild);
    }
    if (message.committedMinor !== 0) {
      obj.committedMinor = Math.round(message.committedMinor);
    }
    if (message.committedMajor !== 0) {
      obj.committedMajor = Math.round(message.committedMajor);
    }
    if (message.launchTcb !== 0n) {
      obj.launchTcb = message.launchTcb.toString();
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.cpuid1eaxFms !== 0) {
      obj.cpuid1eaxFms = Math.round(message.cpuid1eaxFms);
    }
    if (message.launchMitVector !== 0n) {
      obj.launchMitVector = message.launchMitVector.toString();
    }
    if (message.currentMitVector !== 0n) {
      obj.currentMitVector = message.currentMitVector.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<Report>): Report {
    return Report.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Report>): Report {
    const message = createBaseReport();
    message.version = object.version ?? 0;
    message.guestSvn = object.guestSvn ?? 0;
    message.policy = object.policy ?? 0n;
    message.familyId = object.familyId ?? new Uint8Array(0);
    message.imageId = object.imageId ?? new Uint8Array(0);
    message.vmpl = object.vmpl ?? 0;
    message.signatureAlgo = object.signatureAlgo ?? 0;
    message.currentTcb = object.currentTcb ?? 0n;
    message.platformInfo = object.platformInfo ?? 0n;
    message.signerInfo = object.signerInfo ?? 0;
    message.reportData = object.reportData ?? new Uint8Array(0);
    message.measurement = object.measurement ?? new Uint8Array(0);
    message.hostData = object.hostData ?? new Uint8Array(0);
    message.idKeyDigest = object.idKeyDigest ?? new Uint8Array(0);
    message.authorKeyDigest = object.authorKeyDigest ?? new Uint8Array(0);
    message.reportId = object.reportId ?? new Uint8Array(0);
    message.reportIdMa = object.reportIdMa ?? new Uint8Array(0);
    message.reportedTcb = object.reportedTcb ?? 0n;
    message.chipId = object.chipId ?? new Uint8Array(0);
    message.committedTcb = object.committedTcb ?? 0n;
    message.currentBuild = object.currentBuild ?? 0;
    message.currentMinor = object.currentMinor ?? 0;
    message.currentMajor = object.currentMajor ?? 0;
    message.committedBuild = object.committedBuild ?? 0;
    message.committedMinor = object.committedMinor ?? 0;
    message.committedMajor = object.committedMajor ?? 0;
    message.launchTcb = object.launchTcb ?? 0n;
    message.signature = object.signature ?? new Uint8Array(0);
    message.cpuid1eaxFms = object.cpuid1eaxFms ?? 0;
    message.launchMitVector = object.launchMitVector ?? 0n;
    message.currentMitVector = object.currentMitVector ?? 0n;
    return message;
  },
};

function createBaseCertificateChain(): CertificateChain {
  return {
    vcekCert: new Uint8Array(0),
    vlekCert: new Uint8Array(0),
    askCert: new Uint8Array(0),
    arkCert: new Uint8Array(0),
    firmwareCert: new Uint8Array(0),
    extras: {},
  };
}

export const CertificateChain: MessageFns<CertificateChain> = {
  encode(message: CertificateChain, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vcekCert.length !== 0) {
      writer.uint32(10).bytes(message.vcekCert);
    }
    if (message.vlekCert.length !== 0) {
      writer.uint32(50).bytes(message.vlekCert);
    }
    if (message.askCert.length !== 0) {
      writer.uint32(18).bytes(message.askCert);
    }
    if (message.arkCert.length !== 0) {
      writer.uint32(26).bytes(message.arkCert);
    }
    if (message.firmwareCert.length !== 0) {
      writer.uint32(34).bytes(message.firmwareCert);
    }
    globalThis.Object.entries(message.extras).forEach(([key, value]: [string, Uint8Array]) => {
      CertificateChain_ExtrasEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CertificateChain {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCertificateChain();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vcekCert = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.vlekCert = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.askCert = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.arkCert = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.firmwareCert = reader.bytes();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = CertificateChain_ExtrasEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.extras[entry7.key] = entry7.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CertificateChain {
    return {
      vcekCert: isSet(object.vcekCert)
        ? bytesFromBase64(object.vcekCert)
        : isSet(object.vcek_cert)
        ? bytesFromBase64(object.vcek_cert)
        : new Uint8Array(0),
      vlekCert: isSet(object.vlekCert)
        ? bytesFromBase64(object.vlekCert)
        : isSet(object.vlek_cert)
        ? bytesFromBase64(object.vlek_cert)
        : new Uint8Array(0),
      askCert: isSet(object.askCert)
        ? bytesFromBase64(object.askCert)
        : isSet(object.ask_cert)
        ? bytesFromBase64(object.ask_cert)
        : new Uint8Array(0),
      arkCert: isSet(object.arkCert)
        ? bytesFromBase64(object.arkCert)
        : isSet(object.ark_cert)
        ? bytesFromBase64(object.ark_cert)
        : new Uint8Array(0),
      firmwareCert: isSet(object.firmwareCert)
        ? bytesFromBase64(object.firmwareCert)
        : isSet(object.firmware_cert)
        ? bytesFromBase64(object.firmware_cert)
        : new Uint8Array(0),
      extras: isObject(object.extras)
        ? (globalThis.Object.entries(object.extras) as [string, any][]).reduce(
          (acc: { [key: string]: Uint8Array }, [key, value]: [string, any]) => {
            acc[key] = bytesFromBase64(value as string);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: CertificateChain): unknown {
    const obj: any = {};
    if (message.vcekCert.length !== 0) {
      obj.vcekCert = base64FromBytes(message.vcekCert);
    }
    if (message.vlekCert.length !== 0) {
      obj.vlekCert = base64FromBytes(message.vlekCert);
    }
    if (message.askCert.length !== 0) {
      obj.askCert = base64FromBytes(message.askCert);
    }
    if (message.arkCert.length !== 0) {
      obj.arkCert = base64FromBytes(message.arkCert);
    }
    if (message.firmwareCert.length !== 0) {
      obj.firmwareCert = base64FromBytes(message.firmwareCert);
    }
    if (message.extras) {
      const entries = globalThis.Object.entries(message.extras) as [string, Uint8Array][];
      if (entries.length > 0) {
        obj.extras = {};
        entries.forEach(([k, v]) => {
          obj.extras[k] = base64FromBytes(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<CertificateChain>): CertificateChain {
    return CertificateChain.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CertificateChain>): CertificateChain {
    const message = createBaseCertificateChain();
    message.vcekCert = object.vcekCert ?? new Uint8Array(0);
    message.vlekCert = object.vlekCert ?? new Uint8Array(0);
    message.askCert = object.askCert ?? new Uint8Array(0);
    message.arkCert = object.arkCert ?? new Uint8Array(0);
    message.firmwareCert = object.firmwareCert ?? new Uint8Array(0);
    message.extras = (globalThis.Object.entries(object.extras ?? {}) as [string, Uint8Array][]).reduce(
      (acc: { [key: string]: Uint8Array }, [key, value]: [string, Uint8Array]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCertificateChain_ExtrasEntry(): CertificateChain_ExtrasEntry {
  return { key: "", value: new Uint8Array(0) };
}

export const CertificateChain_ExtrasEntry: MessageFns<CertificateChain_ExtrasEntry> = {
  encode(message: CertificateChain_ExtrasEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CertificateChain_ExtrasEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCertificateChain_ExtrasEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CertificateChain_ExtrasEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0),
    };
  },

  toJSON(message: CertificateChain_ExtrasEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<CertificateChain_ExtrasEntry>): CertificateChain_ExtrasEntry {
    return CertificateChain_ExtrasEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CertificateChain_ExtrasEntry>): CertificateChain_ExtrasEntry {
    const message = createBaseCertificateChain_ExtrasEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseSevProduct(): SevProduct {
  return { name: 0, stepping: 0, machineStepping: undefined };
}

export const SevProduct: MessageFns<SevProduct> = {
  encode(message: SevProduct, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== 0) {
      writer.uint32(8).int32(message.name);
    }
    if (message.stepping !== 0) {
      writer.uint32(16).uint32(message.stepping);
    }
    if (message.machineStepping !== undefined) {
      UInt32Value.encode({ value: message.machineStepping! }, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SevProduct {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSevProduct();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.name = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stepping = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.machineStepping = UInt32Value.decode(reader, reader.uint32()).value;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SevProduct {
    return {
      name: isSet(object.name) ? sevProduct_SevProductNameFromJSON(object.name) : 0,
      stepping: isSet(object.stepping) ? globalThis.Number(object.stepping) : 0,
      machineStepping: isSet(object.machineStepping)
        ? Number(object.machineStepping)
        : isSet(object.machine_stepping)
        ? Number(object.machine_stepping)
        : undefined,
    };
  },

  toJSON(message: SevProduct): unknown {
    const obj: any = {};
    if (message.name !== 0) {
      obj.name = sevProduct_SevProductNameToJSON(message.name);
    }
    if (message.stepping !== 0) {
      obj.stepping = Math.round(message.stepping);
    }
    if (message.machineStepping !== undefined) {
      obj.machineStepping = message.machineStepping;
    }
    return obj;
  },

  create(base?: DeepPartial<SevProduct>): SevProduct {
    return SevProduct.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SevProduct>): SevProduct {
    const message = createBaseSevProduct();
    message.name = object.name ?? 0;
    message.stepping = object.stepping ?? 0;
    message.machineStepping = object.machineStepping ?? undefined;
    return message;
  },
};

function createBaseAttestation(): Attestation {
  return { report: undefined, certificateChain: undefined, product: undefined };
}

export const Attestation: MessageFns<Attestation> = {
  encode(message: Attestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.report !== undefined) {
      Report.encode(message.report, writer.uint32(10).fork()).join();
    }
    if (message.certificateChain !== undefined) {
      CertificateChain.encode(message.certificateChain, writer.uint32(18).fork()).join();
    }
    if (message.product !== undefined) {
      SevProduct.encode(message.product, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Attestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.report = Report.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.certificateChain = CertificateChain.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.product = SevProduct.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Attestation {
    return {
      report: isSet(object.report) ? Report.fromJSON(object.report) : undefined,
      certificateChain: isSet(object.certificateChain)
        ? CertificateChain.fromJSON(object.certificateChain)
        : isSet(object.certificate_chain)
        ? CertificateChain.fromJSON(object.certificate_chain)
        : undefined,
      product: isSet(object.product) ? SevProduct.fromJSON(object.product) : undefined,
    };
  },

  toJSON(message: Attestation): unknown {
    const obj: any = {};
    if (message.report !== undefined) {
      obj.report = Report.toJSON(message.report);
    }
    if (message.certificateChain !== undefined) {
      obj.certificateChain = CertificateChain.toJSON(message.certificateChain);
    }
    if (message.product !== undefined) {
      obj.product = SevProduct.toJSON(message.product);
    }
    return obj;
  },

  create(base?: DeepPartial<Attestation>): Attestation {
    return Attestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Attestation>): Attestation {
    const message = createBaseAttestation();
    message.report = (object.report !== undefined && object.report !== null)
      ? Report.fromPartial(object.report)
      : undefined;
    message.certificateChain = (object.certificateChain !== undefined && object.certificateChain !== null)
      ? CertificateChain.fromPartial(object.certificateChain)
      : undefined;
    message.product = (object.product !== undefined && object.product !== null)
      ? SevProduct.fromPartial(object.product)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
